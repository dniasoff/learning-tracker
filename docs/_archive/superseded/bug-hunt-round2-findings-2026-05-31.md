# Bug-hunt round 2 — confirmed 13/17 — ALL 13 FIXED ✅ (2026-05-31)

> **Status: RESOLVED.** All 13 confirmed findings fixed at root cause + regression-tested,
> committed green to `dev` in two passes:
> - Pass A (commit `90c7a20e`): R1 restore cross-profile, R6 notification blanket-cancel,
>   R8 scheduler all-review study-day, R9 back-date offset sign, R10 pace distinct-items,
>   R11 completion source, R12 streak watch-surfaces-errors, R13 v29 syncEnqueuedAt backfill.
> - Pass B (commit `aaeab9aa`): R2 reader insight-chips removed, R3 reader offline/error l10n,
>   R4 reader fake 15% progress bar removed, R5 sacred-time card l10n, R7 password dialogs l10n.
> Fixes done via parallel sub-agents on disjoint files; `make ci` (+8997) + `format-check` green.

## [high/correctness] Device restore content-import step is keyed to profile id 0 and never runs for the actually-restored profiles  (onboarding-restore)
- File: `lib/app/restore/device_restore_service.dart:188`
- Symptom: After a successful cloud pull on a new device, restore Step 2 calls _database.trackDao.getActiveTracksForProfile(_profileId) to derive which curricula to re-activate, and Step 3 imports/activates them. But _profileId is supplied from activeProfileIdProvider (restore_providers.dart:27), which resolves to the sentinel 0 on a fresh device (no profile selected yet — active_profile_provider returns 0). The pulled tracks belong to the restored profiles' real (non-zero) ids, so getActiveTracksForProfile(0) returns an empty list, activeCurriculaKeys is empty, and the import/activation step (active_curriculum + study_day_config seeding via CurriculumActivationService, which is also bound to profileId 0) silently no-ops for every restored profile.
- Root cause: DeviceRestoreService binds its restore work to a single _profileId captured from activeProfileIdProvider, which is 0 before any profile is selected — exactly the state on a clean-install restore. The service should derive active curricula across all just-pulled profiles (or per restored profile), not against profile 0.
- Fix: After pullOnLaunch, enumerate the restored profiles (db.profileDao.getProfilesByAccount(accountId)) and gather active tracks/curricula per restored profile id, then activate per profile, instead of querying/activating against the hard-coded _profileId 0. At minimum, replace getActiveTracksForProfile(_profileId) with a cross-profile query keyed to the restored profile ids.
- Verify: CONFIRMED real. The cited code path is provably broken on a clean-install restore.

Chain verified from code:
1. device_restore_service.dart:187-188 — Step 2 derives active curricula via getActiveTracksForProfile(_profileId). _profileId is injected from restore_providers.dart:27 = ref.watch(activeProfileIdProvider).
2. active_profile_provider.dart:19-29 — ActiveProfileId.build() returns `selected ?? 0`; doc comment line 15 states "Default value 0 represents the legacy/default profile." On a fresh device no profile is selected, so it is the sentinel 0.
3. restore_guard.dart:64-83 — restore is only triggered when the local DB is empty (no completions AND no profiles), i.e. BEFORE any profile is selected → active profile id is exactly the sentinel 0 at restore time. The production wiring captures profileId=0.
4. learner_profiles.dart:13 — profile id is integer().autoIncrement(); SQLite autoincrement starts at 1, so real profiles are never id 0. learner_profile_merger.dart:31-32 preserves the cloud profile_id as the natural key, so restored profiles keep their original non-zero ids. orchestrator comment (sync_orchestrator.dart:946-948) independently confirms "profile 0 before a profile is active."
5. track_dao.dart:153-161 — getActiveTracksForProfile(0) filters profileId.equals(0) → returns [] for every real (non-zero) profile.
6. device_restore_service.dart:189-202,213 — empty localActiveTracks → empty activeCurriculaKeys → the `if (activeCurriculaKeys.isNotEmpty)` guard is false → CurriculumImportService.importAll never runs (Step 3 silently no-ops).
7. This is load-bearing, not redundant: active_curriculum is NOT a synced EntityKind (entity_merger.dart lists trackConfig/studyDayConfig/etc. but no active_curriculum). It is never pulled from cloud; it is reconstructed ONLY by CurriculumActivationService.activate() (the import step). So skipping Step 3 leaves restored profiles with no active_curriculum rows + no re-seeded study_day_config defaults.
8. Secondary claim confirmed: curriculum_activation_providers.dart:11-24 binds CurriculumActivationService (used by CurriculumImportService) to activeProfileIdProvider (=0) too, so even if it ran it would activate against profile 0.

Reachability/by-design/defended/fixed checks:
- Reachable: every clean-install / reinstall cloud restore (restore_guard fires on empty DB).
- Not by-design: profile 0 is the "no profile selected" sentinel; the restore tests (test/integration/restore_skip_onboarding_test.dart:127,187) construct DeviceRestoreService with profileId:1 (non-zero), so the broken production path at profileId=0 is never exercised by tests — they accidentally mask the bug.
- Not already-defended and not in the fixed list: searched entity-model-remediation-log and planning docs; the 0-profile fixes there are about account-switch/empty-login routing, not the restore content-import keying. git log on the file shows no later fix.

Note for accuracy: I found the issue is actually broader than the report states — the per-profile collection pulls inside pullOnLaunch (sync_orchestrator.dart:701,749 → pull_pipeline _pullCollection → firestore_gateway_impl _collection at :1180-1183, path users/{uid}/learner_profiles/{profileId}/<collection>) are ALSO keyed to _profileId (resolve_profile_id_provider.dart:12 = activeProfileIdProvider = 0), so non-zero profiles' tracks/study_day_configs may not even be pulled on a fresh device. Only pullLearnerProfiles (fetchLearnerProfiles, account-wide) restores the profile rows themselves. This makes the report's precondition ("the pull merged curriculum_tracks under the restored non-zero ids") imprecise, but does not weaken the finding: the cited Step-2/3 code is independently and unambiguously broken (queries/activates against profile 0, no-ops for every restored profile). The proposed fix (enumerate restored profiles via getProfilesByAccount — which device_restore_screen.dart:86 already does for routing — and derive/activate per restored profile id) is correct.

## [medium/l10n] Reader injects hardcoded English placeholder "insight chips" for all users (incl. Hebrew)  (content)
- File: `lib/features/content_browsing/presentation/screens/text_display_screen.dart:387`
- Symptom: When the English translation of a segment happens to contain the substrings "priest", "time", or "watch", the reader renders pill chips reading "Vocabulary: Priests" and/or "Concept: Time" beneath the English text (built at line 243, rendered at lines 330-359). These English strings appear unconditionally — including for Hebrew-locale users — and are derived from a naive keyword heuristic, so they show up on unrelated passages (e.g. any text mentioning the word "time") and never on Hebrew content. They are clearly placeholder/demo content that shipped into the production reader.
- Root cause: `_buildInsightChips(String englishText)` (lib/.../text_display_screen.dart:387-395) hardcodes English literals 'Vocabulary: Priests' and 'Concept: Time' and selects them via `text.contains('priest')` / `contains('time')||contains('watch')` on the lowercased English text. There is no localization (no AppLocalizations key) and no real data source — it is a placeholder heuristic that bypasses the Hebrew/English UI locale entirely. Every other user-facing string in this file goes through AppLocalizations.
- Fix: Remove the placeholder `_buildInsightChips` heuristic and its render block (lines 330-360) entirely until a real, data-backed insight source exists; or, if the feature is intended, source the chip text from the content model and route every label through AppLocalizations so Hebrew users get Hebrew. Do not gate UI on English substring matching.
- Verify: CONFIRMED. The cited code matches exactly. `_buildInsightChips(String englishText)` at lib/features/content_browsing/presentation/screens/text_display_screen.dart:387-395 hardcodes English literals 'Vocabulary: Priests' and 'Concept: Time', selected by naive substring matching on the lowercased English text (`text.contains('priest')`, `contains('time')||contains('watch')`). It is invoked at line 243 and rendered unconditionally as pill chips at lines 330-360 whenever the list is non-empty — there is NO locale check anywhere, unlike every other user-facing string in this file (lines 85, 94, 137, 270, 313) which all route through AppLocalizations.

Adversarial verification:
- Reachable: TextDisplayScreen is annotated `@RoutePage()` (line 33), registered in lib/app/router/app_router.dart:189, and navigated to via TextDisplayRoute from 7+ real production surfaces (learning_screen.dart:374, active_track_card.dart:200/374, content_search_screen.dart:145, content_hierarchy_screen.dart:384, track_stat_grid.dart:93, daily_task_card.dart:57). Not a dev/debug-only screen. The chips render in the English section, which requires textContent.englishText.isNotEmpty (line 311).
- Not by-design: the real "learning insights engine" is explicitly listed as deferred post-MVP in docs/planning/prd.md:321 and architecture-design.md:421,1242. There is no data-backed source; it is a keyword heuristic.
- Not already defended / no locale gating: rendering at 330-360 has no AppLocalizations and no Directionality/locale guard. A Hebrew-locale user viewing any text with an English translation containing "priest"/"time"/"watch" sees raw English pill chips, and they fire as false positives on unrelated passages (any text mentioning "time").
- Not in the fixed list: no bug-log entry addresses it. On the contrary, the placeholder behavior is locked in by tests B1–B5 in test/.../text_display_screen_deep_l1_test.dart:343-446 (asserting find.text('Vocabulary: Priests')/('Concept: Time')) and is documented as expected "static heuristic" in docs/planning/on-device-exhaustive-test-plan-2026-05-31.md:2448 — meaning the proper fix must also delete those assertions/test rows.

The fix is sound: remove _buildInsightChips and its render block (and the B1–B5 tests + test-plan row), or back the chips with the content model and route labels through AppLocalizations.

Severity: medium, not high. It is genuinely user-visible (untranslated English noise chips, semantically wrong) but purely cosmetic — no crash, no data corruption, no blocked flow, no functional impact. It only surfaces when the English translation contains specific substrings.

## [medium/l10n] Reader offline/error fallbacks use hardcoded English strings (Hebrew users see English)  (content)
- File: `lib/features/content_browsing/presentation/screens/text_display_screen.dart:164`
- Symptom: When text for a ref is not cached (the core offline-first fallback, very common), the reader shows the English literals 'Text not available' (line 164) and 'Check your internet connection and try again.' (line 171). The error view shows 'Failed to load text' (line 204) plus a raw `error.toString()` (line 211). A Hebrew-locale user opening any not-yet-cached passage sees untranslated English, unlike the rest of the same screen which uses AppLocalizations (e.g. loadingText, textReaderHebrewTab).
- Root cause: `_OfflineMessage` and `_ErrorView` were written with string literals instead of AppLocalizations lookups. The app has a full Hebrew ARB (lib/l10n/app_he.arb) and the same file already localizes neighbouring strings, so these literals are an oversight, not an absence of infrastructure.
- Fix: Add ARB keys (e.g. textReaderTextUnavailableTitle, textReaderCheckConnection, textReaderFailedToLoad) to app_en.arb and app_he.arb and replace the literals at lines 164, 171, 204, and 'Failed to load text' at 204 with `AppLocalizations.of(context)!.<key>`. The raw `error.toString()` at line 211 should also not be surfaced verbatim to end users.
- Verify: CONFIRMED REAL. Verified all four hardcoded English literals in text_display_screen.dart: line 164 'Text not available' and line 171 'Check your internet connection and try again.' inside the private _OfflineMessage widget; line 204 'Failed to load text' and line 211 raw error.toString() inside _ErrorView. None use AppLocalizations.

REACHABILITY (very common path): The screen's build() renders `const _OfflineMessage()` when `textContent == null` (line 108) inside `textAsync.when(data: ...)`. textContentProvider (text_display_providers.dart:24) calls repository.getText(), which text_cache_repository.dart:71 explicitly documents as "cache-only — returns null if no source has it" and returns null at line 156 on a cache miss. Per the project's offline-first design, opening any not-yet-cached passage hits this path, so the offline fallback is routine, not an edge case. _ErrorView is reachable via the `error:` branch (line 118).

HEBREW INFRASTRUCTURE EXISTS + NEIGHBOURS ARE LOCALIZED: lib/l10n/app_he.arb (93KB) exists with real Hebrew translations for the same screen's keys — loadingText="טוען טקסט..." (line 990), textReaderTooltipPrevious/Next, textReaderHebrewTab, textReaderEnglishTab (lines 998-1001). The same file uses AppLocalizations.of(context)! at lines 85, 94, 137, 270, 313. So a Hebrew-locale user sees translated tooltips and loading text but untranslated English offline/error text — an inconsistency proving oversight, not an i18n-infrastructure gap.

NOT BY-DESIGN / NOT ALREADY-DEFENDED / NOT FIXED: The proposed ARB keys (textReaderTextUnavailableTitle/CheckConnection/FailedToLoad) do not exist in app_en.arb or app_he.arb (grep returned only unrelated chartFailedToLoad). The literals exist nowhere else as a localized alternative (an identical literal exists separately in core/widgets/app_error_view.dart:153 but that is a different, also-unlocalized widget the screen does NOT use — the screen rolls its own private _OfflineMessage/_ErrorView). The on-device test plan (docs/planning/on-device-exhaustive-test-plan-2026-05-31.md, rows 97/130) still documents the expected output as these English literals with no localization-fix annotation, confirming this is not in any fixed list.

Minor inaccuracy in the report text: it cites line 204 twice and slightly conflates which literals live in which widget, but the substance (4 hardcoded literals across 2 widgets, all reachable) is correct.

The error.toString() at line 211 leaking a raw error to end users is a valid secondary concern (poor UX / possible internal-detail exposure), corroborating the fix recommendation.

## [medium/ux] Reader shows a fake fixed 15% progress bar regardless of actual position  (content)
- File: `lib/features/content_browsing/presentation/screens/text_display_screen.dart:252`
- Symptom: Every passage in the text reader displays a LinearProgressIndicator pinned at 15% full (`value: 0.15`), giving the false impression that the user is 15% through the content no matter which ref they open or where they have scrolled. The bar never moves.
- Root cause: The progress indicator is hardcoded `value: 0.15` at lib/.../text_display_screen.dart:252 — it was never wired to scroll position, segment index, or curriculum completion. It is a static decorative value mistaken for real progress.
- Fix: Either bind `value` to a real signal (scroll offset fraction via a ScrollController, or the segment's completion ratio), or remove the progress bar until a real source exists. Do not display a static value styled as progress.
- Verify: Confirmed from code. At lib/features/content_browsing/presentation/screens/text_display_screen.dart:251-258, the reader renders a LinearProgressIndicator with a hardcoded `value: 0.15`, styled as real progress (brandBlue fill, rounded, minHeight 5) and positioned at the top of the reader as a progress affordance. Verified it is the only occurrence of `0.15` in the file (rg). It is not wired to any signal: `_TextContentView` is a StatelessWidget (line 224); there is NO ScrollController in the file (rg for ScrollController/controller: returned nothing); the body scrolls inside an uncontrolled SingleChildScrollView (line 263); there is no segment-index state or completion-ratio binding feeding the value. So every passage shows a bar pinned at 15% regardless of ref or scroll — exactly the reported symptom. Reachable: TextDisplayScreen is @RoutePage() (line 33) pushed via TextDisplayRoute from multiple live entry points — daily_task_card.dart:57, active_track_card.dart:200/374, content_hierarchy_screen.dart:384, plus search and learning screens — and the only branch that reaches _TextContentView requires non-null textContent (line 110), so it renders on every successful text open. Not by-design: no comment marks it as a placeholder, and surrounding comments describe genuine features. Not already-defended and not in any fixed list: git history for this file (HEAD) shows the literal still present with no commit touching the progress indicator. Severity adjusted from implied-high to medium: this is a misleading-UI/cosmetic defect, not a functional or data-integrity break. Actual completion lives in a separate _CompletionSection (line 380), so real progress tracking is unaffected; only the visual bar lies. It does not crash, block marking complete, or corrupt state.

## [medium/l10n] SacredTimeSettingsCard renders almost entirely in hardcoded English (no l10n) for Hebrew users  (settings-sacredtime)
- File: `learning_tracker/lib/features/sacred_time/presentation/widgets/sacred_time_settings_card.dart:43`
- Symptom: On a device whose UI is Hebrew, the Sacred Time card in Settings shows the explanatory paragraph (lines 43-45), the header label 'SHABBOS MODE' (line 85) and 'Always on' (line 95), the location-source labels 'Detected automatically' / 'Chosen from city list' / 'Manual coordinates' (lines 123-125), 'No location set' (line 116), the 'I am in Israel' row title (line 245) and its subtitle (lines 252-253), and every detect-outcome snackbar message (lines 214-222) all in English. Only the two action buttons (sacredTimeDetect/sacredTimeChooseCity) are localized.
- Root cause: These strings are inline string literals instead of AppLocalizations keys. The lock overlay was localized (sacredTimeLock* keys exist in both app_en.arb and app_he.arb) and the buttons were localized, but the settings card text was never extracted. The app auto-detects locale (en/he) with no language picker, so Hebrew users get a mixed/English card.
- Fix: Add l10n keys for each literal (e.g. sacredTimeCardDescription, sacredTimeShabbosModeLabel, sacredTimeAlwaysOn, sacredTimeSourceDetected/ManualCity/ManualCoords, sacredTimeNoLocation, sacredTimeInIsraelTitle/Subtitle, sacredTimeDetectOutcome* messages) in app_en.arb and app_he.arb, and replace the literals with AppLocalizations.of(context)! lookups (the snackbar builder in _showOutcome must also resolve via l10n).
- Verify: CONFIRMED REAL. Read sacred_time_settings_card.dart in full and verified every claimed literal against the code:

- Line 43-45: card description "App is silenced and locked during Shabbos and Yom Tov..." — hardcoded English literal.
- Line 85: header label 'SHABBOS MODE' — hardcoded `const Text('SHABBOS MODE', ...)`.
- Line 95: 'Always on' — hardcoded literal.
- Line 116: 'No location set' — hardcoded.
- Lines 123-125: source labels 'Detected automatically' / 'Chosen from city list' / 'Manual coordinates' — hardcoded switch arms.
- Line 245: 'I am in Israel' — hardcoded.
- Lines 252-253: subtitle "One-day chag if on. Auto-set when you detect or choose a city, flip if you are visiting." — hardcoded.
- Lines 214-222: all four _showOutcome snackbar messages — hardcoded English literals.

The ONLY two AppLocalizations lookups in the entire widget are lines 181 and 189 (sacredTimeDetect, sacredTimeChooseCity), exactly as the report states.

ROOT CAUSE confirmed: app_en.arb has exactly 10 sacredTime keys (lines 1032-1042) — 8 sacredTimeLock* keys for the lock overlay plus sacredTimeDetect/sacredTimeChooseCity. app_he.arb mirrors the same 10 keys (lines 1017-1027). None of the settings-card literals exist as l10n keys in either file (only unrelated key notifSacredTime = "SHABBOS MODE" exists for notifications, line 755 EN, not referenced by this card). So the lock overlay and buttons were extracted but the settings-card body was never localized.

REACHABILITY confirmed: SacredTimeSettingsCard is instantiated unconditionally (no locale/role gate) at settings_screen.dart:104 in the DEVICE section. SettingsScreen is @RoutePage() (line 37-38) and routed via AutoRoute(path: 'settings') in app/router/app_router.dart, pushed from onboarding/empty_login_screen.dart. So a Hebrew user navigating to Settings sees this card.

NOT by-design / NOT already-defended / NOT locale-branched: rg for Directionality/Localizations.localeOf/isHebrew/locale in the widget returns NONE — there is no alternate Hebrew rendering path. The app auto-detects locale (en/he) with MaterialApp.locale=null and no language picker (per MEMORY), so a Hebrew device gets a mixed card: two localized buttons surrounded by English literals.

Severity assessment: localization gap on a real, reachable Settings card affecting all Hebrew users for a prominent feature (Shabbos/Yom Tov lock). It is cosmetic/i18n (no data loss, no crash, no functional breakage — times still compute), so not high. It is a clear visible regression on a shipping surface in a bilingual-first app where Hebrew is a primary supported locale, so above trivial. Medium.

## [medium/regression] Daily-reminder batch is fully cancelled while Sacred Time is active, losing the next ~13 weekdays of reminders if the app isn't reopened  (settings-sacredtime)
- File: `learning_tracker/lib/features/notifications/presentation/providers/notification_providers.dart:459`
- Symptom: If the app is open when a Shabbos/Yom Tov window begins, reminderSyncEffect (and streakAlertSyncEffect / allProfilesReminderBootstrap) immediately cancels the ENTIRE 14-day rolling reminder batch via cancelForProfileSacredTime. The batch's individual fire-times were already per-fire-time filtered (NotificationScheduler.buildFireTimesForTest skips any fire-time inside a Sacred window), so the only fire-times the blanket cancel removes are the legitimate non-Shabbos weekdays. They are only re-scheduled when the app is next resumed (TimezoneLifecycleObserver invalidates reminderSyncEffectProvider) or when the CurrentSacredWindow 30s timer flips the window to null — both of which require the Dart isolate to still be alive. If the user closes/kills the app over Shabbos and doesn't reopen it for several days, no daily reminders fire on those weekdays even though scheduling them was safe.
- Root cause: The blanket 'cancel all when sacredTimeActive' guard predates / duplicates the per-fire-time suppression in buildFireTimesForTest. Once each fire-time is individually filtered, cancelling the whole batch on a live window-active state is redundant for Shabbos itself and actively drops the surrounding weekday reminders, relying on an app-resume to restore them.
- Fix: Remove the live `if (sacredTimeActive) { cancelForProfileSacredTime(...) ; return; }` early-return in reminderSyncEffect (notification_providers.dart:459-463) and the analogous branches in allProfilesReminderBootstrap (line 612) and streakAlertSyncEffect (line 697); always (re)schedule the per-fire-time-filtered batch. The suppression for fire-times that fall inside a Sacred window is already handled inside buildFireTimesForTest, so weekday reminders survive while Shabbos fire-times stay suppressed.
- Verify: CONFIRMED REAL. The blanket sacred-time cancel is reachable, redundant with per-fire-time filtering, and drops surrounding weekday reminders that only restore on a live isolate.

Code path: reminderSyncEffect (notification_providers.dart:459-463) does `if (sacredTimeActive) { await scheduler.cancelForProfileSacredTime(profileId); return; }`. cancelForProfileSacredTime (notification_scheduler.dart:112-120) calls cancelBatchRemindersForProfile, which cancels ALL 14 batch IDs (notification_gateway.dart:325-330). The same blanket guard is duplicated in allProfilesReminderBootstrap (line 612: `if (!reminderEnabled || sacredTimeActive) cancelForProfile`) and streakAlertSyncEffect (line 697).

Redundancy confirmed: buildFireTimesForTest (notification_scheduler.dart:182-207) already builds 14 one-per-day candidates starting tomorrow and skips any fire-time inside a Sacred window via SacredWindowRepository.isWindowActive (lines 196-204). So the scheduled batch already excludes Shabbos/Yom Tov fire-times; the blanket cancel only removes the legitimate non-Shabbos weekday one-shots. The provider doc comment (notification_providers.dart:415-416) explicitly calls the blanket cancel "the live lock-screen guard," i.e. a second layer on top of the per-fire-time suppression described at lines 411-413 — corroborating the root cause.

Batch is non-renewing: gateway uses zonedSchedule with explicit scheduledDate and no matchDateTimeComponents for batch entries (notification_gateway.dart:230-238, 312-321). Once cancelled the one-shots are gone.

Restore requires the isolate alive: the only re-schedule paths are TimezoneLifecycleObserver._onResume → ref.invalidate(reminderSyncEffectProvider) which fires only on AppLifecycleState.resumed (timezone_lifecycle_observer.dart:52-73), and the CurrentSacredWindow 30s in-isolate Timer flipping the window to null (sacred_windows_provider.dart:50-59). No WorkManager/background_fetch/periodic background rescheduler exists in lib; the flutter_local_notifications boot receiver only re-arms still-scheduled notifications, not cancelled ones. So if the app is killed over Shabbos and not reopened for several weekdays, those reminders never fire — matching the symptom.

Not by-design / not already-defended: the only test touching this (epic_27_story_14_analytics_test.dart:392-414) merely asserts cancelForSacredTime emits the suppression analytics event; it does not assert the blanket cancel as desired behavior. Nothing defends keeping the weekday batch alive during Shabbos.

The proposed fix (drop the live blanket-cancel early-returns and always reschedule the per-fire-time-filtered batch) is sound: Shabbos fire-times stay suppressed via buildFireTimesForTest while weekday reminders survive. (Note: the streak-alert branch at line 697 is not per-fire-time-filtered like the reminder batch — service.evaluate/scheduleStreakAlert is a single repeating alert, so that branch needs separate treatment rather than a blanket removal; the reminder-batch core of the bug is fully confirmed.)

## [medium/l10n] Change-password and re-authenticate dialogs use hardcoded English field labels and error messages  (settings-sacredtime)
- File: `learning_tracker/lib/features/settings/presentation/widgets/change_password_dialog.dart:79`
- Symptom: On a Hebrew UI, the Change Password dialog shows English field labels 'New Password' (line 79) and 'Confirm New Password' (line 93), and English validator/error text 'Password must be at least 6 characters' (line 82), 'Passwords do not match' (line 97), and 'Failed to change password. Please try again.' (line 55). The Re-authenticate dialog (reauthenticate_dialog.dart:76) shows English 'Current Password' and 'Invalid password. Please try again.' (line 57). These dialogs are reachable from the account-actions sheet opened via the Settings profile header.
- Root cause: These user-facing strings were left as inline literals while the surrounding dialog titles/buttons (changePasswordDialogTitle, reauthDialogTitle, reauthVerify, etc.) were localized — the field labels, validators, and catch-block error messages were never extracted to AppLocalizations.
- Fix: Add l10n keys (e.g. newPasswordLabel, confirmNewPasswordLabel, passwordMinLengthError, passwordsDoNotMatchError, changePasswordFailedError, currentPasswordLabel, invalidPasswordError) to app_en.arb/app_he.arb and replace the literals in both dialogs with AppLocalizations.of(context)! lookups.
- Verify: CONFIRMED REAL. Every cited literal exists verbatim in source. change_password_dialog.dart: line 79 `labelText: 'New Password'`, line 82 validator `'Password must be at least 6 characters'`, line 93 `labelText: 'Confirm New Password'`, line 97 validator `'Passwords do not match'`, line 55 catch-block `_error = 'Failed to change password. Please try again.'`. reauthenticate_dialog.dart: line 76 `labelText: 'Current Password'`, line 57 catch-block `_error = 'Invalid password. Please try again.'`. The mixed-localization is genuine: the surrounding chrome IS localized via AppLocalizations — changePasswordDialogTitle (line 64), actionCancel (108), changePasswordButton (118), reauthDialogTitle (65), reauthDialogBody (69), reauthVerify (96) — so under a Hebrew UI the title/buttons render Hebrew while these field labels, validators, and error messages leak English. Reachability confirmed end-to-end: user_profile_header_card.dart:99,150 -> showAccountActionsSheet -> account_actions_sheet.dart row at :197 (l10n.changePassword) -> _changePasswordFlow (:265) -> showReauthenticateDialog (:270) then showChangePasswordDialog (:277). The triggering row is gated on user.providers.contains('password') (:77), which correctly scopes it to email/password accounts — it is live for that real audience, not dead code, and the gate is the by-design auth check, not a defense against the i18n leak. Not already defended / not fixed: grep of lib/l10n/app_en.arb and app_he.arb finds neither the proposed keys (newPasswordLabel, confirmNewPasswordLabel, passwordMinLengthError, passwordsDoNotMatchError, changePasswordFailedError, currentPasswordLabel, invalidPasswordError) nor the English literal strings. No fix record in test-fix-bug-log.md or bug-hunt-findings-2026-05-31.md; the only doc reference (test-fix-bug-log.md:272 'change_password_dialog (23)') is a unit-test-count tally, not a fix of this i18n issue. Not by-design: locale is auto-detected (no MaterialApp.locale override per project MEMORY), so Hebrew devices genuinely show a Hebrew UI with English leaks. The proposed fix (extract to ARB keys + AppLocalizations lookups in both dialogs) is the correct remediation.

## [medium/correctness] Self-paced projection treats an all-review study-day config as "study every day" (new learning scheduled on review-only days)  (goals-scheduler)
- File: `lib/features/scheduler/presentation/providers/scheduler_providers.dart:743`
- Symptom: On a self-paced (chazara-enabled) track where the user toggles ALL seven days to review-only in the Study Days screen, the projection still schedules new-learning tasks every single day. The dashboard shows brand-new learning units to study on days the user explicitly marked as review-only, directly contradicting the configured study-day pattern. Note the same provider's isStudyDayMap (line 971, via isStudyDayForTrack) correctly reports today is NOT a study day, so the projection and the rest of the pipeline disagree about whether today is a study day.
- Root cause: At lib/features/scheduler/presentation/providers/scheduler_providers.dart:743-749 studyWeekdays is built only from rows whose dayType=='study'. When study_day_config rows EXIST but are all 'review', the comprehension yields an EMPTY set (the studyConfigs.isEmpty branch is not taken because rows exist). StudyDayPattern(studyWeekdays) is the projection's Set-based class in lib/features/scheduler/domain/projection/overdue_types.dart:60-63, where an empty weekdays set is interpreted as "study every day". So all-review collapses to all-study. selfPacedSchedule (overdue_schedule.dart:135) then accrues pace on every day. (A correctly-invariant StudyDayPattern VO exists at lib/core/domain/value_objects/study_day_pattern.dart:70-74 requiring >=1 study day, but the projection uses the other class.) There is no UI guard in study_day_config_screen._toggleDay (lib/.../study_day_config_screen.dart:184) preventing the user from turning every day to review.
- Fix: Disambiguate "no config (default = every day)" from "config exists but zero study days". Either (a) in scheduler_providers.dart:743 distinguish the two cases — when studyConfigs is non-empty but studyWeekdays is empty, treat it as a genuine zero-study-day pattern (schedule nothing / surface a validation error) rather than passing {} to the every-day-defaulting StudyDayPattern; or (b) add a guard in study_day_config_screen._toggleDay that refuses to set the last remaining study day to review (mirroring the >=1-study-day invariant already encoded in core/domain/value_objects/study_day_pattern.dart). Aligning the projection with isStudyDayForTrack semantics is required either way.
- Verify: CONFIRMED REAL. All claims verified against code.

ROOT CAUSE (scheduler_providers.dart:743-749): studyWeekdays is built by a comprehension filtering `if (c.dayType == 'study')`. When study_day_config rows exist but are all 'review', the `studyConfigs.isEmpty` branch is NOT taken (rows exist), so the comprehension yields an EMPTY set {}. `StudyDayPattern(studyWeekdays)` is then constructed with {}.

COLLAPSE TO EVERY-DAY (overdue_types.dart:48-63): This projection-local StudyDayPattern interprets an empty weekdays set as "study every day" — `isStudyDay()` at line 60-62 returns `true` for all weekdays when `weekdays.isEmpty` (line 61). The class comment at line 47 confirms "An empty set means 'study every day'". There is NO invariant requiring >=1 study day on this class.

ACCRUAL (overdue_schedule.dart:135): selfPacedSchedule gates accrual on `studyDayPattern.isStudyDay(cursor.weekday)`, which is unconditionally true here, so `pace` accrues on every calendar day and new-learning units get scheduled daily — contradicting the user's all-review configuration.

PIPELINE DISAGREEMENT (study_day_config_dao.dart): isStudyDayForTrack (line 147-157) returns `config == null || config.dayType == 'study'` — for an all-review day the row exists with dayType=='review', so it returns FALSE (correctly "not a study day"). countStudyDaysInInclusiveDateRangeForTrack (line 171-186) explicitly returns 0 when the study set is empty (line 186). So the rest of the pipeline correctly treats all-review as zero study days while the projection treats it as every-day — the exact disagreement described (isStudyDayMap at scheduler_providers.dart:971 uses isStudyDayForTrack).

TWO-CLASS CONFIRMED: The invariant-enforcing StudyDayPattern at core/domain/value_objects/study_day_pattern.dart:59-75 DOES require at least one study day (throws ArgumentError at line 70-74), but the projection path uses the OTHER class (overdue_types.dart), which has no such guard.

NO UI GUARD: study_day_config_screen._toggleDay (line 184-210) freely flips study<->review with no minimum-study-day check. The only studyCount usage (line 152-161) is a display-only "N study days per week" label; it does not prevent reaching 0. A user can set all 7 days to review.

REACHABILITY: The self-paced path (scheduler_providers.dart:696+) is reached for any self-paced track that has a goal with a resolvable pace; line 743 always runs. All-review config is user-reachable via the toggle screen.

NOT ALREADY DEFENDED / NOT IN FIXED LIST: No match for this scenario in docs/planning/bug-hunt-findings-2026-05-31.md, outstanding-bugs-handoff-2026-05-31.md, or test-fix-bug-log.md. No test in overdue_projection_test.dart or scheduler_providers_branches_test.dart exercises an all-review (dayType=='review' for all days) configuration.

SEVERITY NOTE: The reported symptom frames this as chazara-enabled, but the defect is actually independent of chazara — it affects ANY self-paced track where all study days are toggled to review. All-review is an atypical user configuration (most keep >=1 study day), which limits real-world frequency, but when it occurs the app schedules brand-new learning on days the user explicitly marked review-only and the projection disagrees with isStudyDayForTrack and the deadline-window counter. Genuine logic bug; medium severity.

## [medium/regression] Back-date offset sign is inverted between ProgramStartingPosition.toLegacyGrammar and TrackCreationService._parseProgramStartingRef  (goals-scheduler)
- File: `lib/features/tracks/setup/domain/services/track_creation_service.dart:297`
- Symptom: If a back-dated program enrolment is provisioned via the typed path (ProgramStartingPosition.toLegacyGrammar -> AddTrackResult.startingRef -> _parseProgramStartingRef), the program's trackingStartDate is written N days in the FUTURE instead of N days in the past. The program path in scheduler_providers.dart:542 then skips all task generation for a future anchor, so a program 'started 5 days ago' produces ZERO overdue and zero today tasks for 5 days — the exact opposite of the B3 back-date->overdue rule.
- Root cause: The canonical grammar (ProgramStartingPosition.fromLegacyGrammar, program_starting_position.dart:106-140, and its tests) defines offset:N as 'today MINUS N days'. toLegacyGrammar (program_starting_position.dart:151-154) emits a POSITIVE offset for a back-date (daysFromToday = today-start >= 0). But _parseProgramStartingRef does trackingStartDate = nowUtc().ADD(Duration(days: clampedOffset)) (track_creation_service.dart:297), so a positive offset:5 yields today+5 (future). The live add-track UI happens to avoid this because step_starting_position_calendar.dart:194 encodes a NEGATIVE offset (offset:-5), which .add() resolves correctly to the past; the two producers therefore use opposite sign conventions. ProvisionTrackUseCase (the typed producer) is not yet wired into a live screen, so this is currently a latent regression trap rather than a live break — and the B3 contract test (provision_track_use_case_test.dart) only asserts the startingRef string, never the resulting trackingStartDate, so it would not catch the inversion.
- Fix: Make _parseProgramStartingRef interpret offset:N with the canonical grammar: trackingStartDate = nowUtc().subtract(Duration(days: offset.clamp(0,30))) for the positive-past convention, OR unify the two producers on a single sign convention and adjust both consumers (track_creation_service.dart:297 and calendar_position_providers.dart:78). Add a regression test that asserts _parseProgramStartingRef('offset:5') yields a PAST trackingStartDate.
- Verify: CONFIRMED real (latent). The two sign conventions are provably inverted relative to the canonical grammar.

Canonical grammar = positive offset means PAST:
- program_starting_position.dart:140 fromLegacyGrammar: startDate = today.subtract(Duration(days: offset.abs())) — so "offset:5" resolves to today−5.
- program_starting_position.dart:150-154 toLegacyGrammar: offset = daysFromToday(today), and daysFromToday (line 96-99) = todayDay.difference(startDay).inDays, which is POSITIVE for a back-date. A 5-day back-date therefore emits "offset:5".

The typed producer uses this positive convention: provision_track_use_case.dart:78 calls pos.toLegacyGrammar(today) and the inline comment at 76-77 explicitly expects "the service writes trackingStartDate = today − offset". I confirmed ProvisionTrackUseCase is never constructed anywhere in lib/ (grep shows only doc-comment mentions in track_blueprint.dart and program_starting_position.dart, plus the test) — so it is not yet wired to a live screen.

The consumer is inverted: track_creation_service.dart:297 does trackingStartDate = DateTimeFactory.nowUtc().ADD(Duration(days: clampedOffset)). For "offset:5" this yields today+5 (FUTURE), the opposite of the canonical grammar and the producer's own comment. This trackingStartDate is written to profile_programs (track_creation_service.dart:140 and tracking_start_date column at :207) via the call at :79-81.

The scheduler then skips a future anchor: scheduler_providers.dart:533 sets anchor = enrollment.trackingStartDate, and :542 'if (!anchor.isAfter(todayDate))' gates all task generation — a future anchor produces zero overdue and zero today tasks, the exact opposite of the B3 back-date→overdue rule.

Why it is latent rather than live: the live add-track UI uses the OPPOSITE (negative) sign convention. step_starting_position_calendar.dart:73 _selectedDate = nowLocal().add(_offsetDays); :84 _minOffsetDays = minDate.difference(maxDate) (= −30, negative); :194 emits 'offset:$_offsetDays'. So the live UI sends offset:−5 for a 5-day back-date, and .add(−5) correctly lands in the past. _parseProgramStartingRef is therefore only correct for the negative-offset (live UI) producer and wrong for the positive-offset (typed) producer. calendar_position_providers.dart:78 has the same .add() convention, matching the live UI but not the typed grammar.

Why the test misses it: provision_track_use_case_test.dart:211-227 asserts only capturedResult.startingRef == 'offset:5' (the string), never routing it through _parseProgramStartingRef to check the resulting trackingStartDate. The B3-1..B3-5 projection tests (lines 276-402) build the anchor directly via today.subtract(...), bypassing the parser entirely. So the inversion is undetected.

This is reachable (through the typed ProvisionTrackUseCase path), not by-design (it contradicts the documented canonical grammar AND the producer's own inline comment), not already-defended, and not in any fixed list. The proposed fix is correct: unify the sign conventions (e.g. make _parseProgramStartingRef subtract for the positive-past convention, or normalize both producers/consumers) and add a regression test asserting _parseProgramStartingRef('offset:5') yields a PAST trackingStartDate.

## [medium/correctness] Pace calculation counts completion ROWS, not distinct items — chazara tracks falsely report 'ahead'  (bulk-lifetime)
- File: `lib/features/progress/presentation/providers/progress_providers.dart:224-231`
- Symptom: On a chazara-enabled track (stages learn+chazara1+chazara2), every fully-completed item produces 3 completion_events rows. The pace status (rendered as 'Ahead by N days' in pace_indicator.dart) over-counts live progress by the stage multiplier (up to 3x), so the track shows a false 'ahead' status and an inflated days-ahead number. The bulkBaseline is likewise 3x-inflated, which can drive (totalItems - bulkBaseline) negative and clamp requiredVelocity to 0 (degenerate pace). This is the same class of 'phantom Ahead' bug the F2 fix (see the doc comment at lines 171-175) was meant to eliminate, reintroduced for multi-stage tracks.
- Root cause: liveProgress and bulkBaseline are computed as allCompletions.where(...).length — counting completion_events rows. allCompletions (db.completionDao.getCompletionsByCurriculumAndProfile) returns one row per (sefariaRef, stageId), so multi-stage tracks yield N_items x N_stages rows. But totalItems comes from scopedItemCountProvider, which counts DISTINCT leaf items (curriculum_scope_providers.dart:83, items.where((i)=>i.isLeaf).length) with no stage multiplier. Comparing a row count against an item-based velocity/target over-counts progress. The identical defect exists in lib/features/tracks/setup/presentation/screens/track_detail_screen.dart:51-65 (_trackPaceCalcProvider uses getCompletionsByTrackAndProfile(...).where(...).length).
- Fix: Count DISTINCT sefariaRefs that are FULLY complete (all required stages present), or at minimum distinct sefariaRefs, instead of raw rows — consistent with TrackCompletionService.computeTrackPercentage. e.g. group allCompletions by sefariaRef, then liveProgress = number of distinct refs whose completions are all dated >= trackStartDate (or simply allCompletions.map((c)=>c.sefariaRef).toSet() partitioned by date when stage-completeness isn't required). Apply the same change to track_detail_screen.dart's _trackPaceCalcProvider so both pace surfaces use an item-based numerator that matches the item-based totalItems denominator.
- Verify: CONFIRMED REAL. Verified end-to-end in code:

1. ROW vs ITEM mismatch confirmed. `getCompletionsByCurriculumAndProfile` (completion_dao.dart:177-189) and `getCompletionsByTrackAndProfile` (587-597) both `select(completionsView)` with NO stageId filter and return `rows.map(_fromView)` — one row per underlying completion_events row. completion_events has a UNIQUE key on (profileId, sefariaRef, stageId, trackType, curriculumId) (completion_events.dart:21-22) and completionsView projects stage_id (completions_view.dart:27), so each stage of the same sefariaRef is a distinct row. markComplete writes one row per stage (completion_repository_impl.dart:253/322/485 stageId: request.stageId). Chazara stages are real and separate: 'חזרה א׳' / 'חזרה ב׳' in stage_definition_repository_impl.dart. So a fully-completed item on a learn+chazara1+chazara2 track yields 3 rows.

2. The pace numerators are raw row counts: progress_providers.dart:224-231 (bulkBaseline / liveProgress = allCompletions.where(dateFilter).length) and track_detail_screen.dart:51-65 (liveCount / bulkBaseline = .where(...).length). The .where clauses filter only by date, never by stage — no defense.

3. The denominator is item-based: scopedItemCountProvider (curriculum_scope_providers.dart:76-84) returns items.where((i)=>i.isLeaf).length — distinct leaf items, no stage multiplier. Passed as totalItems to PaceCalculator.compute (progress_providers.dart:254, track_detail_screen.dart:85).

4. PaceCalculator.compute (pace_calculator.dart:135-202) computes requiredVelocity = (totalItems - bulkBaseline)/trackDays (item-based denom, row-based bulkBaseline) and paceVariance = liveProgress(rows) - expectedProgressToday(items); ahead when paceVariance > requiredVelocity. Worked example: 100-item, 100-day chazara track, day 50, perfectly on pace (50 items fully done) → liveProgress=150 rows, expected=50, paceVariance=100 > requiredVelocity 1 → PaceStatus.ahead, paceVarianceInDays=100. Rendered as 'Ahead by 100 days' (pace_indicator.dart:40-42 paceAheadByDays). False ahead confirmed.

5. Negative-bulkBaseline degenerate case confirmed: bulk-marking 40 items on a 3-stage track → bulkBaseline=120 > totalItems=100 → (totalItems-bulkBaseline) clamped to 0 (pace_calculator.dart:149-150) → requiredVelocity 0 → paceVarianceInDays 0 (degenerate pace).

6. Not by-design / matches the cited 'correct' code: TrackCompletionService.computeTrackPercentage (track_completion_service.dart:36-49) and CurriculumProgressService.compute/computeCompletionPercentage (curriculum_progress_service.dart:32-52, 93) both group by sefariaRef and require all stageOrders present — item-based, deduped. The DAO even ships getAggregateCountByProfile using COUNT(DISTINCT sefariaRef) 'so that completing the same ref at multiple stages counts once' (completion_dao.dart:206-225). The pace path is the lone offender mixing units.

7. Reachable: both providers are watched by live UI (curriculum_progress_screen.dart watches curriculumPaceStatus; track_detail_screen.dart watches _trackPaceCalcProvider). This re-introduces exactly the F2 'phantom Ahead' class (doc comment progress_providers.dart:171-175) for multi-stage tracks.

Severity adjusted to medium (from an implied high): the defect is display-only — it corrupts the user-facing pace indicator on every chazara-enabled track but does not affect scheduling, completion credit, or stored data; single-stage tracks (the common case) are unaffected; and the degenerate branch fails safe (clamps to onTrack/grace, no crash). Misleading motivational signal, not a functional break.

## [medium/correctness] prior_completion_imports.source is hard-coded to 'bulkInTrack' — 'lifetimeOnly' provenance can never be persisted  (bulk-lifetime)
- File: `lib/features/learning/data/completion_writer.dart:200-203`
- Symptom: The prior_completion_imports.source column is documented (prior_completion_imports.dart:48-54) as the single source of truth distinguishing bulkInTrack from lifetimeOnly imports, and the three-tier filter getCompletionsByTier relies on it (completion_dao.dart:817-819 keys trackAchievement on pci.source = 'bulkInTrack'). But CompletionWriter is the ONLY writer of this table and it always stores 'bulkInTrack'. Every consumer branch that handles source == 'lifetimeOnly' (lifetime_knowledge_providers.dart:100, items_learned_providers.dart:447, lifetime_tree_builder.dart) is dead code, and lifetimeRefs partitions are always empty. If/when a lifetimeOnly completion ever reaches this writer (e.g. MarkCompletionUseCase.call(source: lifetimeOnly) -> markComplete -> priorMarkOnly=true, or any future BulkMarkCompletionUseCase wiring), it would be mis-tagged 'bulkInTrack' and would leak into the trackAchievement tier (Items Learned, charts, recent activity, siyumim) which must EXCLUDE lifetimeOnly per B1.
- Root cause: In both write paths the source is derived solely from cmd.priorMarkOnly: at line 200-203 the entries are pre-filtered to priorMarkOnly==true so the ternary always yields 'bulkInTrack'; the single-row commit() path hard-codes source: 'bulkInTrack' at line 682. CompletionCommand carries no CompletionSource discriminator, so the writer cannot tell bulkInTrack from lifetimeOnly. (BatchPlan exists to carry this distinction but has no callers, so it never reaches the writer.)
- Fix: Add a CompletionSource (or an explicit source string) to CompletionCommand and write it through to PriorCompletionImportsCompanion.source in both completion_writer.dart locations (lines ~194-204 and ~675-684). Map bulkInTrack->'bulkInTrack', lifetimeOnly->'lifetimeOnly'. Then have CompletionRepositoryImpl populate the command's source from request.creditsAchievement (creditsAchievement && priorMarkOnly -> bulkInTrack; !creditsAchievement && priorMarkOnly -> lifetimeOnly), so the tier filter correctly excludes lifetimeOnly imports.
- Verify: CONFIRMED REAL (latent design defect; severity reduced high→medium because no current runtime producer of lifetimeOnly reaches this writer).

Verified claims from code:
1. CompletionWriter is the sole writer of prior_completion_imports and never persists 'lifetimeOnly'. Batch path (completion_writer.dart:191-205) pre-filters to priorMarkOnly==true (line 192) then ternary `cmd.priorMarkOnly ? 'bulkInTrack' : 'live'` (200-202) — always 'bulkInTrack'. Single-row path (674-682) is guarded by `if (cmd.priorMarkOnly)` and hard-codes source:'bulkInTrack'.
2. CompletionCommand (completion_command.dart:19-31) carries only `bool priorMarkOnly`, no CompletionSource — so the writer cannot distinguish bulkInTrack from lifetimeOnly. BatchPlan (batch_plan.dart) carries the distinction but has zero callers.
3. Consumer lifetimeOnly branches are dead / partitions always empty: items_learned_providers.dart:443-449 (lifetimeRefs, case 'lifetimeOnly':447) and lifetime_knowledge_providers.dart:96-101 (case 'lifetimeOnly':100). The tier filter completion_dao.dart:819 keys trackAchievement on pci.source='bulkInTrack', so any mis-tagged row would leak into Items Learned/charts/siyumim, violating B1 (prior_completion_imports.dart, completion_source.dart:11).
4. Reachability is genuine but latent: MarkCompletionUseCase.call(source: lifetimeOnly) → markComplete(awardGamificationPoints:false,creditsAchievement:false) (impl:87-91) → _createCompletion(priorMarkOnly:!awardGamificationPoints=true) (impl:154-160, 666-689) → writer writes 'bulkInTrack' (682). Bulk path mirrors it: _bulkSourceFor returns lifetimeOnly when !creditsAchievement (impl:470-473) yet _bulkMarkCompletePriorOptimized builds CompletionCommand(priorMarkOnly:true) with no source (impl:340-356).

Why medium not high: grep across lib/ shows NO current caller passes source: CompletionSource.lifetimeOnly to either use case; the use-case defaults are live/bulkInTrack; the only BulkCompletionRequest producers set creditsAchievement:true (bulk_prior_completion_service.dart:257, bulk_mark_completion_use_case.dart). The real lifetimeOnly historical-import feature writes to a DIFFERENT table (learning_ledger via LearningLedgerRepositoryImpl, default source=lifetimeOnly at line 133), bypassing prior_completion_imports — which is exactly why the consumer branches stay empty. So there is no production behavior leak today; it is dead infrastructure plus a latent mis-tagging trap that fires as soon as a lifetimeOnly source is wired through these use cases (which the `source` params and _bulkSourceFor explicitly invite). Not by-design (the table doc, enum, and tier filter all intend the distinction), not already-defended, not in a fixed list. The proposed FIX (add CompletionSource to CompletionCommand, populate from request.creditsAchievement matching the existing _makeSource at impl:466-474, write through both writer locations) is correct.

## [medium/regression] D17: rewritten streak watch() silently swallows restoreIfEmpty errors in onListen, leaving the streak stream stuck with no emission (perpetual loading) instead of surfacing the error  (review-recent-fixes)
- File: `learning_tracker/lib/features/gamification/streak/streak_state_provider.dart:90-115`
- Symptom: If `_restorer.restoreIfEmpty(profileId)` throws — e.g. during a profile switch / account removal when userDatabaseProvider has just been invalidated and the Drift handle is closing (the exact D20 teardown sequence) — the streak stream never emits a value or an error. The dashboard streak card stays on its loading/zero state with no recovery, whereas the previous async* implementation propagated the throw as a stream error the consumer could observe.
- Root cause: The D17 rewrite moved from an `async*` generator (where `await _restorer.restoreIfEmpty(...)` before the first yield would surface a throw as a stream error) to a manual StreamController whose `onListen` is an `async` void callback. An exception thrown inside `onListen` is an unhandled async error that is never forwarded to controller.addError, so eventsSub/tickSub are never wired and the controller never closes or emits.
- Fix: Wrap the onListen body in try/catch and forward failures to the controller, e.g. `try { await _restorer.restoreIfEmpty(...); ...wire subs... } catch (e, st) { if (!controller.isClosed) { controller.addError(e, st); await controller.close(); } }`, so a restore/DB error reaches the consumer instead of hanging the stream.
- Verify: CONFIRMED REAL. The cited code at streak_state_provider.dart:90-115 sets `controller.onListen = () async { await _restorer.restoreIfEmpty(...); ...wire eventsSub/tickSub... }`. There is no try/catch around the body. I empirically verified the Dart semantics (standalone test): when a StreamController's `onListen` is an async function that throws after an await, the throw becomes an UNHANDLED async error — it does NOT reach the listener's onError, onDone is never called, and no data is emitted. The stream simply hangs. So if `_restorer.restoreIfEmpty(profileId)` throws, eventsSub/tickSub are never wired and the controller never closes or emits → perpetual loading on the consumer.

This is a genuine D17 regression, confirmed by git: commit 7d455c5d (D16/D17) replaced the prior implementation. The diff shows the exact transition the report describes — from `Stream<StreakState> watch(...) async* { await _restorer.restoreIfEmpty(...); yield* ...}` to `final controller = StreamController(); controller.onListen = () async { await _restorer.restoreIfEmpty(...); ...}`. In the old async* generator, a throw from `await restoreIfEmpty` before the first yield surfaced as a STREAM ERROR the consumer's `await for`/`yield*` could observe; in the new form it is silently swallowed.

Reachability of the throw is real: restoreIfEmpty does live Drift reads (`_db.select(_db.streakEvents)...getSingleOrNull()` and `_db.completionDao.getCompletionsByProfile(profileId)`) on the injected UserDatabase. database_provider.dart:32-38 shows `userDatabaseProvider` is keepAlive, watches `accountDbFileNameProvider`, and `ref.onDispose(database.close)` closes the old handle on a profile switch / account swap. Consumers (dashboardStreak async* at dashboard_providers.dart:252-265; streakMilestoneAnalyticsObserver at streak_milestone_analytics_observer.dart:45) `ref.watch(userDatabaseProvider)` then `await for`/`yield*` the watch() stream. During the swap/teardown window an in-flight restoreIfEmpty on the closing Drift handle can throw a 'database closed'/StateError, which is then swallowed.

Not by-design: the onCancel branch carefully closes the controller, and `if (!controller.isClosed)` guards exist on add() — the author clearly intended robust lifecycle handling but missed the onListen error path. Not already-defended: no try/catch, no zone guard, no addError on the init path. The proposed fix (wrap onListen body in try/catch and forward to controller.addError + close) is correct and restores the pre-D17 error-propagation behavior.

Severity LOWER than implied 'perpetual loading forever': the trigger is a fairly narrow race (restore throwing specifically during the DB-close window of a profile/account swap), and because consumers ref.watch(userDatabaseProvider) they rebuild and establish a FRESH stream on the new DB, which normally succeeds — so the hang is typically transient/on the dying handle rather than a permanent dead dashboard. Real correctness regression worth fixing (silent error swallowing + a hangable stream), but not a high-frequency permanent-breakage; medium.

## [low/regression] D14: v29 migration leaves syncEnqueuedAt NULL on all existing ledger rows, so the reconciliation re-pushes every historical points-ledger entry on first post-upgrade launch  (review-recent-fixes)
- File: `learning_tracker/lib/core/database/user/user_database.dart:255-260`
- Symptom: On the first launch after upgrading to schema v29, a cloud-born account with existing points history floods the outbox: PointsBalanceDao.reEnqueueUnsyncedLedgerRows() enqueues a fresh outbox row for EVERY pre-existing ledger entry (completions, parent adjustments, redemption debits/refunds) and re-pushes each to Firestore, even though they were already synced. Observable as a one-time outbox-depth spike, a burst of N Firestore writes, and the sync-status badge briefly showing a large 'pending' backlog.
- Root cause: The v29 migration (`if (from < 29) m.addColumn(pointsLedger, pointsLedger.syncEnqueuedAt)`) is additive-nullable and does NOT backfill the marker for rows that were already pushed under v27/v28. reEnqueueUnsyncedLedgerRows() (points_balance_dao.dart:568-588) selects every row WHERE sync_enqueued_at IS NULL with no created-at floor, so all legacy rows qualify and get re-enqueued via _enqueueLedgerInTransaction. The re-push is idempotent on the receiver (deterministic ulid doc-id), so data is not corrupted, but the work/backlog is needless.
- Fix: In the v29 migration, backfill the marker for rows that have already been synced before declaring them unsynced — e.g. after addColumn, run `await m.database.customStatement('UPDATE points_ledger SET sync_enqueued_at = created_at WHERE ulid IS NOT NULL')` so only genuinely never-enqueued rows (ulid NULL, or future rows) are picked up by the reconciliation. Alternatively gate reEnqueueUnsyncedLedgerRows with a per-profile 'reconciled once' flag.
- Verify: CONFIRMED REAL (severity adjusted down to low). The mechanism holds against the code:

1) v29 migration is additive-nullable with NO backfill. user_database.dart:255-260: `if (from < 29) { await m.addColumn(pointsLedger, pointsLedger.syncEnqueuedAt); }`. There is no `UPDATE points_ledger SET sync_enqueued_at ...` anywhere in lib/core/database (grep confirms zero backfill statements).

2) Pre-D14 (schema v27/v28) ledger rows carry a non-NULL ulid. `_applyDeltaInTransaction` stamps `ulid: Value(newUlid(now))` on every insert (points_balance_dao.dart:501), and v27 added the nullable ulid column (user_database.dart:252). Before D14 these rows were pushed to Firestore via the old `_pushLedgerEntry → sink.enqueuePointsLedgerEntry` path (confirmed in the D14 diff of commit 9fa15974, which removed that path). The sync_enqueued_at column did not exist until v29, so those already-synced rows cannot have been stamped.

3) reEnqueueUnsyncedLedgerRows (points_balance_dao.dart:568-588) selects WHERE `profileId == p & syncEnqueuedAt.isNull() & ulid.isNotNull()` (lines 574-576). The already-synced v27/v28 rows satisfy BOTH predicates after the v29 migration (ulid != NULL, marker == NULL), so they are re-enqueued via `_enqueueLedgerInTransaction` and re-pushed.

4) Reachable: wired at two composition roots right after sink registration — sync_orchestrator_providers.dart:94 and sync_providers.dart:153 — so it fires on first post-upgrade launch.

Not by-design: the method's own docstring says its purpose is to recover the null-sink window, not to re-push rows already pushed under v27/v28; re-pushing those is unintended needless work. Not already-defended: the existing `ulid IS NOT NULL` guard does NOT protect against this — the offending rows have non-NULL ulid. (The report's stated WHERE clause omits the `ulid IS NOT NULL` clause and overclaims that 'all legacy rows qualify' — pre-v27 rows with NULL ulid are correctly excluded — but its core conclusion is right for the v27/v28 population, and its PRIMARY proposed fix, backfilling sync_enqueued_at=created_at in the v29 migration, is the correct remedy; note the report's secondary suggestion to add `WHERE ulid IS NOT NULL` is a no-op since it is already present.)

Severity lowered to low: (a) the report itself acknowledges data is NOT corrupted — receiver merge is idempotent on the deterministic ulid doc-id — so the only impact is a one-time outbox spike / redundant Firestore writes / a transient pending-badge; (b) per project memory the app is confirmed pre-launch with no live users and schema/Firestore resets are explicitly safe (no in-place upgrade window), so in practice essentially no account carries v27/v28 ledger data that would hit this path. Real defect, but cosmetic/efficiency-grade and effectively unreachable in production today.

