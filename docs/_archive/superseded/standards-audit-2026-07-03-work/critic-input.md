# Critic input — coverage snapshot (audit 2026-07-03)

## Per-batch: files / findings / coverage statement

### t1-app-l10n-p1 (23 files, 9 findings)
Read all 18 non-empty files in the batch in full (5 .gitkeep files are 0 lines, confirmed trivial), plus app_en.arb (1421 unique keys across 3040 lines), which I audited programmatically rather than visually: a full JSON parse, a full-file duplicate-top-level-key scan, an ICU-placeholder-consistency check, and a brace-balance check, all covering every key rather than a sample — this surfaced the 5-duplicate-key finding that a manual skim would likely have missed. Both HOTSPOT files (app_router.dart, app_shell.dart) got the deepest pass, including grep sweeps for Rule 1/3/4/5/Hebrew-terms viola

### t1-app-l10n-p2 (12 files, 6 findings)
Read all 12 files in full. lib/l10n/app_he.arb (2740 lines) was additionally cross-checked programmatically against app_en.arb and the generated app_localizations_{en,he}.dart for EN/HE key parity (1421/1421, matches baseline), duplicate top-level keys, and untranslated/empty content. The two largest test files were checked against AG-3's 400-line cap and for assertion density (grep for `expect(`). Every test file that claims to guard a specific production bug or behavior (AN-6, AN-8, the phase-label mapping, the double-read fix) was diffed against the real production symbol it claims to test,

### t1-app-l10n-p3 (5 files, 3 findings)
Read all 5 test/l10n files in full and ran them via `flutter test` (152/152 passed), confirming none are stale or broken. Cross-checked every assertion against the live lib/l10n/app_en.arb, app_he.arb, and generated AppLocalizations*.dart content (not just internal self-consistency) to rule out tautological or stale regression tests — all assertions in hebrew_shabbos_strings_il9_test.dart, icu_plural_il3_test.dart, and tier_counter_pluralization_test.dart matched the real ARB/generated-code values exactly, including Hebrew one/two/other plural forms. Grepped lib/ for call sites of all 40 keys 

### t1-core-analytics-logging (17 files, 7 findings)
Read all 17 files in the batch in full (core/analytics + core/logging plus their tests, ~2448 lines) and checked each against the digest's applicability sweep (PV-1..6/AU-1..5 in full per the area note, plus SM/EH/AG/TQ/Fowler-Feathers-Evans-Nygard lenses). Per the area note, grepped the wider repo (core/sync/outbox_processor.dart, pull_pipeline.dart, sync_orchestrator.dart, merge/*, features/tutoring/*, completion_writer.dart, main.dart, app/bootstrap/*) to trace every caller of AnalyticsService.logEvent and the LogEvents.sync/.tutor constants, which surfaced a systemic PV-1 gap well beyond t

### t1-core-auth (8 files, 7 findings)
Read all 8 files in full and ran the providers/gateway applicability sweep (SM-1..8, EH-1..6, Rule 3/4) plus the full PV-1..6 and AU-1..5 area-note sweep; PV rules don't apply here (no analytics/Crashlytics/ads code anywhere in this batch). Verified two factual doc-comment claims via wider-repo greps (firebase_auth import confinement to core/auth+core/sync; google_sign_in full-package import confined to the gateway with only exception types imported elsewhere) — both accurate, no drift — and traced the real callers of the flagged link/reauth methods into features/account/data/repositories/auth

### t1-core-content (7 files, 10 findings)
Read all 7 files in full (content_grouping.dart, content_index.dart, content_tree.dart, hierarchy_browser.dart, hierarchy_selection.dart, program_ref_resolver.dart, coarse_unit_leaf_refs_test.dart, ~978 lines) plus their generated .g.dart siblings, ran the Rule-1..5/SM/EH/AX/PF/AG applicability sweep (provider rules for the two @Riverpod(keepAlive) functions, widget rules for hierarchy_browser.dart, naming/placement for the plain classes, TQ for the test file), and grepped the wider repo for every exported symbol's callers and tests to separate baselined items from novel ones. Both core/conten

### t1-core-database-daos-p1 (31 files, 11 findings)
Read all 25 DAO files and 6 test files on the batch list in full, plus base_dao.dart, the ~21 Drift table definitions they map to, the learning_tracker/Makefile audit target, docs/coding-standards.md's profileId-in-PK section, and four out-of-batch callers (goal_repository_impl.dart, bookmark_repository_impl.dart, stage_definition_repository_impl.dart, sacred_window_repository.dart, track_learning_order_repository_impl.dart) to confirm or rule out exploitability of gaps found in the DAO layer. Verified DB-2/DB-3 transaction-and-batch discipline on every multi-row write path in the batch, and c

### t1-core-database-daos-p2 (28 files, 6 findings)
Read all 28 files in the batch in full (every Drift DAO test under test/core/database/daos, ~6489 lines) plus the shared test/helpers/drift_memory.dart fixture, and cross-referenced lib/core/database/daos/points_balance_dao.dart (grep) to confirm one finding and to sanity-check DB-2/DB-3 transaction/batch discipline as revealed through the tests. Every file uses a fresh inMemoryDb() per test with proper tearDown, hand-rolled fakes only (no mockito/mocktail), no empty/log-less catches, and no raw Talker/print, so EH-3/EH-4/Rule-4/TQ-2/TQ-4 clear cleanly across the whole batch; batch()/transacti

### t1-core-database-daos-p3 (13 files, 6 findings)
Read all 13 batch files in full (2962 lines) — every DAO test under test/core/database/daos/{text_download_status,track,track_learning_order,track_scoped,user_profile}*_test.dart. Per the AREA NOTE, also read the production DAOs these tests exercise (track_dao.dart, text_download_status_dao.dart, track_learning_order_dao.dart, user_profile_dao.dart, active_curriculum_dao.dart, base_dao.dart) plus one repository consumer (track_learning_order_repository_impl.dart) to check DB-1..6/transaction/batch discipline, and ran tool/schema_check.dart (profileId-in-PK checker — passes, 13/13; confirmed Tr

### t1-core-database-root-p1 (28 files, 9 findings)
Read all 28 files in full (~6,293 lines): 17 lib/core/database sources (ContentDatabase + its 4 DAOs, DeviceRegistryDatabase + tables, SeedManager, ContentDbHealthChecker, UserDatabase HOTSPOT, BaseDao, CompletionsView, TrackScope, seed data/version) and 11 test files. Verified DB-1..6 and profileId-in-PK against every table/DAO/migration in scope: profileId-in-PK doesn't literally bind here since the only tables defined in this batch (DeviceAccounts, DeviceState) are device-scoped, not profile-scoped, and are correctly exempt — the actual per-profile tables in user_database.dart live under li

### t1-core-database-root-p2 (7 files, 5 findings)
Read all 7 files in full (5641 lines: seed_manager_test.dart 794, text_cache_dao_test.dart 33, track_scope_test.dart 63, user_database_companion_coverage_test.dart 1109, user_database_dataclass_core_test.dart 1176, user_database_dataclass_extended_test.dart 1613, user_database_dataclass_test.dart 853). Cross-diffed group names and Companion `.insert()`/`.custom()` signatures across the four large dataclass/companion files to detect duplication and profileId omission, then grepped lib/core/database/tables/track_learning_order.dart and the Makefile for corroborating context (both outside this ba

### t1-core-database-root-p3 (5 files, 3 findings)
Read all 5 assigned files in full (6,616 combined lines: 1308+916+804+477+2435; the 2435-line orderby file was read directly in 200-900-line windows across 3 Read calls plus a full-file grep sweep of every test(), assertion style, TODO/FIXME, DateTime.now(), and commented-code pattern to eliminate blind spots outside the windows read). Cross-checked `grep -rl '\.managers\.' lib/` (0 hits repo-wide) to confirm the Drift generated Manager API these 5 files exist to exercise has zero production callers, and ran `flutter test` on the smallest file (crossref_filter_test.dart: 26/26 green) and the l

### t1-core-database-root-p4 (2 files, 5 findings)
Both assigned files were read in full (676 + 1073 lines) and cross-checked against lib/core/database/user/user_database.g.dart (grepped for the non-nullable return types of every *Refs getter and for the real `completionEventsRefs` member name), lib/core/database/daos/profile_dao.dart (confirmed ProfileDao is a plain DatabaseAccessor, not an extension, and is unused in the test), analysis_options.yaml (confirmed unused_import is not in the hand-rolled lint set), test/helpers/drift_memory.dart (context for the completions→completionEvents rename), and the sibling test/core/database/*.dart files

### t1-core-database-tables (28 files, 8 findings)
Read all 28 files in learning_tracker/lib/core/database/tables/ in full (27 Drift table definitions plus one empty .gitkeep) and cross-checked every profileId/trackId column against its declared (or missing) FK/cascade, uniqueKeys, and @TableIndex constraints. Rather than trust the looser prose in docs/coding-standards.md, I located and directly ran the actual profileId-in-PK checker (tool/schema_check.dart) — both as-shipped and with an expanded whitelist — to get empirical, tool-verified compliance results instead of hand-tracing regexes, then traced the missing-FK tables' real-world consequ

### t1-core-domain-learning (19 files, 9 findings)
Read all 19 batch files in full (11 lib value-object/constant files, 8 mirrored unit-test files, ~1109 source + ~1140 test lines). For every type, grepped the wider repo (lib/ and test/) to determine real production call sites, checked import graphs for Rule 1/3/4/5 layering, cross-checked exception base classes (core/exceptions/app_exception.dart, validation_exception.dart) and two out-of-batch consumers (stage_definition_repository_impl.dart, upgrade_to_cloud_service.dart, profile_model.dart, set_parent_pin_use_case.dart, child_mode_guard.dart, scheduler_engine.dart, bulk_prior_completion_se

### t1-core-l10n (1 files, 2 findings)
Read the full batch file (learning_tracker/test/core/l10n/app_localizations_test.dart, 94 lines) end to end and ran the TQ applicability sweep: TQ-1/2/6 apply and pass (pure hermetic unit test, fresh instances per test via setUp, no network/wall-clock/randomness/shared state); TQ-3/4/5/9 are N/A (no widgets, no mocks, no goldens, not a rules suite). No Firebase/Drift/sync/security-rule/provider surface in this file, so FB/SR/DB/SM/AU/PV rule families don't apply. Applied the Fowler duplication lens against the wider repo: confirmed with `find`/`ls` that lib/core/ has no l10n/ subdirectory (rea

### t1-core-labels (14 files, 5 findings)
Read all 14 batch files in full (6 lib + 8 test, 3021 lines). Cross-checked the Rule-1 baseline (all 5 core→features imports in this batch are pre-baselined; zero new edges), the no_color_literal_outside_theme lint source (confirmed Colors.amber.* is out of its scope, not a violation), and hebrew-terms.md §10-12 (confirmed domain_term_labels.dart's hardcoded Hebrew/English catalog is a deliberate, ARB-mirrored, already-documented design, not a new violation). Traced CurriculumLabel.item's breadcrumb/parent rendering path into a live caller (step_starting_position.dart, wired from add_track_flo

### t1-core-nav-utils (34 files, 8 findings)
Read all 34 files in full (18 lib/ + 16 test/, ~5,809 lines) plus cross-repo greps for context: import fan-out for the 4 core/navigation re-export shims (61/0/8/3 importers respectively), confirmation that the pin_guard.dart core→features edge is already baselined in make-audit.txt, the Flutter SDK's own material.dart DateUtils declaration (read directly from the installed Flutter SDK), zero-caller confirmation for HebrewCalendarUtils.isShabbos/isYomTov/formatHebrewDate against all of lib/, and the learner_profiles CHECK-constraint claim in child_mode_guard.dart's comments against the actual t

### t1-core-network (7 files, 9 findings)
Read all 7 batch files in full (1188 lines: connectivity_gateway.dart, curriculum_content_fetcher.dart, content_item.dart, curriculum_hierarchy_config.dart, connectivity_service_test.dart, dio_client_test.dart, fetcher_test.dart) plus wider-repo greps to trace every type/function's real callers/implementers across lib/, tool/, test/, Makefile and docs/ (docs/seed-build.md, docs/explainers/content-database.md, docs/source-tree-analysis.md, tool/seed_content.dart, composite_curriculum_strategy.dart, curriculum_id.dart, content_repository_impl.dart, bookmarks.dart, learning_order.dart, mock_servi

### t1-core-providers-prefs (19 files, 7 findings)
Read all 19 batch files in full (9 lib + 2 test files under core/preferences; 7 lib files under core/providers; 1 large test file under test/core/providers), then cross-checked every keepAlive/Provider declaration, features/-import, and raw-Talker usage against the make-audit baseline (scratchpad/baseline/make-audit.txt) and docs/coding-standards.md to separate baselined SM-1/Rule-1 backlog from new violations — confirmed both core->features import edges in this batch (preference_providers.dart:12-13, calendar_providers.dart:3-4) are pre-existing baselined edges, not findings. Traced preferenc

### t1-core-services (7 files, 6 findings)
Read all 7 batch files in full (transactional_email_service.dart 211 lines, .gitkeep 0 lines, 5 test files totaling 631 lines) plus wider-repo context: git history of lib/core/services/ (commits 21e9c357, f99aee14), the current lib/ tree (confirmed calendar_program_registry.dart, calendar_program_service.dart, cross_curriculum_aggregator.dart and learning_program_service.dart now all live under features/scheduler/domain/services/, whose test/features/scheduler/domain/services/ sibling already holds ~30 other test files but not these 5), docs/architecture.md and docs/coding-standards.md placeme

### t1-core-small-a (22 files, 8 findings)
Read all 22 batch files in full (~3419 lines): core/constants (3, incl. the 1270-line curriculum_defaults.dart and the 202-line hebrew_terms.dart), core/enums (3), core/exceptions (5), core/ids (2), core/time (2), and 6 test files under test/core/constants + test/core/exceptions and .gitkeep. Cross-checked claims with repo-wide greps (caller counts for displayNameEn/He, the two dead exception classes, all 7 NaturalKey factories, DateTimeFactory/useLocalDayClock usage, and .gitkeep prevalence) and read several external files (mergers, track_repository.dart, text_display_screen.dart snippet, cor

### t1-core-streak (2 files, 6 findings)
Read both assigned files in full, then read their full production call graph for context (StreakEvent, StreakStateProvider, StreakReducer, StreakEventDao, LocalDayClock/FakeLocalDayClock, DateUtils.extractLocalDate, drift_memory test helper, and the sibling streak_reducer_test.dart) since correctly judging a test file requires understanding what it actually exercises. Ran `flutter test test/core/streak/` four times, including once under `TZ=Pacific/Kiritimati`, to empirically test a timezone-hermeticity hypothesis rather than leave it speculative, and confirmed the AG-5 misplacement against gi

### t1-core-sync-codec (15 files, 8 findings)
Read all 15 batch files in full (encode/decode logic + docstrings), plus supporting context: entity_merger.dart, every *_merger.dart caller of these codecs, merge_router.dart's dispatch loop, firestore_gateway_impl.dart's Timestamp-patch logic, and firestore.rules' hasOnly()/request.time clauses for every collection this batch pushes to. Cross-checked every encode() output against its firestore.rules hasOnly() allowlist (all compliant), checked Drift's actual DateTime round-trip behavior against the pinned drift-2.31.0 package source, and grepped the full test/ tree for coverage of each codec.

### t1-core-sync-exceptions (4 files, 4 findings)
Read all 4 files in learning_tracker/lib/core/sync/exceptions/ in full (118 lines total). For each, ran the applicability sweep (Rule 3/4 imports — clean, neither Firebase nor Talker imported directly; EH-2/3/4/6 — all four correctly extend an AppException category base and implement Exception, no catch blocks to check for EH-3, no sealed switches), then traced every constructor call site across lib/ and test/ via grep to determine actual production usage. For the two exceptions used in the live sync path (FirestorePermissionDeniedException, SyncPushException) I walked the full call graph end-

### t1-core-sync-merge (28 files, 11 findings)
Read all 28 files in full (21 lib/core/sync/merge/** implementation files + 7 test files, ~6023 lines): every EntityMerger implementation, DriftMergeStore, MergeRouter, the EntityKind taxonomy, and merge_rules.dart, cross-checked against docs/sync-conflict-resolution.md's strategy table. For context only (not audited as files-of-record) I read sync_orchestrator.dart's pull-step error handling, pull_pipeline.dart's dispatch call sites, and the Drift table definitions for stage_definitions/goals (to confirm a real FK), plus grepped firestore_gateway_impl.dart to confirm hasPendingWrites/isFromCa

### t1-core-sync-outbox (3 files, 8 findings)
Read all 3 batch files in full (599 + 166 + 1773 lines). Cross-checked docs/coding-standards.md for exact AG-3/EH-4/PV-5 wording, and verified the _dispatch switch and _nonCompletionKinds list are 1:1 (no missing/orphaned OutboxEntityKind). Grepped/read supporting context needed to root-cause and confirm findings: completion_writer.dart (entityKey construction — confirmed it embeds profileId+sefariaRef), firestore_gateway.dart/firestore_gateway_impl.dart (batching, idempotent set(merge:true), deterministic doc-ID derivation), log_events.dart and analytics_service.dart (event-catalog boundaries

### t1-core-sync-providers (7 files, 8 findings)
Read all 7 files in full (721 lines) and cross-referenced them against merge_router.dart, entity_merger.dart, outbox_processor.dart, points_balance_dao.dart, outbox_sync_write_facade.dart, local_data_upload_service.dart, sync_orchestrator.dart, lifecycle_observer.dart, auth_state_provider.dart, active_profile_provider.dart, database_provider.dart, features/sync/presentation/providers/sync_providers.dart, tutored_mirror_wipe_service.dart, manage_tutors_providers.dart, backup_sync_section.dart and sync_status_indicator.dart to verify claims statically rather than by inspection alone. Verified ev

### t1-core-sync-root-p1 (15 files, 9 findings)
Read all 15 files in this batch in full (~5,352 lines: FirestoreGateway interface + impl, listener source/supervisor, pull/push pipelines, initial-sync flag, lifecycle observer, sync orchestrator (HOTSPOT), sync-write facade, identity status, and the five tutored-session files). Ran the FB-1..9 + Nygard sweep against every I/O call site (push/pull/listen/delete), traced LWW field handling against docs/sync-conflict-resolution.md and coding-standards.md's FB-2 text, and grepped outside the batch for corroborating context: functions/src/index.ts (confirmed the onUserDeleted CF backstop), firesto

### t1-core-sync-root-p2 (5 files, 9 findings)
Read all 5 assigned files in full (6024 lines), plus their mirrored lib/core/sync/** implementations, the FB/TQ/AG rule text in docs/coding-standards.md, and cross-repo greps (test/sync/**, the fake_cloud_firestore and cloud_firestore package sources under ~/.pub-cache) to confirm each finding wasn't already covered by a sibling test file or baselined. Confirmed strong, genuine coverage of LWW clock-skew arbitration, cross-profile isolation, single-flight per-kind serialization, the pull-guard state machine, and profileId==0 sentinel handling — these are not padding findings against. The batch

### t1-core-theme-widgets (34 files, 6 findings)
Read all 34 batch files in full (21 lib + 12 test + 1 empty .gitkeep; ~4863 lines). Ran repo-wide greps to separate live from dead code (CurriculumIndicator, TrackProgressBar, AppTextStyles.getTextDirection/getStyleForContent, ErrorDisplay, StatCard), to confirm ARB key availability/parity for the hardcoded-string findings (app_en.arb), to check AX-1 directional-API misuse, TODO/FIXME hygiene, try/catch presence, and duplicate top-level class names across lib/. Did not execute the test suite or app (static/grep evidence only, all findings CONFIRMED not SUSPECTED). Did not deep-audit out-of-bat

### t1-feat-account-p1 (34 files, 13 findings)
Read all 34 batch files in full (28 real Dart files totaling ~6500 lines + 6 empty .gitkeep placeholders): the auth repository/gateway boundary, local-born password auth + argon2id hashing, magic-link service, session persistence, account lifecycle/deletion, local-to-cloud upgrade flow, sign-up/sign-in screens + SignInController notifier, account picker/switcher, and email-verification widgets. Cross-checked against docs/coding-standards.md rule families (Rules 1-5, SM-1..8, EH-1..6, AX-1..4, PF-1..4, AG-3/4/11) and grepped the wider repo for context: Makefile audit-check logic (reproduced the

### t1-feat-account-p2 (14 files, 9 findings)
Read all 14 batch files in full (4 presentation widgets + 10 test files, ~5921 lines) and, per the feature-slice area note, cross-read the production files those tests exercise (magic_link_service.dart, upgrade_to_cloud_service.dart, account_lifecycle_service.dart, pending_local_signup.dart, local_auth_service.dart, auth_validators.dart, signup_screen.dart, sign_in_controller.dart, account_picker_screen.dart) to judge whether the tests actually pin the behaviors those files can get wrong. Verified the presentation->data import ban (clean), Rule 3/4 Firebase/Talker confinement (clean — magic_li

### t1-feat-account-p3 (14 files, 11 findings)
Read all 14 files in this batch in full (5,847 lines) and cross-referenced the account feature's lib code (sign_in_controller.dart, account_picker_screen.dart, email_verification_confirm_panel.dart + its dialog caller, connectivity_providers.dart, offline_top_banner.dart) plus the shared test/mocks and test/helpers modules, per the batch's instruction to judge lib code and tests as a unit. Verified two findings empirically rather than by inspection alone: ran the overflow-matrix test file (passes today, including Hebrew RTL) and ran a scratch copy of email_verification_confirm_panel_test.dart 

### t1-feat-auth (9 files, 7 findings)
Read all 9 batch files in full (8 real test files + 1 empty .gitkeep, ~1906 lines) plus their paired lib subjects in full (auth_state.dart, local_auth_service.dart, auth_repository_impl.dart, sign_in_screen.dart) to confirm the tests still exercise real, current code. Grepped the whole test/ and lib/ trees to: rule out other coverage of the same subjects, confirm no lib/features/auth/ directory exists, confirm test/helpers/pump_app.dart doesn't exist yet (TQ-3 baseline), confirm zero references to RegistryMatchKind anywhere in test/, and trace EmailCollisionException's 3 catch sites in upgrade

### t1-feat-content_browsing-p1 (32 files, 11 findings)
Read all 32 batch files in full, including the 9 empty .gitkeep placeholders. For layering/dead-code questions I grepped the wider repo (lib/ and Makefile) to confirm live vs. dead call sites for every provider defined in this batch, and reproduced the Makefile's own Rule-2 awk logic directly to verify a suspected false-negative. Consulted docs/coding-standards.md for exact SM-6/EH-4/AX-2/AX-3/AG-3 wording and docs/planning/e2e-test-suite-plan.md to confirm CurriculumListScreen's decorative search button (R-LC1) is an already-tracked, accepted risk rather than a fresh finding. Not audited: the

### t1-feat-content_browsing-p2 (24 files, 6 findings)
Read all 24 batch test files in full (~6,364 lines) plus the ~20 lib/domain files they exercise (content_repository_impl.dart, text_cache_repository.dart, cloud_content_service.dart, text_download_service.dart, composite_curriculum_strategy.dart, content_providers.dart, cloud_content_providers.dart, text_display_screen.dart, content_hierarchy_screen.dart, curriculum_list_screen.dart, content_search_screen.dart, breadcrumb_navigation.dart, content_item_tile.dart, item_review_breakdown.dart) to judge lib+test as a unit per the area note. Verified two claims empirically rather than by inspection:

### t1-feat-content_browsing-p3 (1 files, 3 findings)
Read the single batch file in full (r2_panel_leaf_crumb_ellipsis_test.dart), then — per the AREA NOTE — read the production widget it exercises (hierarchy_selection_panel.dart) and its sibling breadcrumb_navigation.dart to judge lib+test as a unit. Went beyond static reading: ran `flutter test` on the file as-is (4/4 green), then twice more against deliberately mutated copies of the widget (full revert of the R2 fix → confirmed RED; partial revert removing only the ConstrainedBox width-cap while keeping overflow/maxLines → stayed GREEN, exposing a tautological assertion), then restored the wid

### t1-feat-dashboard-p1 (34 files, 12 findings)
Read all 34 files in full (25 substantive .dart files + 9 .gitkeep placeholders) covering the dashboard feature's domain models/services/use-cases, providers, screen, and presentation widgets. Cross-checked dead-code/duplicate-symbol claims with repo-wide greps (CurriculumSummary, ChazaraStatus, MomentumStatus, dashboardModelProvider, nextRewardAsync, dashboard.dart barrel), confirmed Rules 3/4/5 and the Hebrew-terms rule are clean in this batch (no Firebase/raw-Talker/displayName/HebrewTerms/DateTime.now violations), and confirmed no direct Drift mutations live in this presentation/domain-onl

### t1-feat-dashboard-p2 (29 files, 8 findings)
Read all 29 files in full (9 dashboard presentation widgets + 20 test files spanning domain services, use-cases, provider integration tests, screen tests, and widget regression tests), and cross-referenced lib code the tests exercise but that lives outside this batch (dashboard_body.dart, DashboardLevelPointsCard, TrackCompletionService/TrackProgressService, the Makefile's make-audit target, add_profile_dialog.dart) via targeted greps to verify liveness, baseline status, and tooling correctness rather than taking claims at face value. Two claims were verified empirically rather than just read:

### t1-feat-dashboard-p3 (1 files, 4 findings)
Read the full 233-line file end-to-end, then cross-referenced its provider-override list against every ref.watch in the widget subtree it actually pumps (dashboard_body.dart, active_track_card.dart, main_focus_mission_card.dart, dashboard_providers.dart, database_provider.dart) to check the test truly pins what its header comment claims. Two of the four findings were verified by direct execution, not just static reading: ran the file under `flutter test` with a diagnostic probe to prove the exception-drain loop never fires under the file's own device/scale matrix, then temporarily reverted mai

### t1-feat-gamification-p1 (34 files, 16 findings)
Read all 34 batch files in full (25 substantive Dart files + 9 confirmed-0-byte .gitkeep placeholders). For every non-trivial claim I went beyond the file itself: grepped the whole repo for every RewardMilestoneService/StreakService/StreakStateProvider construction site, every cross-feature import into features/gamification/**, and reproduced the Makefile's check-15 awk pipeline directly against the tree to confirm it structurally cannot fire. I also read the relevant slices of the existing gamification test suite (reward_milestone_service_test.dart ~1000 lines, reward_config_controller_test.d

### t1-feat-gamification-p2 (31 files, 14 findings)
Read all 31 files in full (13 lib files under features/gamification/{presentation/widgets,streak}, 17 test files, 1 empty .gitkeep) and cross-checked against docs/coding-standards.md, the make-audit.txt baseline, and wider-repo greps for context (completion_constants.dart, scheduler_engine.dart, streak_events table def, streak_event_dao.dart, user_database.g.dart, progress_summary_card.dart, reward_milestone_service.dart, app_theme.dart). Judged lib code and its tests as a unit per the area note: traced RewardForm/TierStyle/StreakReducer/StreakEventLog logic against their actual test coverage 

### t1-feat-gamification-p3 (14 files, 9 findings)
Read all 14 batch files in full (~5742 lines) plus cross-referenced ~10 lib files they exercise (reward_config_controller.dart, reward_form.dart, reward_milestone.dart, reward_milestone_service.dart, achievements_overview_provider.dart, child_redemption_screen.dart, points_balance_dao.dart, parent_pending_redemptions_screen.dart, completion_repository_impl.dart, text_display_screen.dart) to judge whether the tests actually pin reachable lib behavior per the AREA NOTE. Verified: no Firebase/Talker/displayName/mockito/print violations in-batch (grep clean); AG-5 mirroring satisfied for all 14 fi

### t1-feat-labels (1 files, 2 findings)
Read learning_tracker/test/features/labels/domain_term_rendering_modes_test.dart in full (1088 lines, the batch's only file). Verified every production symbol it exercises (CurriculumLabels.primaryUnitLabel/containerCountLabel/containerSectionHeader, domainTermLabels().chazara/.shabbos, CurriculumListScreen, ScopeSelectionScreen, HierarchyProgressCard, SiyumimGroupedView, GoalSetupForm, SacredTimeLockOverlay, SacredTimeSettingsCard, CurriculumLabel.curriculum) still exists with matching signatures via grep against lib/core/labels, lib/core/constants/curriculum_defaults.dart and the seven lib/f

### t1-feat-learning-p1 (34 files, 10 findings)
Read all 34 batch files in full (26 non-empty Dart files ~3945 lines + 8 empty .gitkeep placeholders): the learning feature's completion/bookmark/ledger/track data layer, domain entities/repositories/services/use-cases, and presentation providers. Cross-checked against the 18 test files under test/features/learning/ (spot-read completion_repository_impl_test.dart's setUp in full, plus targeted greps of the rest) to judge whether the suite pins the behaviors the lib code can get wrong, per the area note. Grepped the wider repo for context only (RewardMilestoneService/BookmarkRepositoryImpl cons

### t1-feat-learning-p2 (21 files, 8 findings)
Read all 21 batch files in full (2 are empty .gitkeep placeholders, trivially SOUND) and cross-referenced the wider repo for context that a per-batch view can't provide on its own: completion_repository_impl.dart, completion_detection_service.dart, learning_ledger_repository_impl.dart, the five feature barrel files (learning/dashboard/profiles/scheduler/tutoring), the root and inner Makefile audit targets, both CLAUDE.md files, docs/coding-standards.md, and the tutoring feature's own mirrored test files -- none of which are in this batch but were needed to confirm novelty and blast radius. Ran

### t1-feat-learning-p3 (3 files, 7 findings)
Read all 3 batch files in full, plus lib counterparts needed to judge test adequacy per the AREA NOTE: learning_screen.dart (provider reads), optimistic_completion_provider.dart, app_error_view.dart, and the tutoring feature's own permissions test. Empirically ran `flutter test` on both LearningScreen test files (not just static reading) and reproduced a real defect: Drift's "created UserDatabase multiple times... might corrupt the database" warning fires on 23/26 and 1/3 widget pumps respectively, because neither file overrides the DB-backed providers LearningScreen now reads. Confirmed via t

### t1-feat-learning_order (3 files, 3 findings)
Read all 3 batch files in full (359 lines) and, per the AREA NOTE, the lib code they exercise — lib/features/tracks/whole_curriculum_order/** (repository, screen, providers, dialog, use case, domain model), its LearningOrderDao, and its cross-feature consumers (bookmark_repository_impl.dart, scheduler_learning_order_repository_impl.dart) — to judge whether the suite actually pins the behavior the lib code can get wrong. Confirmed via git history this is the 'unmirrored test dir' case the digest flagged: lib/features/learning_order/ was deleted and consolidated into lib/features/tracks/whole_cu

### t1-feat-notifications-p1 (34 files, 15 findings)
Read all 24 non-empty files in the batch in full (~6,400 lines), including the complete 1,415-line notification_gateway_test.dart and the 753-line HOTSPOT notification_providers.dart, plus the 10 .gitkeep placeholders (trivially SOUND). Grepped the wider repo for context on every finding: caller sites for every scheduling method (to confirm dead code), the Makefile check-15 awk logic (to explain the undetected cross-feature import), core/sync/merge/notification_settings_merger.dart and docs/sync-conflict-resolution.md (to scope the client-clock LWW claim), and construction-site counts for ever

### t1-feat-notifications-p2 (5 files, 7 findings)
Read all 5 batch files in full (l1_test 977 lines, screen_test 122, ws5_key_prefs 87, ws5_per_profile 85, ws5_two_layers 127) plus their production collaborators (notification_providers.dart, notifications_screen.dart, device_notification_toggle.dart, notification_gateway.dart, notification_preferences_repository.dart) to judge whether the tests actually pin what the lib code can get wrong, per the AREA NOTE. Empirically ran each of the 5 files with `flutter test` (not just static reading) to confirm pass/fail and to verify/rule out suspected runtime issues: notifications_screen_test.dart repr

### t1-feat-onboarding-p1 (34 files, 10 findings)
Read all 25 non-empty files in the batch in full (the 9 .gitkeep entries were verified 0 bytes via wc -c, confirming they are genuine empty-directory placeholders, not truncated reads). Followed up with targeted greps into stage_dao.dart, pin_service.dart, completion_repository_impl.dart, docs/coding-standards.md's Rule 2 text, the make-audit.txt baseline, git log/blame for onboarding_providers.dart and onboarding.dart, and 4 test files (wizard_steps_l1_test.dart, bulk_mark_screen_test.dart, learning_process_wizard_service_test.dart, bulk_prior_completion_service_test.dart) to confirm findings

### t1-feat-onboarding-p2 (19 files, 15 findings)
Read all 19 batch files in full (~6158 lines) and, per the AREA NOTE, also read the paired lib code each test/widget exercises (auth_validators.dart, curriculum_import_service.dart, learning_process_wizard_service.dart, bulk_prior_completion_service.dart, bulk_mark_screen.dart, onboarding_screen.dart, app_intro_screen.dart, plus core/theme, core/labels/domain_term_labels.dart, and 2 ARB files) to judge whether the suite actually pins lib behavior. Verified the presentation->data import ban is clean across all 3 widgets (no features/*/data/** imports). Ran the actual Makefile "15/15 cross-featu

### t1-feat-onboarding-p3 (4 files, 8 findings)
Read all 4 batch files in full (2694 lines) plus the lib widgets they exercise (permission_prompt_screen.dart, signup_screen.dart [985 lines], wizard_steps.dart [807 lines], onboarding_step.dart, onboarding_handoff_step.dart, onboarding_add_another_prompt_step.dart, onboarding_profile_creation_step.dart [partial]) to judge, per the area note, whether the tests actually pin what the lib code can get wrong. Cross-checked every l10n claim against app_en.arb/app_he.arb, and confirmed the WizardCustomStep2 Slider crash empirically with a throwaway flutter-test render (file created under test/ and d

### t1-feat-parent_mode (11 files, 12 findings)
Read all 11 batch files in full (2380 lines) plus their lib/ targets for cross-reference (pin_service.dart, pin_flow_controller.dart, pin_flow_screen.dart, pin_flow_machine.dart, parent_dashboard_aggregator.dart, child_mode_guard.dart, app_exception.dart) and the properly-mirrored test/core/navigation/child_mode_guard_test.dart. Ran targeted greps across lib/ and test/ to confirm dead-code and duplication claims (pin_flow_machine.dart reference count, ProfilesCompanion typedef resolution, Locale('he')/ProviderContainer.test() prevalence as codebase norms) and ran `dart analyze` (clean, matchin

### t1-feat-profiles-p1 (30 files, 14 findings)
Read all 30 batch files in full (profiles feature: domain models/services/use-cases, the Drift-backed repository, and every presentation provider/screen/widget covering profile CRUD, the PIN setup/verify/change flows, and parent settings/track-management screens). For every class or route that looked unreferenced or duplicated, I cross-checked with repo-wide grep against lib/ and test/ (PinFlowMachine, ProfileCreationUseCase, Set/VerifyParentPinUseCase, PinFlowVerifyRoute/PinFlowChangeRoute, ParentPinAuthenticatedProfileId, pinGuard/router_provider wiring) rather than relying on a single file'

### t1-feat-profiles-p2 (11 files, 14 findings)
Read all 11 batch files in full (2 lib widgets + profiles.dart barrel + 8 test files, ~5687 lines) and cross-checked every claim against the wider repo: ran the actual `make audit` check-15/15 cross-feature-import grep by hand against the current tree (reproduced its self-comparison awk bug), diffed all 16 feature barrels for export counts, grepped for every consumer of `features/profiles/` deep paths repo-wide (65 files), checked `git log`/blame to confirm one provider is genuinely new code, verified `test/helpers/` contents for a missing shared pump helper, and grepped for `Locale('he')` pre

### t1-feat-profiles-p3 (15 files, 6 findings)
Read all 15 assigned test files in full (~5,757 lines: parent_settings_screen_l1_test.dart, parent_track_management_screen_l1_test.dart, profile_picker_screen_l1_test.dart, profile_picker_segmentation_test.dart, rpr2_picker_offline_delete_test.dart, ts14_parent_track_management_copy_test.dart, ts3_parent_track_archive_test.dart, add_profile_dialog_test.dart, parent_pin_keypad_dialog_test.dart, pp11/pp12/pp13/pp16/pp1/pp2 widget tests). Per the area note (judge lib+tests as a unit), cross-referenced each regression test against its production target: parent_track_management_screen.dart, curricu

### t1-feat-profiles-p4 (6 files, 6 findings)
Read all six batch files in full (3100 lines) plus their paired production code across lib/features/profiles/**, lib/features/tutoring/data/**, lib/features/sync/presentation/providers/sync_providers.dart, and lib/app/router/app_shell.dart, tracing every repository/provider call each test exercises to check whether the suite actually pins what the lib code can get wrong, per the area note. Confirmed the tutor-write swallow chain by tracing editProfileFlow through ProfileRepositoryImpl.updateProfile, syncWriteFacadeProvider, and TutoredWriteRouter.pushLearnerProfile, and confirmed ws4_mode_boun

### t1-feat-progress-p1 (32 files, 10 findings)
Read all 23 non-empty files in the batch in full (9 .gitkeep placeholders confirmed empty/trivial), plus skimmed the mirrored test/features/progress/ suite (46 files, ~16.8k lines) to judge whether lib behaviors are actually pinned. Ran targeted repo-wide greps per the applicability sweep (Firebase/Talker/HebrewTerms/DateTime.now/EdgeInsets-left-right/mockito/StateProvider/keepAlive/TODO/writes-in-a-read-only-slice) and confirmed the AG-3 line counts with `wc -l`. The standout finding is that items_learned_providers.dart reimplements lifetime_tree_builder.dart's tree-building engine from scrat

### t1-feat-progress-p2 (26 files, 9 findings)
Read all 26 batch files in full (15 presentation/widgets files, the progress.dart barrel, and 10 test files under test/features/progress/{data,domain}), and per the area note also read the non-batch lib source each test file exercises (chart_data_service.dart, chart_data.dart, journey_view_model.dart, progress_repository_impl.dart, plus the callers of the widgets under test such as recent_activity_screen.dart) to judge whether the tests actually pin the behaviors the lib code can get wrong. Verified the layering, duplicate-color, i18n, AG-3, and AG-4 findings against live grep/wc output rather

### t1-feat-progress-p3 (15 files, 6 findings)
Read all 15 files in full (6,412 lines) plus the lib code they exercise (chart_data_service.dart, curriculum_progress_service.dart, lifetime_tree_builder.dart, pace_calculator.dart, overlapping_curricula_deduplicator.dart, items_learned_providers.dart, progress_providers.dart, lifetime_knowledge_providers.dart, journey_providers.dart) to judge whether the tests actually pin what the lib code can get wrong. Grepped the wider repo for: DateTime.now()/wall-clock use, mockito, unseeded Random, skip:, weaken-ok markers, untracked TODOs, cross-batch duplicate test coverage (notification_providers_te

### t1-feat-progress-p4 (18 files, 10 findings)
Read all 18 batch files in full (test code, ~5,477 lines) plus their cross-referenced lib/features/progress presentation-layer counterparts (5 screens, 9 widgets, 3 providers files, the barrel) end-to-end — roughly 3,300 additional lib lines — since the area note required judging lib code and its tests as a unit. Verified every non-trivial claim against the wider repo before filing: grepped for MonthlyActivitySliverCalendar usage repo-wide (confirmed dead), disambiguated ContentItem.displayNameHe from the Rule-5-governed curriculum-enum accessor (confirmed NOT a violation, so not reported), ch

### t1-feat-progress-p5 (3 files, 4 findings)
Read all 3 batch files in full (2588 lines) plus the production code they exercise -- recent_activity_screen.dart, hierarchy_selection_panel.dart, chart_data_service.dart, siyumim_timeline_view.dart, lifetime_knowledge_providers.dart (a named churn hotspot), lifetime_tree_builder.dart, learning_ledger_dao.dart -- and the shared helpers drift_memory.dart and overflow_harness.dart, to judge whether the suite actually pins behaviors the lib code can get wrong per the AREA NOTE. Verified layering cleanliness (no core/features, no raw Firebase/Talker/.displayNameEn/.displayNameHe/HebrewTerms, no pr

### t1-feat-sacred_time (21 files, 11 findings)
Read all 21 files in full (13 lib + 8 test, ~4033 lines) and cross-referenced Makefile check 15, coding-standards.md (Rules 1-5, SM-1..8, EH-1..6, AX-2, SM-6, placement guide), the make-audit baseline, and 7 external files (settings/onboarding/notifications features + app_router/app_shell) that deep-import sacred_time, to check the layering and presentation->data areas the task called out. Confirmed via grep/dart analyze that this batch has zero raw DateTime.now(), raw Talker, Firebase, HebrewTerms.*, or duplicate-symbol issues, and that `flutter analyze` is clean. Could not verify, from this 

### t1-feat-scheduler-p1 (34 files, 10 findings)
Read all 34 batch files in full (29 Dart files totalling 2359 lines + 5 empty .gitkeep placeholders), and additionally read/grepped the associated scheduler test files under test/features/scheduler/{data,domain}/ (daily_plan_repository_test.dart, daily_plan_repository_backfill_test.dart, goal_repository_impl_test.dart, scheduler_content_repository_impl_test.dart, program_label_resolver_test.dart, goal_entity_test.dart + _extended + _extra, overdue_from_elapsed_study_days_test.dart) to satisfy the area note's "does the test suite pin the behaviors the lib can get wrong" question — this is what 

### t1-feat-scheduler-p2 (27 files, 14 findings)
Read all 22 non-empty files in the batch in full (9 domain services, 2 providers files, 3 screens, 4 widgets, 1 barrel, 3 tests) plus the 5 .gitkeep placeholders. Grepped the wider repo (lib/, test/, Makefile, docs baseline) to verify Rule 1/2/3/4/5 compliance, hunt AG-4 duplicate symbols, confirm dead-code call sites (onCompleted chain, scheduler PaceIndicator, toggleStudyDay, ComposedDailySchedule.summary), and trace a suspected date-arithmetic bug in SchedulerEngine end-to-end from clockProvider through to _processDelayStage. Manually reproduced the Makefile's `make audit` check-15 pipeline

### t1-feat-scheduler-p3 (20 files, 9 findings)
Read all 20 batch files in full (test/features/scheduler/{data,domain}/**, ~5889 lines), plus the lib/ files they exercise end-to-end (goal_entity.dart, delta_value.dart, pace_status.dart, program_label_resolver.dart, goal_repository_impl.dart, scheduler_content_repository{,_impl}.dart, daily_task_generator.dart, local_calendar_engine.dart, pace_calculator.dart, and the 747-line scheduler_engine.dart) to judge whether the tests actually pin what the lib code can get wrong, per the area note. Ran `flutter test` across the batch's directory tree (379-381 tests, all green) to confirm nothing here

### t1-feat-scheduler-p4 (13 files, 6 findings)
Read all 13 batch files in full (~5,753 lines: sefaria_ref_matcher_test.dart 1010, study_day_chazara_gate_test.dart 154, study_day_toggle_write_test.dart 187, study_day_trackid_fallback_test.dart 115, scheduler_all_daily_tasks_test.dart 957, scheduler_contiguous_overdue_no_gap_test.dart 232, scheduler_providers_branches_test.dart 684, scheduler_providers_test.dart 1092, goal_and_study_day_i18n_test.dart 170, scheduler_goal_card_pluralization_test.dart 242, scheduler_screen_l1_test.dart 677, scheduler_screen_test.dart 183, study_day_config_plural_test.dart 50), plus the scheduler lib code they 

### t1-feat-scheduler-p5 (12 files, 9 findings)
Read all 12 batch files in full (4,732 lines) plus their lib counterparts required by the area note (study_day_config_screen.dart, daily_task_card.dart, both pace_indicator.dart files, hebrew_date_picker.dart, goal_setup_screen.dart, overdue_projection/schedule/types.dart, the relevant scheduler_providers.dart and study_day_config_providers.dart/repository sections, and notification_providers.dart), verifying every non-trivial claim with greps (caller counts, ARB text, sibling-test coverage) rather than assuming. Did not read the ~30 non-batch scheduler/notifications test files end to end or r

### t1-feat-settings-p1 (26 files, 7 findings)
Read all 26 batch files in full: 17 substantive Dart files spanning settings' domain/exceptions, domain/services, presentation/providers, presentation/screens, presentation/utils, presentation/widgets, plus 9 empty .gitkeep placeholders. Ran the applicability sweep (DB-1..6, SM-1..8, EH-1..6, AX-1/2, AG-3/6, presentation->data import ban, DateTime.now/raw-Talker/mockito/legacy-provider greps) and the Fowler/Feathers/Evans/Nygard lens pass on every substantive file, plus targeted repo-wide greps for context (TrackDao, signInAnonymously/isAnonymous, DataExportImportService callers, AppException 

### t1-feat-settings-p2 (19 files, 10 findings)
Read all 19 batch files in full (2 lib files, 1 barrel, 1 .gitkeep, 15 test files, ~6320 lines), plus the directly-paired lib files those tests exercise but that sit outside this batch's file list (account_management_service.dart, curriculum_activation_service.dart, data_export_import_service.dart's full body already partly in-batch, lifetime_marking_screen.dart, and the relevant app_router.dart route-guard block) to judge whether the settings test suite actually pins the behaviors those lib files can get wrong, per the feature-slice AREA NOTE. Verified the Rule 2 cross-feature-import checker 

### t1-feat-settings-p3 (11 files, 11 findings)
Read all 11 files in this batch in full (5,899 lines: scope_lifetime_l1_test.dart, settings_screen_r5_regression_test.dart, settings_screen_test.dart, st3_settings_switch_a11y_test.dart, upgrade_to_cloud_cancel_collision_test.dart, upgrade_to_cloud_screen_l1_test.dart, settings_overflow_guard_test.dart, account_actions_test.dart, account_actions_sheet_nav_test.dart, an7_destructive_icon_contrast_test.dart, backup_sync_error_l10n_test.dart), covering the settings feature's scope/lifetime-marking, main SettingsScreen (HOTSPOT), upgrade-to-cloud, account-actions (HOTSPOT lib file) sign-out/delete

### t1-feat-settings-p4 (7 files, 5 findings)
Read all 7 batch files in full (3474/3474 lines, matching the digest's line count) plus their paired lib widgets/services — backup_sync_section.dart, account_actions.dart, user_profile_header_card.dart, send_logs_service.dart, point_config_screen.dart, and the relevant slice of settings_screen.dart — to judge, per the area note, whether each suite actually pins the behavior its lib code can get wrong rather than just exercising a mock. Ran the batch under `flutter test` (all 84 tests across the 5 non-trivial files pass today) to rule out DEFECTIVE verdicts and to confirm two findings at runtim

### t1-feat-sync-p1 (34 files, 15 findings)
Read all 34 files in the batch in full: 25 substantive Dart files across lib/features/sync/{data,domain,presentation} and test/features/sync/** plus test/sync/{bookmark_outbox_entity_key,codec_rules_contract}_test.dart, and 9 empty .gitkeep scaffold placeholders. Cross-checked claims against docs/sync-conflict-resolution.md's LWW invariant, coding-standards.md's FB/SM/EH/AG/AX rule text, and grepped the wider repo (lib/core/sync/, lib/app/restore/, lib/features/gamification/) to confirm consumer counts, the dead-code and duplicate-provider claims, and that SyncEngine/FirestoreDataSource/Offlin

### t1-feat-sync-p2 (24 files, 7 findings)
Read all 24 files in full (~6422 lines): the sync-path merge/listener/outbox regression suite under test/sync/ and test/sync/merge/. Per the AREA NOTE, cross-checked LWW/merge behavior against docs/sync-conflict-resolution.md and applied the Nygard lens (process death, offline queueing, clock skew, permission-denied, listener echo of own writes), grepping the wider repo (test/core/sync/, lib/core/sync/, and the orchestrator's own docs/audits/.../_work/global/ pre-scans) for context. Per-file test quality is high: real in-memory Drift DBs, hand-written fakes (zero mockito/mocktail/ProviderConta

### t1-feat-sync-p3 (16 files, 6 findings)
Read all 16 test/sync/*.dart files in full (~6420 lines), cross-checked against docs/sync-conflict-resolution.md, docs/coding-standards.md, and the source they exercise (sync_orchestrator.dart in full; targeted reads/greps of firestore_gateway_impl.dart, drift_merge_store.dart, goal_merger.dart, gamification_settings_merger.dart, study_day_config_merger.dart, mock_repositories.dart) to judge whether assertions match real FB-1..9/Nygard failure modes. FB-3 (hasPendingWrites/isFromCache echo suppression) is handled centrally in firestore_gateway_impl.dart before snapshots reach any merger, so th

### t1-feat-sync-p4 (4 files, 7 findings)
Read all 4 files in full (1452 lines) end to end, then traced every production symbol each test exercises (TutoredPullService, TutoredMirrorWipeService, OutboxProcessor, SyncOrchestratorImpl.recordDrainAttempt, OutboxSyncWriteFacade.onEnqueueDrain, resolveOwnerAccountIdForWipe, manage_tutors_providers.dart's incomingTutorGrantsProvider, FirestoreGateway/PushPipeline) via grep and full reads to check whether each test's claimed regression coverage actually reaches the code path that shipped the fix. Ran `dart analyze` on one file and executed all 15 tests in the batch with `flutter test` (all g

### t1-feat-tracks-extras (11 files, 7 findings)
Read all 11 batch files in full (~3,071 lines) and cross-checked the highest-severity findings against their production counterparts (stage_definition_repository_impl.dart, add_track_flow_screen.dart, add_track_flow_state.dart, program_selection_step.dart) plus git history (git log/git show) rather than relying on static reading alone. Verified via repo-wide greps: no test/core/domain/value_objects/schedule_spec_test.dart exists anywhere, ProgramSelectionStep is never rendered by any test in the repo, 102 other test files already run a Locale('he') variant, and no other story-suffixed rogue te

### t1-feat-tracks-p1 (24 files, 12 findings)
Read all 21 substantive files in the batch in full (plus 3 trivial .gitkeep placeholders), applied the applicability sweep per file type (screens: SM/EH/PF/AX/layering; domain services: EH/DB; providers: SM-1/6/7/8), and cross-referenced ~15 test files (mirrored, mis-mirrored, and the digest-flagged stale test/features/track_setup/ directory) to judge lib+test as a unit per the area note. Ran targeted repo-wide greps for context only (cross-feature import graph, provider/class usage census across all of lib/ and test/, git log/blame for two files, a live reproduction of the learning_tracker/Ma

### t1-feat-tracks-p2 (30 files, 14 findings)
Read all 30 batch files in full (goal_helpers.dart through track_learning_order_providers.dart, ~6486 lines) and applied the applicability sweep + Fowler/Feathers/Evans/Nygard lens to each. Pulled wider-repo context via grep/read where needed to verify rather than assume: chazara_widgets.dart and track_dao.dart (callers/implementations), all external callers of StageDefinitionRepository/StageEditorNotifier and TrackOrder (to confirm dead code), and the test suite (to confirm claimed coverage actually imports current lib code). Reproduced two Rule-0 Makefile checks by hand (root check 10/12 for

### t1-feat-tracks-p3 (22 files, 12 findings)
Read all 22 batch files in full — 11 lib files forming the track_order + whole_curriculum_order vertical slices, and 11 tests (those slices plus the standalone tracks/setup domain and screen suites) — plus ~12 out-of-batch files (learning_order_dao.dart, track_creation_service.dart, track_blueprint.dart, provision_track_use_case.dart, add_track_flow_screen.dart, step_chazara.dart/chazara_widgets.dart, ARB/router wiring) needed to judge whether the tests actually pin real lib behavior per the area note. Two findings were verified empirically, not just by inspection: reran track_creation_service

### t1-feat-tracks-p4 (13 files, 8 findings)
Read all 13 files on the batch list in full (5412 lines) plus the lib code they exercise — track_detail_screen.dart, edit_track_screen.dart, track_management_hub_screen.dart, step_goal.dart, goal_cards.dart, step_scope.dart, step_chazara.dart, chazara_widgets.dart, tutored_write_router.dart, local_day_clock.dart, overflow_harness.dart — to judge the AREA NOTE's question of whether the suite actually pins what the lib code can get wrong, not just whether tests exist. Verified hermeticity (fresh ProviderScope/DB per test, no mockito, no skip:/weaken-ok/TODO markers — grepped and confirmed clean)

### t1-feat-tracks-p5 (12 files, 7 findings)
Read all 12 batch files in full (~5304 lines: presentation-step tests, track-management-hub tests, track_order domain/presentation tests, and the two unmirrored test/track_setup/ files). Cross-referenced 9 lib files to verify each test's assertions actually match production behavior: program_starting_position.dart, step_study_days.dart, track_info_card.dart, program_selection_step.dart, track_management_hub_screen.dart, track_learning_order_screen.dart, edit_track_screen.dart, goal_helpers.dart, and track_order.dart/masechta_ordering_policy.dart (confirmed math for paceGoalUnitNoun, derivePace

### t1-feat-tutoring-p1 (27 files, 14 findings)
Read all 27 batch files in full (~6,134 lines) plus, per the AREA NOTE, cross-read the co-located test suite: all ~34 files under test/features/tutoring/ were grepped and the most relevant ones (mark_live_completion_use_case_test.dart, ws3_3f_manage_remove_tutors_test.dart, manage_tutors_screen_l1_test.dart, manage_grants_screen_l1_test.dart, s1_tutored_write_router_test.dart, both firestore_tutor_grant_repository_test.dart files) were read to check whether the lib defects were actually pinned. Swept for the presentation→data import ban (found the reverse: data→presentation), Rule 3/4 (no raw 

### t1-feat-tutoring-p2 (15 files, 15 findings)
Read all 15 batch files in full (4 lib screens/barrel + 11 test files, ~5874 lines) and ran the required applicability sweep + Fowler/Feathers/Evans/Nygard lens pass on each. Followed necessary context outside the batch to verify test-vs-production behavior per the AREA NOTE: tutor_pin_service.dart, tutor_invite_use_cases.dart, accept_invite_screen.dart, decline_invite_screen.dart, invite_tutor_screen.dart, features/account/account.dart, and the Makefile's check-15 grep (empirically re-executed and debugged with awk). Verified the Rule-2 barrel-naming question against docs/coding-standards.md 

### t1-feat-tutoring-p3 (11 files, 8 findings)
Read all 11 files in full (6458 lines) plus the tutoring lib/ modules each test targets (routers, use cases, providers, screens) for context on whether the suite actually pins production behavior, per the area note. Grepped the wider test tree (test/features/tutoring, test/core/sync, test/sync) to statically confirm every absence-based finding (mirror-wipe, router pass-through, analytics event, RTL variant, AG-3 checker) rather than asserting from memory, and grepped for Rule 3/4/5/Hebrew-terms/mockito/wall-clock violations (none found) and shared-ProviderContainer leaks (none found — the one 

### t1-feat-tutoring-p4 (13 files, 6 findings)
Read all 13 assigned files in full (4519 lines: tutor_pin_entry_gate_l1_test.dart, tutor_pin_reset_screen_l1_test.dart, tutor_pin_setup_screen_l1_test.dart, v3c_ui_gating_exception_catching_test.dart, w3_41_tutor_security_rules_test.dart, ws3_3a-h_*.dart). Cross-checked claims against lib/features/tutoring source, sibling gamification/tracks test suites (to confirm which screens have zero alternate behavioral coverage), dart_test.yaml, and functions/test/ (to confirm a real Cloud-Functions-side test exists for expirePendingInvites). Ran `dart test` on two of the pure-grep files to confirm they

### t1-story-epic-25 (13 files, 6 findings)
Read all 13 story_acceptance/epic_25_*.dart files in full (6642 lines) and applied the TQ-1..8 sweep plus the Fowler/Feathers/Evans/Nygard lens pass to each; cross-checked line counts with `wc -l` against the batch list, grepped the whole batch for mockito imports, shared/leaked ProviderContainers, skip:/weaken-ok/TODO markers, and try/catch blocks, and confirmed test/features/auth/data/repositories/auth_repository_impl_test.dart exists so the mocked AuthRepository acceptance test isn't the only coverage of that class. Every DB-backed test uses real in-memory Drift (NativeDatabase.memory()) ra

### t1-story-epic-26 (10 files, 11 findings)
Read all 10 story-acceptance files in full (~2,911 lines), then cross-checked in-test claims against the wider repo — Makefile, tool/, test/helpers/, git log, app_router.gr.dart, and the production files each test reads or claims to guard (text_display_screen.dart, track_management_hub_screen.dart/track_management_body.dart, hebrew_terms_preference.dart, add_track_flow*.dart). The batch is clean on shared/global ProviderContainer, DateTime.now()/Random(), Future.delayed/sleep, package:mockito, skip:, TODO/FIXME, and weaken-ok markers (TQ-2/4/6/7 and AG-6 all hold); I could not run `flutter tes

### t1-story-epic-27 (9 files, 12 findings)
Read all 9 files on the batch list in full (~3,629 lines) per the digest protocol, ran the applicability sweep (TQ-1..8) plus the Fowler/Feathers/Evans/Nygard lens pass on each, and traced every non-trivial claim into the production code it exercises or claims to test: completion_repository_impl.dart, scheduler_providers.dart, streak_reducer.dart/streak_state_provider.dart, local_day_clock.dart/date_utils.dart, pin_service.dart, analytics_service.dart, firestore.rules, and user_database.dart, cross-checked against docs/planning/prd.md (FR20), docs/planning/epics.md / epics-greenfield-rebuild.m

### t1-story-epics-01-08 (10 files, 8 findings)
Read all 10 files in full (6,748 lines): the epic 01/02/03/05/06/07/08 story-acceptance suites, the N1-N8 quality-crisis regression-invariant net, the I-3 items-learned/lifetime-view suite, and the track-lifecycle regression suite. Cross-checked test claims against production source (active_track_card.dart, track_detail_screen.dart _estimatedFinish, coding-standards.md AG-3/AG-5 text, the Makefile's test-story-* targets) rather than taking test titles/comments at face value, and grepped the full batch for tautological-assertion and wall-clock patterns to size each finding's site count precisel

### t1-story-epics-09-16 (7 files, 6 findings)
Read all 7 story_acceptance files in full (8,143 lines: epics 9, 10, 12, 13, 14, 15, 16), plus the production code needed to verify specific claims — ParentDashboardAggregator, LocalDayClock/SystemLocalDayClock, StreakStateProvider, OnboardingScreen's private _ScreenPhase enum, and AddTrackStep — to separate real defects from things that only look wrong. Ran batch-wide greps for DateTime.now(), Future.delayed, skip:, TODO/FIXME, weaken-ok, Talker(), ProviderContainer(, and mockito to check TQ-2/TQ-4/TQ-6/AG-6. Before filing an RTL-coverage finding for the two widget-tested screens in this batc

### t1-story-epics-17-20 (4 files, 9 findings)
Read all 4 batch files in full (124 + 689 + 624 + 294 = 1731 lines). Cross-checked every DAO/service call against its real implementation to verify assertions match actual behavior and to catch tests that simulate rather than exercise production code: track_dao.dart (restoreOrCreate/deleteTrackAndData/purgeHistory), completion_dao.dart (getCompletionsByTrack/completionsView), seed_manager.dart (ensureContentDb/_extractSeedDb/_decompressAndWrite), onboarding_screen.dart (childAwareText), learning_program_service.dart, sync_status.dart, and cross-referenced test/core/database/seed_manager_test.d

### t1-story-epics-21-24 (3 files, 9 findings)
Read all 3 batch files in full (1048 + 176 + 423 = 1647 lines) and cross-referenced 7 production sources (TrackRepositoryImpl, LearningOrderRepositoryImpl, SyncWriteFacade, PasswordHasher, CrashlyticsService, AuthState, account_picker_screen.dart), firestore.rules, the Makefile, learning_tracker/CLAUDE.md's testing conventions, git history for the two epic_24 files, and repo-wide greps for duplicate mock classes (11 files) and the story_acceptance directory listing (checked epic_24's naming against every other multi-file epic's convention). All 9 findings are CONFIRMED via static trace (source

### t1-story-epics-28plus (1 files, 4 findings)
Read the batch's single file in full (445 lines) and additionally ran it via `flutter test` to confirm all 19 assertions pass and to capture real runtime warnings, rather than relying on static reading alone. Cross-referenced the production code the test claims to verify (curriculum_overlap_registry.dart, curriculum_id.dart, lifetime_knowledge_providers.dart) and the shared fixtures it partially reuses (test/helpers/drift_memory.dart), confirmed Makefile wiring (test-story-i4/test-epic-28 targets, both folded into the `make test`/`make ci` aggregate via `flutter test --coverage`), and checked 

### t1-test-cross-p1 (10 files, 5 findings)
Read all 10 batch files in full (6008 lines: the E2EHarness plus 9 journey-test files spanning auth, dashboard, gamification, and guards/sync). Cross-checked test claims against the production code they exercise (reward_configuration_screen.dart, reward_config_controller.dart, active_tutored_profile_provider.dart, point_config_screen.dart) and against git history (git log -p, merge-base --is-ancestor) to date-order a bug-fix commit against the test that cites it. Empirically re-ran gamification_p0_test.dart's E2E-601 group with its skip removed via `flutter test --plain-name E2E-601` to confir

### t1-test-cross-p2 (9 files, 5 findings)
Read all 9 files in full (5,842 lines, matching the batch manifest exactly) and cross-referenced test claims against current source: backup_sync_section.dart, sync_orchestrator.dart, sync_status_providers.dart, outbox_providers.dart, onboarding_profile_creation_step.dart, app_en.arb/app_he.arb, notification_preferences_repository.dart, and city_picker_screen.dart. Confirmed one live EH-5/AX-2 defect the suite documents but leaves untracked (E2E-1113, no Linear id), one stale bug-skip that has silently forfeited real coverage since the underlying bug (R-OB7) was fixed, one tautological SharedPr

### t1-test-cross-p3 (8 files, 7 findings)
Read all 8 assigned files in full (6124 lines) plus the shared test/e2e/harness/e2e_harness.dart for context, and cross-checked skip-reason claims against integration_test/app_test.dart, docs/planning/e2e-test-suite-plan.md's R-code risk registry, and the sibling hebrew_rtl_p1_test.dart to verify which harness/coverage claims made in comments were actually true (one true-negative check: batch files lack per-file Locale('he') variants, but this is intentionally centralized in hebrew_rtl_p1_test.dart, so not flagged). Swept every file for wall-clock DateTime.now(), unseeded Random(), top-level m

### t1-test-cross-p4 (7 files, 3 findings)
Read all 7 batch files in full (5874 lines: scheduler_p1, settings_p0, settings_p1, sync_p1, tracks_p0, tracks_p1, tutoring_p0) plus the shared E2EHarness (test/e2e/harness/e2e_harness.dart), the E2E catalog (docs/planning/e2e-test-suite-plan.md), and the sibling hebrew_rtl_p1_test.dart for cross-reference on RTL conventions. Verified via git log and source inspection that most "device/harness limitation" skip comments in the batch (cloud-born auth tier, UncontrolledProviderScope, PersistentSwitcherScaffold, live-Firestore/DeviceRegistry flows, SharedPreferences wizard-resume state) are factua

### t1-test-cross-p5 (6 files, 10 findings)
Read all 6 files in full (6,367 lines): 2 integration tests (bulk-mark-prior streak suppression, B6/B8 bulk-prior-completion), 2 combined "L1" widget-test files (tracks/study-days/content-hierarchy; tutor-PIN-dialog/goal-setup), 1 E2E journey suite (tutoring), and 1 golden-screenshot suite (store screenshots). For each I cross-checked the test's own clock/DB/provider seams against their lib/ source (core/time/local_day_clock.dart, core/utils/date_utils.dart, core/providers/database_provider.dart, core/database/tables/stage_definitions.dart, test/helpers/drift_memory.dart, features/content_brow

### t1-test-cross-p6 (25 files, 5 findings)
Read all 25 files in full (~5233 lines): 8 integration tests (outbox bypass-cleanup, Firestore wipe-install round-trip, pull-on-launch merge, restore-skips-onboarding, Sacred-Time overlay scope, Shabbos notification suppression, stage sync, two-device convergence), 11 migration-gate tests (v15→v32, including the R5o-C1/R4-10/D14 regression gates), 2 overflow/widget layout tests, and 4 tool/subprocess tests (Android manifest, make-audit/arb-parity, cities-db builder, schema_check). For each file: verified fresh-container/fresh-DB-per-test discipline (TQ-2), wall-clock/randomness hermeticity (TQ

### t1-test-helpers (26 files, 8 findings)
Read all 26 batch files in full (fixtures, JSON shape files, flutter_test_config.dart, all of test/helpers/, infrastructure_test.dart, both mock files). For every helper I grepped the wider repo for callers/collisions (drift_memory.dart vs test_database.dart, MockConnectivityService, StageFixtures, goldenTest call sites, AppLocalizations.delegate boilerplate) and cross-checked exact claims against docs/coding-standards.md and lib/core/database/user/user_database.dart. Beyond static tracing I ran three empirical checks: `flutter test test/infrastructure_test.dart` (confirms it currently passes 

### t2-app-tool-p1 (30 files, 8 findings)
Read all 30 files in full (~5987 lines): Dart CLI tools (ARB/asset/cities/argon2/fixture builders, 8 Sefaria content fetchers, and a 1619-line static data table), two Python curation/audit scripts run against an external Sefaria-Project+Mongo checkout, a Node hebcal fetcher, 6 on-device QA workflow.js scripts, and seed/docker-compose.yml. Cross-checked every script's invocation against learning_tracker/Makefile and .github/workflows/*.yml (all comply with AG-1 — each is either wired to a make target or is itself a committed tool/ script), diffed curate_curricula/README.md's strategy-status tab

### t2-app-tool-p2 (15 files, 6 findings)
Read all 15 files in full (~4,257 lines): the Mongo-backed Sefaria ref-resolution library and its manual sample-checker (tool/seed/), five top-level Dart CLI build/verify scripts, a Go calendar fetcher plus go.mod, a Python extraction pair with its README, and a JS/bash pair that uploads content to Firebase Storage. Cross-checked every file against the Makefile (grepped for each filename), docs/seed-build.md, docs/linear-status.md, docs/api-contracts.md, docs/explainers/content-database.md, lib/firebase_options.dart, and repo-wide grep/find for downstream consumers, to separate what's actually

### t2-build-ci-guardrails (23 files, 15 findings)
Read all 23 batch files in full (including the full 993-line root coding-standards.md, sampled at start/middle/end and diffed against docs/coding-standards.md to confirm it's the already-baselined stale duplicate). Cross-referenced every Makefile target, CI job, and documented command against its counterpart in the other Makefile/workflow and against the real tool/ scripts and test suite via targeted greps: tag usage across test/story_acceptance vs learning_tracker/dart_test.yaml's declared set, the literal TODO/FIXME/XXX banned-phrase grep against real lib/ comments, mockito imports (none — o

### t2-firebase-rules-functions-p1 (16 files, 15 findings)
Read all 16 files in full: firestore.rules (447 lines) block by block against SR-1..5; functions/src/index.ts (2029 lines) end to end for every onCall/trigger/scheduled function's auth, validation, idempotency and concurrency behavior; all 7 functions/test/cf_*.test.mjs files (3,236 lines) plus their shared harness; firestore.indexes.json cross-checked against the queries in index.ts; and both docs cross-checked against the live rules/indexes files. Grepped the wider repo for supporting context only: Makefile/ci.yml (to confirm the CI-wiring gap is distinct from the already-baselined test-rule

### t2-firebase-rules-functions-p2 (6 files, 6 findings)
Read all 6 batch files in full (1819 lines): the canonical 887-line Firestore rules emulator suite (firestore_rules.test.mjs), its 101-line write_payloads.json fixture, functions/tsconfig.json, and the 3-file deprecated Jest suite under test/firestore-rules/. For grounding, also read the live learning_tracker/firestore.rules (447 lines) in full, the SR-1..5/TQ-9 sections of docs/coding-standards.md, tool/emit_fixture_payloads.dart, ci.yml's firestore-rules job, and the Makefile's test-rules/emit-fixtures targets — all out-of-batch context used only to verify claims made in or about my 6 files,

### t2-lints-hooks (21 files, 10 findings)
Read all 21 batch files in full: hooks/README.md, hooks/pre-commit, and the complete packages/custom_lints package (plugin registrar, all 9 lint-rule implementations under lib/src/rules/, pubspec.yaml, README.md, and the 7 existing rule test files). For every rule I cross-checked its path/whitelist logic against docs/coding-standards.md Rules 2-5 and against the parallel root-Makefile audit greps (checks 1, 2, 6, 11, 15), and confirmed every claim with a targeted repo-wide grep (existence of `providers.dart` vs `<feature>.dart` barrels, existence of `lib/features/auth/`, `firebase_providers.da

### t2-platform-config-p1 (34 files, 6 findings)
Read all 34 Android/iOS platform-config files in full (Gradle scripts, 3 AndroidManifest.xml variants, Android splash/style/color resource XML, MainActivity.kt, and the complete iOS Xcode project/workspace/plist/gitignore set). Beyond static reading, verified two suspicious claims empirically rather than by inspection alone: live-fetched the Android App Link target domain (confirmed assetlinks.json and Firebase Hosting are both absent, breaking two named deep-link features) and read flutter_tools' own source to confirm/calibrate the real blast radius of an untracked gradle-wrapper.jar before f

### t2-platform-config-p2 (6 files, 1 findings)
Read all 6 batch files in full plus adjacent context (AppDelegate.swift, pubspec.yaml, notification_gateway.dart, location_service.dart, Assets.xcassets contents, AndroidManifest.xml permissions, and Info.plist's git history) to check correctness and cross-config drift. Verified LaunchScreen/Main storyboard image and class references resolve against Assets.xcassets and Info.plist's UILaunchStoryboardName/UIMainStoryboardFile/UISceneDelegateClassName keys, and verified NSLocationWhenInUseUsageDescription against actual when-in-use-only geolocator usage in the sacred_time feature (accurate, no o

### t2-root-tool (33 files, 11 findings)
Read all 33 batch files in full — for the run3-6_full_suite.mjs siblings, full-content equivalence to the fully-read run2/run7_full_suite.mjs was established via byte-level `diff` (a stricter check than manual re-reading) rather than a second manual pass. Grepped the wider repo (root and learning_tracker Makefiles, docs/planning/epics.md, goal_dao.dart and goals.dart table/DAO source, `git branch -a`, `git log`) to verify cross-file drift, dead-code, and Rule-0-checker-coverage claims, and scanned the whole batch for hardcoded secrets/API keys/tokens (none found). Could not execute any script 

### t4-docs-canonical-p1 (19 files, 13 findings)
Read all 19 files in full (~6,426 lines) and cross-checked every concrete path/count/version/behavior claim against the codebase via targeted greps (learning_tracker/lib/**, test/**, Makefile x2, analysis_options.yaml, firestore.rules, firestore.indexes.json, functions/src/index.ts, .github/workflows/ci.yml, pubspec.yaml, AndroidManifest.xml, CLAUDE.md x2). Classified 6 files POINT-IN-TIME (dated bug-fix-session artifacts, a one-shot execution prompt, and a redesign spec that explicitly self-flags as canceled/archival) per the triage protocol and did not full-drift-audit them beyond a self-con

### t4-docs-canonical-p2 (18 files, 10 findings)
Read all 18 files in this batch in full. Classified 8 as POINT-IN-TIME per the digest's triage protocol (dated session captures / exec-prompts / a machine-generated workflow record that do not present themselves as current) and ran a full factual-drift sweep against code for the remaining 10. Verification method: grepped/read the actual current source (user_database.dart, content_database.dart, device_registry_database.dart, analytics_provider.dart, firebase_bootstrap.dart, crashlytics_service.dart, completion_dao.dart, test/helpers/test_database.dart, lib/core/enums/, lib/features/, seed_vers

### t4-docs-triage-p1 (40 files, 5 findings)
Read all 40 files in full or by thorough targeted sampling (the two >1500-line docs — architecture-design.md and the 4708-line on-device-exhaustive-test-plan-2026-05-31.md — were sampled by frontmatter/headers/footers plus targeted section reads, appropriate to their POINT-IN-TIME/well-self-annotated classification). Classified each per the Tier-4 triage protocol (POINT-IN-TIME vs presents-as-current) and ran a full drift-check with live-repo greps and git-log checks only on the latter: docs/explainers/* (all three), architecture-design/-offline-v2/-quick-reference.md, calendar-cycle-analysis.

### t4-docs-triage-p2 (40 files, 7 findings)
Read all 40 assigned files (docs/qa/legacy/00-13 in full; 26 docs/planning files — most in full, with the longest homogeneous tutor-edit-propagation/talmid-view sibling logs and orchestration-kickoff-prompt templates sampled at start/middle/end since their companion tracker/plan files were read in full and are authoritative for status). Verified specific factual claims against the live codebase via targeted greps/reads: confirmed the credential-less offline-account redesign in signup_screen.dart, confirmed TutorPinGuard no longer exists (superseded by lib/features/tutoring/ grant model), confi

### t4-docs-triage-p3 (40 files, 4 findings)
Read all 40 files in full (~12,603 lines): 6 qa/legacy manual-test docs, 8 dashboard-redesign-set scenario/spec docs (Epic 20), 19 evolution catch-up/amnesty scenario+spec docs (Epic 22), and 7 Stitch AI-image-generation prompts. Classified each POINT-IN-TIME vs presents-as-current per the AREA NOTE, then ran a full drift-audit only on the latter. For claims that could be checked, cross-referenced against the live codebase: bundled seed-asset filename (seed_manager.dart, tool/seed_content_db.dart), stage schedule types/10-stage cap (schedule_type.dart, stage_definition_repository.dart), onboar

### t4-docs-triage-p4 (40 files, 2 findings)
Read all 40 batch files in full: 11 stitch-prompt design-generation docs (08-17 + README), 4 docs/status/*.yaml trackers, and 25 story docs in docs/stories/implementation/ for epics 1, 16, 18, and 19. Cross-checked status claims against docs/linear-status.md (Tier-4 canonical doc, read for context per digest allowance), against each story file's own Status/Dev-Agent-Record fields, and — where a story claimed specific deletions/refactors — against the live learning_tracker/lib/ tree via targeted grep (confirmed for story 19-10). Found two systemic, well-evidenced status-tracking defects both an

### t4-docs-triage-p5 (40 files, 2 findings)
Read all 40 files in full (epic 19/21/25/27 implementation stories + DNI-32x/33x/34x/35x/37x/38x). Classified each by Status header, then cross-checked representative claims against current `learning_tracker/lib/` via targeted greps/finds (syncEngineProvider, `class SyncEngine`, sync_engine.dart, device_registry_database.dart, account_tier.dart, functions/src/index.ts onUserDeleted, completion_events.dart, add_track_flow_state.dart, schemaVersion, Makefile test-story targets) to confirm whether each doc's deliverables actually landed and whether the two non-"done"/"review" files still describe

### t4-docs-triage-p6 (2 files, 0 findings)
Read both files in full (845 + 1544 lines) and classified per the Tier-4 triage protocol. Both are design/planning artifacts written to seed story creation, not living documentation of current state: file 1 is an unbannered "Status: Draft" tool spec from 2026-03-29 written in prescriptive/future voice, cited only by five docs/stories/implementation/*.md story files (19-2b, 19-3, 19-4, 19-6, 19-12) and absent from docs/index.md, developer-handbook.md, and architecture.md; file 2 carries an explicit, dated "HISTORICAL — 2026-04-19 review... superseded by shipped v1... should not be used for curr

## Ledger verdicts (audited tiers)
{'SOUND': 783, 'ISSUES': 957, 'DEFECTIVE': 157, 'POINT-IN-TIME': 166}

## Exclusions by reason
{'vendored-agent-tooling': 2094, 'binary asset': 70, 'archived/test-artifact per Part C': 59, 'superseded-output-dir (hygiene finding)': 29, 'content/data asset or cache': 25, 'lockfile/license': 6, 'accidental/binary artifact (hygiene finding)': 5}

## Raw findings per rule family (983 total incl. 7 orchestrator)
lens/other:257, TQ-8:123, AG-3:82, TQ-6:54, AG-5:44, AG-8:36, AX-2:30, TQ-3:26, EH-3:24, EH-2:22, EH-5:19, Rule 0:16, SM-4:16, Rule 2:16, TQ-7:15, AG-4:15, TQ-1:14, AG-6:12, profileId:11, SM-7:10, AX-3:10, DB-2:9, AG-1:9, SM-6:8, EH-4:8, SM-2:8, AX-1:8, DB-3:6, TQ-4:5, PV-1:4, EH-6:4, DB-1:4, FB-2:4, PF-3:3, SM-1:3, SR-2:3, DB-4:2, AX-4:2, DB-6:2, FB-5:2, FB-3:2, TQ-2:2, SM-8:2, TQ-5:2, SR-1:2, AG-11:2, AG-2:2, AU-1:1, Rule 1:1, Rule 5:1, PV-5:1, FB-4:1, FB-9:1, SM-3:1, PF-1:1, DB-5:1, Rule 3:1, SR-5:1, TQ-9:1, SR-4:1, AG-7:1, PV-6:1, AG-9:1

## Global deterministic checks already run by orchestrator
AG-3: 327 files>400 lines | AG-4: 7 dup type names | AG-5: 537/715 lib files unmirrored | AG-6: 8 untracked TODOs | TQ-6: 377 DateTime.now() in test/ | SM-1: 109 hand-written providers | Tier-3 regen: 3 stale .g.dart | AG-4 global dedup: DONE

## Zero-or-one-finding batches (suspicion candidates)
t2-platform-config-p2(1), t4-docs-triage-p6(0)