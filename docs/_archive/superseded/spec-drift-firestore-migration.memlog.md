---
topic: Drift→Firestore-native migration of Learning Tracker
updated: 2026-07-31T03:28
---

- (direction) Headless/express mode; create op. Distilling ARCHITECTURE-SPINE (30 ADs, decision-complete), migration-plan (Phases 0-7), and the ratified architecture .memlog.md into the canonical migration SPEC; spine/plan/baseline stay adopted companions.
- (note) Why: retire the 12.2k-line (64-65 files) bug-prone custom Drift+outbox sync engine + per-account Drift user DBs; make Firestore (offline persistence, N named FirebaseApp per owner) the SDK-owned data layer. Driver = the fickleness/footprint evidence (reliability review 2026-07-29) + a simpler maintainable codebase; ~35 hard-won corruption guards must survive the port.
- (capability) CAP-1 AD-1 named-app topology smoke test (existential gate). intent: prove N named FirebaseApps against one project with independent Android caches is feasible. success: two named apps / one project / two anon users on the OLDEST supported device — writes+listeners in app A invisible to app B cache, no cache-directory collision; passes before any Phase-1 investment. (AD-1, AD-29 tier-3)
- (capability) CAP-2 Deterministic id hygiene. intent: no device-local Drift autoincrement id ever appears in a synced payload. success: doc-id module reproduces today formulas byte-for-byte EXCEPT AD-25 track-scoped re-key; learner_profiles doc-id = profile-scoped ULID distinct from path uid; canonical track key = curriculum_id; standing grep finds zero autoincrement-in-payload outside merge/. (AD-5, AD-13, AD-24, AD-25)
- (capability) CAP-3 Single-owner canonical LWW predicate. intent: one conflict decision governs every LWW collection. success: the +/-5s -> synced_at -> D15 -> true-tie predicate lives in exactly one grep-gated module, called by every reconciliation path; golden branch tests pin all four cases; no second copy can exist. (AD-7, AD-29 tier-1)
- (capability) CAP-4 Boundary enforcement via make-audit greps. intent: every mechanical boundary rule has a real mechanical gate (custom_lint is non-functional here). success: no-firebase-outside-core retargeted to lib/data/firestore + lib/data/repositories; bare FirebaseFirestore.instance / FirebaseAuth.instance banned outside the AccountFirebase registry; autoincrement-in-payload sweep is a standing gate; dependency-direction check green; all in make audit. (AD-28, AD-2, AD-3, AD-23)
- (capability) CAP-5 Listener resubscribe-on-error. intent: a dead snapshots() channel never stays dark while the network is up. success: each listener marks-dead + bounded-exponential-backoff resubscribes; connectivity-online/resume triggers resubscribe of dead channels; own AND tutored parity; capped retry never resolves as falsely-synced. Landable now on the current engine (survives migration). (AD-9, AD-22)
- (capability) CAP-6 Connectivity-source fix + slim 3-state status. intent: sync status is exactly synced|syncing|offline from SDK signals only. success: connectivity sourced from the hardened connectivityStreamProvider/platform events (direct connectivity_plus dep), never the 5s demo poller; exhausted-dead channel maps to syncing (never synced); zero app-level pending/dead-letter bookkeeping. Landable now (survives migration). (AD-11, E-1)
- (capability) CAP-7 Per-account named-FirebaseApp subsystem. intent: each device account (<=5) owns an isolated FirebaseApp+Auth+Firestore+cache. success: AccountFirebase registry creates/tears-down account_<registryUuid> apps with bounded-cache Settings pinned before first use; handle resolves via accountFirebase(activeAccountId); named-app key = stable registry UUID, path uid = persisted field w/ remap-on-anon-reset; instant offline switch between two previously-synced accounts w/ network disabled; no PERMISSION_DENIED flood on switch. (AD-1, AD-2, AD-18, AD-24)
- (capability) CAP-8 Repository seam + tutor_grants vertical slice. intent: repositories are the sole data seam and the native pattern is proven end-to-end. success: 10 empty repo scaffold dirs filled/re-pointed, 27 direct-DAO bypasses closed, zero feature/service/provider imports cloud_firestore (grep proves it); tutor_grants reads/writes entirely through its repository over a named app w/ codec, doc-id, canonical predicate, resubscribe listener, routing-parity test green. (AD-3, AD-17, AD-23, AD-22)
- (capability) CAP-9 Per-collection strangler waves to native. intent: cut each collection from Drift+merger to a native repository, risk-ordered and reversible. success: per wave, shadow run shows ZERO divergence before reads flip; Wave A (track-scoped identity cluster) cuts atomically w/ the AD-25 one-time re-key backfill + cross-device match test; Wave B config+prefs fan-out (single owner); Wave C append-only history + derived balances; Wave D completions+tombstones; red-demos per wave (tombstone resurrection, double-credit idempotency, out-of-order arrival); legacy path retained for per-collection rollback. (AD-4, AD-5, AD-6, AD-7, AD-8, AD-10, AD-12, AD-13, AD-25, AD-27, AD-29 tier-2)
- (capability) CAP-10 Permanent-write-failure recovery affordance. intent: a genuinely non-retryable write is not silently lost. success: a permission-denied/non-retryable write (distinct from offline/backoff the SDK will drain) surfaces a per-item out-of-band tap-to-retry, separate from the 3-state chip; NOT a revived dead-letter counter; successor to the retired error-status/dead-letter recovery path. (AD-30)
- (capability) CAP-11 Cross-account defect fixes + Anonymous-Auth local-born tier + profile hard-delete. intent: fix the shipped cross-account data-loss defects and give the credential-less tier a real principal. success: deleteAccount scoped to the target account namespaced keys (others intact); per-account sync/restore flags; (accountId,profileId) key namespacing w/ idempotent additive on-device re-home; local-born = Anonymous Auth, upgrade = account-linking, survives anon-uid reset w/o stranding cache/tree; curriculum_scopes full bidirectional sync; uniform write path + transactional fulfilRedemption; profile hard-delete set is registry-derived (incl. curriculum_scopes AND import_metadata), client-deletable via batch, SR-1 history via CF recursiveDelete, zero orphans. (AD-14, AD-15, AD-19, AD-20, AD-21, AD-24, AD-26)
- (capability) CAP-12 Existing-user backfill + shadow verification + point of no return. intent: existing users cloud/local data lands natively without loss. success: device-local per-account idempotent Drift->named-app Firestore replay via the LIVE codec/doc-id; precondition gates (refuse pre-v30 ledger, require >=v37 stage_id_format marker, re-verify TrackLearningOrder.profileId); dual-run verifier asserts count + per-doc field equality per collection/account; firestoreSourceOfTruth flag gated on a clean verifier pass; re-run is idempotent (no duplicates) and mid-backfill crash is resumable; Drift copy retained read-only until Phase 6. (AD-13, AD-16, AD-29)
- (capability) CAP-13 Custom sync engine retirement + deletion. intent: delete the engine once no account depends on it. success: lib/core/sync/** (~12.2k lines, 64-65 files) + Outbox/OutboxProcessor/pipelines/supervisor/MergeRouter/18 mergers/SyncKv + per-account Drift user-DB schema + dead paths + the 84 orphaned sync test files removed; no cloud_firestore import outside data/firestore + data/repositories; app builds and passes full make ci + make audit with the engine absent; gated on all accounts past point of no return + retention window elapsed. (Phase 6)
- (capability) CAP-14 Verification, telemetry + staged rollout. intent: make the previously-unmeasurable observable and roll out safely. success: telemetry for per-collection read counts per launch/resume, listener onError/resubscribe, backfill/verifier outcomes, slim-status queue-depth proxy; App Check enforcement confirmed green in production (debug-token discipline); min-app-version/cohort staged rollout w/ no cross-account regression; full MCF-1..35 sign-off (every row demonstrably handled by its AD). (AD-12, AD-29, Phase 7)
- (decision) Companions ADOPTED (referenced, not edited): ARCHITECTURE-SPINE.md, migration-plan.md, drift-to-firestore-migration-baseline.md. Spec-authored companion: traceability.md (capability -> AD -> phase -> MCF). Diagrams stay in the spine (adopted). Source fully absorbed: architecture .memlog.md (ratified owner decisions). MCF-1..35 register is the binding completeness contract carried by the adopted baseline.
- (constraint) Every MCF-1..35 (and suffixed variants) maps to an owning AD/capability OR an explicit retirement rationale — the Capability->Architecture Map in the spine is the completeness contract; narrowing it silently reintroduces a shipped P0.
- (constraint) Child-data integrity: never reopen the shipped P0 corruption classes — PointsBalance derived-not-synced-counter (MCF-2), hard uniqueness by doc-id to close double-credit (MCF-8), completion tombstone-resurrection matched WITHOUT a timestamp filter (MCF-6), never migrate a pre-v30 ledger (MCF-9), no device-local autoincrement id in any payload (MCF-11).
- (constraint) Cloud-schema continuity: existing users historical docs must keep round-tripping — reproduce every doc-id formula byte-for-byte and keep decoding every legacy field alias, EXCEPT the AD-25 track-scoped one-time re-key (curriculum_id key) which byte-for-byte cannot cover. (AD-13, AD-25, MCF-3-continuity)
- (constraint) Content DB (~87K bundled read-only rows, SeedManager gzip/.bak self-heal) and the Device Registry (pre-auth account picker) stay LOCAL and offline-from-first-launch — never migrate to Firestore. (AD-16, MCF-35)
- (constraint) Security-rules-as-boundary preserved: SR-1 append-only (deny delete; update only on identical replay) + SR-3 real Timestamp <= request.time; App Check enforcement MUST be extended to firestore.rules (request.app) before the strangler waves ship broadly, because the direct-SDK surface widens. (AD-12)
- (constraint) Mechanical boundary rules bind to real make audit greps, never custom_lint (documented non-functional in this repo — reports No issues found even on violations). (AD-28)
- (constraint) Strangler discipline: nothing legacy is deleted until its native replacement ships and shadow-verifies zero divergence; the engine is retired only in Phase 6, after every account passes the backfilled+verified point of no return. Rollback is per-collection until then. (migration-plan strategy)
- (constraint) Every phase exits green on make ci + make audit + its named red-demo gates; an internal-track release ships at each phase exit.
- (constraint) The named-app topology paradigm is contingent on the Phase-0 empirical smoke test (CAP-1) passing on the oldest supported device; the single existential risk. No Phase-1 investment before it passes.
- (note) Non-goal: no hardening of the custom sync engine beyond the two migration-surviving fixes (resubscribe-on-error AD-9; connectivity-source + slim-status AD-11). sync-wave1/2/3 threads FROZEN; outbox depth-gating, dead-letter accounting, batch-writer wiring, at-limit/cold-launch tuning and the rest of the review 15 fixes are DROPPED as mooted by Phase-6 deletion.
- (note) Non-goal: no iOS work; Web offline-switch is an explicit non-target (Web persistence differs) unless separately funded. (AD-18)
- (note) Non-goal: no Content-DB migration — the bundled ~87K-row content DB and CitiesRepository sqlite stay local/out-of-scope. (AD-16)
- (note) Non-goal: no rich pending-count status UI — slim 3-state is decided; no N-pending/N-stuck accounting, no dead-letter counter. (AD-11, MCF-22)
- (note) Non-goal: no parsha-level / content restructuring; no server-side data migration except the client-driven idempotent gamification_settings fan-out.
- (note) Non-goal (deferred, not this migration): delta-pull watermark+composite indexes, real-time re-scope to collaborative-only data, metered/cellular mode, sqlite3 2.x->3.x upgrade, single-doc->collection preference-listener consolidation, park-delay tuning. (spine Deferred)
- (assumption) N named FirebaseApps against one project/database with fully independent Android persistent caches is feasible on the oldest supported device — proceeded under, EMPIRICALLY GATED by CAP-1 (Phase-0 smoke test) before any Phase-1 commitment. The API surface supports it but it is not an officially blessed config.
- (assumption) functions/** (tutor invite/accept/revoke, recursiveDelete) remain unchanged; tutor read continues to hinge on tutor_active_access exists(); no server-side data migration except the client-driven gamification_settings fan-out.
- (assumption) Android is the sole offline-switch target; Crashlytics/Analytics stay default-app-only (per-account crash attribution is accepted as coarse/device-level).
- (question) Fate of legacy users/{uid}/profile/data and any pre-version import_metadata production data — read-compat shim or conscious GC? import_metadata is added to the AD-26 delete set, but the round-trip owner and the profile/data orphan path are not owned by any AD. (baseline Q11)
- (question) If CAP-1 (named-app topology smoke test) fails on old hardware there is NO ratified fallback — the owner explicitly rejected the reduced-experience (most-recent-account-only offline) option; a failed gate reopens the core paradigm decision. (What-could-kill-this #1)
- (question) GoalMerger F4 tie-break reachability (MCF-27) is unconfirmed (no test found); the targeted unit test in Phase-3 Wave A decides preserve-vs-fix — concurrent-decision behavior parity is undetermined until then.
- (event) Self-validate Pass 1 (coherence, Spec Law 1-6+8): PASS. All 14 caps carry intent+success; intents are WHAT (mechanism only in success/testable gates); all 9 constraints rule something out; 6 explicit non-goals; success signal is testable (engine deleted, named gates, zero-divergence verifier, MCF sign-off); CAP-1..14 stable/unique. No content invented; unconfirmed calls parked as 3 assumptions, unresolved gaps as 3 open questions.
- (event) Self-validate Pass 2 (preservation, vs migration-plan phases + ratified decisions): PASS. 30 ADs all land in a capability (traceability AD-table); Phases 0-7 -> CAP-1..14; MCF-1..35 preserved by citation (Constraint: MCF-mapping mandate + traceability MCF column + adopted baseline register + adopted spine map, per the cite-not-duplicate rule); ratified owner decisions + sync-wave freeze -> caps/non-goals/assumptions; AD-1 topology caveat -> CAP-1 gate + assumption + open question. Intentional wrapper-only drop: the sonnet/opus subagent-execution note (process, not a spec constraint). No silent drops.
- (event) Spec finalized: SPEC.md (14 caps) + traceability.md derived from memlog; status draft; adopted companions = spine, migration-plan, baseline; source absorbed = architecture .memlog.md.
