---
scope: Drift→Firestore-native migration: retire the 12.2k-line custom sync engine + per-account Drift user DBs; Firestore + offline persistence becomes the data layer
purpose: build-substrate + owner-facing phased migration plan
altitude: initiative
updated: 2026-08-02T03:34
---

- (decision) [ADOPTED][owner] Multi-account: FULL per-account offline via N named FirebaseApp instances (≤5/device); per-account app/cache lifecycle is a core subsystem. Binds: every service resolves Firestore through the active account's FirebaseApp. Prevents: cache/identity bleed between accounts.
- (decision) [ADOPTED][owner] Sync status: slim 3-state (synced/syncing/offline) derived from SDK signals only; no custom queue bookkeeping. Prevents: reintroducing app-level outbox accounting.
- (decision) [ADOPTED][owner] All pre-existing cross-account defects in scope: deleteAccount blast radius, global once-only sync/restore flags, bare-profileId key namespacing → (accountId,profileId).
- (decision) [ADOPTED][owner] Bundled content DB (~87K rows) stays local/bundled — never migrates to Firestore.
- (constraint) MCF-1..35 register in docs/planning/drift-to-firestore-migration-baseline.md is binding: every invariant must map to a target-design owner or an explicit retirement rationale. MCF-2 (derived points balance) and MCF-4/-11 (no device-local autoincrement ids in payloads) are dangerous-class.
- (constraint) Existing users' cloud data must keep working: reproduce doc-id formulas + legacy field aliases (MCF-3-continuity); tutor_grants CF/exists() model (MCF-17) preserved.
- (constraint) Standing rule (Daniel): heavy work by sonnet/opus subagents only; deliverables land status:draft in docs/planning/ and are committed to dev.
- (direction) Fast path chosen; deliverables = ARCHITECTURE-SPINE.md + migration-plan.md (owner-facing phases w/ entry-exit criteria, risks, rollback).
- (event) Fast-path draft complete: spine (AD-1..23) + migration plan (Phases 0-7) written by opus drafter
- (event) Reviewer gate: lint PASS(0); rubric APPROVE-with-changes(5); web-verify conditionally-sound (N-named-apps topology uncertified -> Phase-0 smoke gate); adversary REJECT (6 divergence pairs, 2 internal contradictions)
- (decision) AD-24: app-name = stable device-registry UUID; Firestore path-uid = persisted field with remap-on-anon-reset; never live currentUser. Closes adversary pair 1.
- (decision) AD-25: canonical track key = curriculum_id; one-time doc-id re-key + backfill supersedes byte-for-byte continuity for track-scoped collections. Closes AD-5/AD-13 contradiction.
- (decision) AD-26: profile/account delete set is registry-derived (16 subcollections, not Drift's 6); SR-1-protected history deleted via CF recursiveDelete. Closes pair 3.
- (decision) AD-27: every two-store fan-out gets a single owner; Firestore authoritative, prefs a rebuildable projection. Closes pair 6.
- (decision) AD-28: every boundary AD binds to a real make-audit grep (custom_lint dropped); supersedes-and-retargets the no-firebase-outside-core gate.
- (decision) AD-29: three-tier verification (unit-pinned predicate, emulator, on-device smoke) for SDK-signal invariants.
- (decision) AD-30: non-transient sync failure surfaces a recovery affordance (carries R-4/R-7 forward natively).
- (event) Revision applied all reconcile+gate findings; 30 ADs; docs remain status:draft; 5 items surviving for Daniel's Fast-path review
- (decision) [ADOPTED][owner 2026-07-30] AD-19 ratified: offline/local-born tier = Firebase Anonymous Auth from creation; convert-on-reconnect = account-linking; AD-24 re-home covers anon-uid loss.
- (decision) [ADOPTED][owner 2026-07-30] AD-20 ratified: curriculum_scopes = full bidirectional owner sync; tutor CF path unchanged; +import_metadata on delete list.
- (decision) [ADOPTED][owner 2026-07-30] AD-21 (narrowed) + AD-22 ratified: uniform write path incl. completions, transactional fulfilRedemption; tutored listener park/unpark parity, rules-enforced isolation.
- (decision) [ADOPTED][owner 2026-07-30] Sync-wave coordination: FREEZE to the two migration-surviving fixes (dead-listener resubscribe-on-error; connectivity fix + slim-status groundwork). All other wave-1/2/3 engine-internal items dropped as mooted by Phase-6 deletion.
- (event) Fast-path owner review complete: 4/4 assumptions ratified to recommended options; only AD-1 topology caveat remains (empirical, Phase-0 smoke gate).
- (event) Ratification edits applied and verified (1 remaining [ASSUMPTION] = AD-1 empirical topology caveat; lint 4 pre-existing low false-positives on doc-id path templates). Spine is decision-complete; status stays draft pending human/wiki ratification per owner's standing rule.
- (decision) Plan aligned to the 2026-07-30 two-survivor freeze: both quick-win passages corrected from three to two (R-4 dropped; masking half mooted by AD-11, recovery half at AD-30/CAP-10 Phase 3). Coordinator resolution of the prose/decision inconsistency flagged by the epics extraction.
- (decision) Epics breakdown scope corrected: CAP-3 (canonical LWW predicate extraction) confirmed IN Phase 0 per the ratified plan; the narrower CAP-1/2/4-only framing was instruction drift.
- (decision) [owner 2026-08-02] Story 1.5 slim-status kept as ratified on final [C]: AD-30 recovery affordance stays Phase 3, no Story 1.6. Accepted UX regression window (permanently-failed write shows as 'syncing' until Phase 3).
- (event) Epics & Stories workflow COMPLETE (step-04 [C]): epics-firestore-migration-phase0.md READY — 2 epics / 11 stories, adversarially validated. Deliverables chain closed: baseline -> spine(30 ADs) -> plan(Ph0-7) -> SPEC(14 caps) -> epics/stories.
- (event) STORY 2.1 GATE RESULT = **GO** (2026-08-02). AD-1's N-named-FirebaseApp topology is EMPIRICALLY CONFIRMED on real hardware. Evidence: integration_test/firestore_multi_app_isolation_test.dart run on emulator-5560 (API 34) AND emulator-5554 (API 28 = oldest supported), both green, the API-34 run independently reproduced by the coordinator. Distinct on-disk persistence artifacts DIRECTLY OBSERVED (not inferred): databases/firestore.account_A.torah-study-tracker.%28default%29 vs ...account_B... — app identity embedded in the filename. Cross-app cache read of app A's doc is invisible to app B. Red-demo (one shared app/cache) correctly caught the leak, so the isolation assertion is not vacuous. Resource pass: API 28 VmRSS 228->281MB, fds 94->121; API 34 395->434MB, fds 144->173 — no OOM/fd-exhaustion/crash.
- (decision) AD-1 [ASSUMPTION] topology caveat RETIRED by the Story 2.1 GO. The spine has no remaining open assumptions; Phase 1 (per-account FirebaseApp subsystem) is cleared to build on the confirmed topology.
- (note) Story 2.1 environmental findings for future device runs: (1) these AVDs boot with airplane_mode_on=1 — must be cleared or all Firebase emulator connections fail with 'Failed to connect to 10.0.2.2' (an infra failure that must NOT be misread as a topology NO-GO); (2) a debug-only network_security_config + AndroidManifest merge is required for cleartext to 10.0.2.2 (release/profile untouched); (3) worktrees need the gitignored google-services.json + firebase_options.dart copied in and 'dart run build_runner build' before make audit compiles.
