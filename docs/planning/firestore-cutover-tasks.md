# Firestore cutover — task list

Durable companion to [`firestore-cutover-plan.md`](firestore-cutover-plan.md).
Session task lists do not survive; this file does. **Update it in the same
commit as the work it describes.**

**Last updated:** 2026-08-07 · head at P2-23 (commit SHA not yet knowable
— same self-reference lag as every prior closing commit; the immediate
parent is `c794cb35`, a concurrent sibling session's own `T-58` fix that
landed mid-session — zero file overlap with this round, see
`firestore-cutover-log.md`'s **P2-23** entry, DEVIATION section, for the
full account; the last CODE commit touching this round's own files is
`bb97707e`, P2-21).
**Phase 2 is `NOT RESOLVED` — but `T-49` itself is now closed for real.**
P2-23 fixed the defect P2-22's fourth-round review reopened: all THREE of
`_ensureFirestoreProfile`'s original callers (`createProfile`,
`ensureDefaultProfile`'s self-heal branch, `ensureRemoteProfile`) are now
proven, by a PERMANENT test covering all three in one file, never to
clobber a newer profile selection with a late-settling write. **Fix
shape, exactly as the reviewer who reopened the task identified and P2-23
verified before applying:** `createProfile`/`ensureDefaultProfile` now go
through a NEW method, `_activateThenEnsureFirestoreProfile`, which
activates `activeProfileDocIdProvider` BEFORE attempting the Firestore
write, not after it settles — closing the window a late-settling write
used to clobber through. `ensureRemoteProfile` is unchanged in behaviour
(still never activates) but now reaches the write through a plain
`_ensureFirestoreProfile(model)` with no boolean at all: the
`activateProvider` parameter P2-18 added is DELETED — activation and the
write are two different methods now, so there is no flag to get wrong.
**Proof, in order:** (1) the reopening probe was reproduced RED on the
unfixed tree first (`Expected: 'ulid-p223-profile-b' / Actual:
'ulid-p223-profile-c'`, and the equivalent for `ensureDefaultProfile`);
(2) made into a permanent file,
`test/features/profiles/data/repositories/profile_repository_impl_t49_activation_ordering_test.dart`,
covering all three callers; (3) GREEN after the fix (`+3`); (4) proved
real by a byte-exact `cp` revert (never `git stash`) — RED again on the
reverted tree, restored, md5-verified identical to the fixed file; (5)
`flutter test test/features/profiles/` → `+428` (425 baseline + 3 new), 0
failures; (6) `make test` (full suite) — see this file's row, below, and
`firestore-cutover-log.md`'s **P2-23** entry for the verbatim numbers.
**Phase 2 stays `NOT RESOLVED` regardless — `T-39` and a fresh independent
review of THIS commit (not self-certified) still gate Phase 3** — see
`firestore-cutover-log.md`'s **P2-23** entry, "Phase 3 ENTRY CRITERIA," for
the current checklist (every prior snapshot, including P2-22's, is left
unedited, append-only, per that file's rule). **Three further findings
from P2-22's review remain open, unaffected by this fix, none blocking
Phase 3 on their own (MINOR, carried like `T-44`/`T-46`/`T-55`):**
`T-56` (a second, unguarded post-await write to `activeProfileDocIdProvider`
in `AutoSelectedProfileId._resolveSelection`), `T-57` (adult-profile
creation deterministically, not racily, mis-keys all 13 profile-scoped
Firestore providers), `T-58` (`audit_and_arb_parity_test.dart`'s
`'prints file:line paths for violations'` confirmed RED in the
`make test-serial-tools` lane — pre-existing, not Phase-2-attributable,
previously undertracked; a concurrent sibling session was separately
working this task while P2-23 ran — see that entry for the disclosure).
This does **not** unwind P2-16's own verified
work on `T-40`/`T-43`, nor `T-50`/`T-51`/`T-52`/`T-53`/`T-54` (all
genuinely closed or ruled, re-confirmed this round, unaffected): `T-40`
and `T-43` — the plan's own **original** named blocking exit criterion —
were fixed at P2-14 and independently re-verified at P2-16 (a separate
pass traced all three activation paths call-site to call-site, personally
reproduced the wiring test's RED-before/GREEN-after toggle with an
md5-verified restore, and independently re-ran the directory-level test
nets rather than trusting P2-14's own file list), and `T-48` (SERIOUS —
the `created_at` clobber) was fixed at P2-15 and independently re-derived
at P2-16. `T-52` (MINOR — `make audit` had two different Makefile targets)
was fixed docs-only at P2-17. `T-50` (MINOR — `repository_providers.dart`'s
doc-comment fix, applied to 3 `.md` files at P2-16 but never to the code
itself until P2-20) and `T-51` (the v38 schema-migration `ulid IS NULL`
producer, CARRIED-BY-RULING at P2-20 under the owner's 2026-08-07
greenfield ruling) both stand, unaffected by `T-49`'s reopening. `T-53`
(7 e2e-journey ulid-less seeders) and `T-54` (a stale `learning_order`
rules-test assertion that also blocked the TQ-9 rule-coverage gate) were
both closed at P2-21, when `make ci`'s full, unscoped `flutter test` and
`make test-rules` were run to completion for the first time this phase —
also unaffected. `T-44` and `T-46` (MINOR, informational, unrelated to
Phase 3) remain open, unchanged, and are explicitly **not** Phase 3
blockers — carried outside the plan's original T-40/T-43 criterion.
`T-45` and `T-47` are `done` (P2-19) — the third and fourth ulid-less test
seeders fixed and all 6 inherited red tests closed. `T-55` (MINOR,
informational, ~60 further ulid-less test seeders, none currently failing)
remains `todo` (P2-21), unaffected. `T-41` and 15 of `T-42`'s 16 items
remain independently reconfirmed `done`. Full disposition, evidence,
mechanism and every proof: `firestore-cutover-log.md`'s **P2-14** entry
(T-40/T-43), **P2-15** entry (T-48), **P2-16** entry (independent
re-verification + two documentation corrections), **P2-17** entry (the
four unrecorded gaps, the new SERIOUS defect, the DECISION RULE applied,
the original Phase 3 checklist), **P2-18** entry (`T-49`'s FIRST,
INCOMPLETE fix), **P2-19** entry (`T-45`/`T-47`'s fix), **P2-20** entry
(`T-50`'s code fix, `T-51`'s ruling), **P2-21** entry (`T-53`/`T-54`'s
fix, the first full-CI-suite run this phase), **P2-22** entry (`T-49`'s
reopening, reproduced by execution; `T-56`/`T-57`/`T-58`; the superseding
deferred-verification table), and **P2-23** entry (`T-49`'s real close —
all three callers, the permanent test, the revert-proof). Phase 3's own
prerequisite `T-39` remains `todo` below, unaffected by `T-49`'s closure.
`T-37` remains `todo` below, also unrelated.

Status values: `todo` · `doing` · `done` · `blocked` (open, and something —
usually evidence, not just effort — must land before it can move) ·
`decided` (owner has ruled; implementation still outstanding) · `re-phased`
(moved to a later phase by an owner ruling, evidence recorded in place
rather than deleted). **`blocked` also covers "was `done`, a later
independent review found the fix does not work"** (`T-40`, this revision) —
the row is corrected in place rather than assigned a new id, since it is
literally the same unresolved task.

---

## Open

| ID | Phase | Status | Task |
|----|-------|--------|------|
| T-30 | 3 | re-phased | **Owner-path Cloud Functions still key `learner_profiles` by the Drift int.** Re-phased from Phase 2 to Phase 3 per Q1's ruling (`firestore-phase2-plan.md` §3), landing with T-20 as one commit-unit. `functions/src/deletes.ts`: `deleteLearnerProfile` (:135), `deleteCurriculumTrack` (:214), `deleteBulkMarkedCompletions` (:406) each validate `profileId` as a positive integer (:136, :215, :407) and address `.collection("learner_profiles").doc(String(profileId))` (:225, :441). Post-cutover they address a path holding no data — **delete nothing, report success**. `deleteBulkMarkedCompletions` implements the owner's un-tick-a-bulk-mark rule, so that feature silently stops working. **Design note added at re-phasing:** `deleteLearnerProfile` must capture the profile's ULID **before** the local delete removes the row — `profile_repository_impl.dart:284` deletes the Drift row, `:291` calls the sync engine, and the adapter at `:538-539` delegates straight through, so a naive re-keyed implementation would have nothing left to send by the time it tries to build the remote path. |
| T-31 | 3 | re-phased | **Tutoring identity is Drift-int end-to-end.** Re-phased from Phase 2 (was `decided`) to Phase 3 per Q1's ruling — the coupling is blocking, not stylistic. **Evidence (verified 2026-08-06, `firestore-phase2-plan.md` §3 Q1):** `TutoredProfileSelection.profileId` is a live Firestore path segment on both sides — **13 read collections** (`pull_pipeline.dart:73-98`: completions, bookmarks, curriculum_tracks, settings, goals, learning_ledger, stage_definitions, streak_events, study_day_configs, profile_programs, learning_order, points_ledger, reward_redemptions, plus 3 preference docs) and **9 write collections** (`tutor_writes.ts:187` builds one `profilePath`; written through at completions `:285`, goals `:346,:399`, curriculum_tracks `:455,:506`, stage_definitions `:562`, study_day_configs `:621,:672`, preferences/gamification_settings `:744`, bookmarks `:804`, profile_programs `:864`, curriculum_scopes `:927`). For 11 of the 13 read collections the owner-side writer is still the int-keyed sync engine — re-keying tutoring alone in Phase 2 would make the tutor read a tree nothing writes and write a tree nobody reads, silently, with every gate this phase runs green. **Corrected site count:** `manage_tutors_screen.dart`'s `.id.toString()` sites are **six** (`163,173,206,293,298,312`), not the three the original task text named — the three it omitted include `outgoingTutorGrantsProvider`, which filters `where("child_profile_id","==",…)` (`tutor_invites.ts:522-528`), so converting only three would list a child's grants under the old id while creating them under the new one. **Two citation drifts found during the extraction pass:** `invite_tutor_screen.dart:112` is the **converse** operation (parses the string profile id back to int for comparison, not a write), and `tutored_write_router.dart:410` is the **signature line** — the actual `int.tryParse` call is at `:412`. **Decoupled line item already shipped:** `ProfileDao.upsertTutoredProfile` records the remote child's own id as `ulid` rather than minting a fresh one — landed in P2-2 (`0d5d9125`), changes no wire value and no path segment. **The rules formula does not change** — `hasActiveTutorAccess` (`firestore.rules:87-91`) and `acceptInvite` already build the identical `{tutorUid}_{ownerUid}_{profileId}` string; only what `profileId` *is* changes. |
| T-37 | 3 | todo | **Tutored read seam — owner-uid-scoped handles.** P2-5's hoist (`repository_providers.dart`'s `_watchActiveAccountAndProfile`) correctly refuses all 13 profile-scoped providers during a tutored session, but sources `uid` from the signed-in account — so simply substituting the profile ULID without also substituting the owner's `uid` would address `users/{TUTOR}/learner_profiles/{talmid ULID}`, a brand-new wrong tree, not the parent's real one. Build the owner-uid-scoped handle seam so a tutored session reads `users/{ownerUid}/learner_profiles/{talmid ULID}/...` directly; the rules already permit it (`firestore.rules:450` + 16 sibling `allow read: if isOwner(uid) \|\| hasActiveTutorAccess(uid, profileId)` lines). Blocks D1's completion (the tutor-reads-the-parent's-tree-directly decision). **Trap already fixed at its source (P2-12):** `repository_providers.dart`'s comment previously gave a false reason for the tutored refusal (said the mirror carries no ULID — it does, since P2-2, `profile_dao.dart`'s `upsertTutoredProfile`); the comment now states the real reason — pairing the talmid's ULID with the TUTOR's own account handles here would still resolve `users/{TUTOR}/learner_profiles/{talmid ULID}`, a wrong tree — so a T-37 implementer reading it cold is no longer misdirected. The wrong-tree trap itself is still real and is exactly what this task's owner-uid-scoped handle seam must avoid. |
| T-38 | 5 | todo | **Gate retarget + housekeeping, folded together.** Fold check 104 into T-23's gate retarget. Fix `Makefile:1366`'s stale `all 68 greps clean` summary string (T-23 already knew of this; check 104 made it stale a second way — the check count is now 104, not the number the string implies). Un-skip `test/tool/audit_and_arb_parity_test.dart:112-125` — its `skip:` reason is now false (`make audit` currently exits 0). |
| T-39 | 3 | todo | **Reconcile the gate's WATCHLIST against `CURRENT STATE`'s "dead adapters" list before wiring anything.** Check 103's 10-collection WATCHLIST and the log's 7 "dead adapters" are not the same set — 5 gate names have no counterpart in the log's list, 2 log names have no watchlist entry. Prerequisite for T-20; do this reconciliation first so Phase 3's wiring order is not built on a wrong inventory. |
| T-40 | 2 | **`done` (P2-14, third attempt)** | **FIXED — the trigger moved to `SelectedProfileId.select()` (`profile_providers.dart`), the ONE seam every activation path in the app already funnels through** (verified by enumerating every `selectedProfileIdProvider.notifier).select(` call site in `lib/`: the route guard via `router_provider.dart`, the ≥2-profile picker, the switcher sheet, sign-in, onboarding, device restore, a notification tap, add-profile, edit-profile, and `AutoSelectedProfileId`'s own zero-profile self-heal — all of them, nothing else). This reaches cold start (the guard calls `select()` synchronously before the shell can build) AND the ≥2-profile picker AND an in-app switch, with one hook instead of three. Gated on `ref.read(activeAccountIdProvider) == null` before touching `profileRepositoryProvider` — cheap, in-memory, matches the precondition `activeAccountFirebaseProvider` itself already enforces — because `profileRepositoryProvider`'s build unconditionally opens a REAL on-disk Drift database via `userDatabaseProvider`; without this gate the fix broke `profile_switcher_sheet_test.dart`'s "tapping a non-active profile" test with "A Timer is still pending even after the widget tree was disposed" (measured, then fixed — see `firestore-cutover-log.md`'s **P2-14** entry). **Proof, not a trace:** a new WIRING test (`test/features/profiles/presentation/providers/profile_activation_heal_wiring_test.dart`) drives the REAL `ProfileGuard`, wired exactly as `router_provider.dart` wires it in production, through a cold-start single-profile auto-select with a missing remote document — confirmed RED on the pre-fix tree (reverted the trigger, re-ran, got the exact failure), GREEN after. `app_shell.dart`'s dead `ref.listen` block deleted; `profile_repository_impl.dart`'s class doc comment, `ensureRemoteProfile`'s own doc comment, and `profile_repository.dart`'s interface doc all corrected to name the real trigger. Full evidence: `firestore-cutover-log.md`'s **P2-14** entry. |
| T-41 | 2 | `done` | **RESOLVED for its stated scope (P2-9) — independently reconfirmed at P2-13.** `ProfileDao.upsertFromSync`'s insert branch (no local row for the remote id yet) now throws a new named `ProfileSyncMissingUlidException` instead of inserting a `ulid IS NULL` row — the legacy int-keyed `learner_profiles` wire format (`LearnerProfileCodec`) has never carried a `ulid` in either direction, so under the greenfield ruling this path cannot legitimately carry one through. `LearnerProfileMerger`'s existing per-row `on Exception catch` isolation contains the throw — one offending row is skipped/logged, not the whole sync pull — and because `DriftMergeStore.upsert` runs inside the merger's `runInTransaction`, any FK-remap side effect `_resolveLocalAccountId` already wrote also rolls back. The UPDATE branch (a local row already exists) is untouched — it never wrote `ulid` before and still doesn't. `DataExportImportService` now carries `ulid` in both `exportData`'s map and `importData`'s companion, mirroring the sibling `learningLedger` block exactly. `learner_profiles.dart:58-77`'s doc comment corrected. **Residual, NOT prevented by this fix — see `T-44`:** the refusal *relocates* the second-identity outcome (a self-heal can still mint a fresh ULID for the same logical profile) rather than eliminating it; **see `T-45`:** a third ulid-less test seeder survives; **see `T-46`:** the export/import half fixes code with no production caller. None of these three reopen `T-41` itself — its stated scope (stop the two live writers from inserting `ulid IS NULL`) is genuinely met. Full detail: `firestore-cutover-log.md`'s P2-9 entry; residuals in the P2-13 entry. |
| T-42 | 2 | **`done`, amended (P2-13)** | **15 of 16 items independently reconfirmed at P2-13; 1 amended.** Triaged all findings an end-of-phase review (plus three mid-phase verifiers) raised beyond the two BLOCKING defects. **P2-8:** `ensureDefaultProfile`'s double-decision — confirmed fixed. **P2-10:** "exactly one site mints" overclaim (confirmed — one call site, one dormant unreachable second formula); `updateProfile`'s dead-weight pass-through; the `ProfileGuard`/`setSelectedProfileId` nullable seam (now `{required String ulid}`); `BookmarkRepositoryNotReadyException`'s incomplete causes — all confirmed. **P2-11:** check 104's dedup fail-open (occurrence counts now ratchet, independently re-proved by three fresh probes); the mislabeled OK line; `_patternListHash`'s narrow-scanner blind spot; the silent unreadable-file swallow — all confirmed. **P2-12:** D9's criterion; `repository_providers.dart`'s reason; the P2-1 false-verification claim; the four gate-block parentheticals; P2-3's blast-radius deviation; commit `4877c7ef`'s message pointer; the "P2-3 silently fixed a bug" rejection — all independently re-derived and confirmed at P2-13, not merely trusted. **AMENDED (the one item that does not hold): the offline-first non-fatal contract (P2-8's fix 4).** The structural half landed (`profile_repository_impl.dart:749-774`'s provider read is genuinely back inside the `try`), but the method's own last statement (`:775`) is still outside it, and P2-8's own new test proving this fix is RED — a 2-minute timeout, not a pass. **Tracked as `T-43`, not reopened here** — see that row and `firestore-cutover-log.md`'s **P2-13** entry, KNOWN ISSUES row 8. |
| T-43 | 2 | **`done` (P2-14), independently re-verified (P2-16)** | **FIXED — two independent defects, both closed.** (1) The residual escape: `_ensureFirestoreProfile`'s `activeProfileDocIdProvider` set moved inside the outer `try`, with an `_ref.mounted` guard on the outer `catch`'s own attempt so a genuinely disposed `Ref` never throws out. (2) The HANG itself (root cause, not previously diagnosed): Riverpod 3's default per-provider `retry` (`ProviderContainer.defaultRetry`) treats every plain `Exception` a `FutureProvider`'s build throws — sync or async — as transient (10 attempts, 200ms doubling to a 6.4s cap), and the interim state is `AsyncLoading(..., retrying: true)`, which does **not** complete `.future`'s `Completer` — only a terminal `AsyncError` does. `activeAccountFirebaseProvider` throwing `AccountNotAuthenticatedException` (a plain `Exception`) was therefore retried for the FULL backoff before `.future` ever settled; `firestoreLearnerProfileRepositoryProvider` (the direct thing `_ensureFirestoreProfile` awaits) retried **independently again** on top, since `.future`'s rejection propagates the raw error, not a retry-exempt `ProviderException`. Both providers now declare `retry: (retryCount, error) => null` — a structural account-resolution failure is not transient, and retrying it defeats the offline-first non-fatal contract every caller depends on. **Proof:** `flutter test .../profile_repository_impl_test.dart --plain-name "does not propagate out of createProfile"` now completes in `00:00` (was `02:00` timeout), `+1` pass. **CORRECTED (P2-16) — the "every sibling provider shares the same risk" claim was a false production statement.** The app's ONLY `ProviderContainer` (`lib/app/bootstrap/bootstrap.dart:68-81`, confirmed the sole construction site by `grep -rn "ProviderContainer(" lib/`) already declares `retry: (_, __) => null` container-wide, disabling Riverpod's default auto-retry for every provider in a running app — so the other 12 providers in `repository_providers.dart` sharing this `await` shape carry **no live production risk**; the per-provider declarations exist to make bare test containers (which don't go through `bootstrap()`) match production behaviour, which is genuinely needed for the test suite, just not a production gap. Full mechanism: `firestore-cutover-log.md`'s **P2-14** entry; the correction: **P2-16** entry. |
| T-44 | 2 | `todo` (new, P2-13) | **MINOR — `upsertFromSync`'s refusal (`T-41`) relocates the second-identity outcome instead of preventing it.** A sync-pulled profile with no local row is refused (`ProfileSyncMissingUlidException`), caught per-row, logged at warning only. On a device with no other profile, `AutoSelectedProfileId`'s zero-profile self-heal then calls `ensureDefaultProfile` → `_resolveProfileUlid(null)` → `DocIds.mintProfileUlid()` — a brand-new identity for what may be the same logical profile the sync pull just refused. This row's own text ("minting one here would hand the profile a second identity") currently reads as if the refusal *prevents* that outcome; it does not — it moves where the second mint happens. Needs a product/design decision (surface the refusal to the user? block the self-heal when a refusal was just logged for the same account?), not a mechanical fix. Full evidence: `firestore-cutover-log.md`'s **P2-13** entry. |
| T-45 | 2 | **`done` (P2-19)** | **FIXED — both ulid-less test seeders now mint a distinct `ulid` per profile.** `test/helpers/test_database.dart`'s `seedProfileWithIds` (14 dependants) now inserts `ulid: Value('ulid-seed-profile-$profileId')` — derived from `profileId`, not a shared literal, so a test seeding more than one profile (e.g. `stage_definition_repository_impl_26_26_test.dart`, profile ids 1 and 2) still gets distinct ulids per profile; mirrors `seedProfile`/`seedProfileZero` (`drift_memory.dart`)'s existing fixed-literal pattern (`T-41`, P2-9). `test/features/profiles/profile_picker_deep_l1_test.dart`'s second, independent inline `LearnerProfilesCompanion.insert(...)` seeder (8 call sites across `D2`/`F1`/`F2`/`F3`/`F4`) now carries a distinct `ulid: const Value('ulid-picker-<name>-<n>')` per occurrence. **Proof:** all 14 `seedProfileWithIds` dependants re-run as directories, zero regressions — `test/app/` +92, `test/core/navigation/` +74, `test/features/gamification/` +447, `test/features/stages/` +19 (the last specifically includes the profileId-1/profileId-2 cross-profile-isolation test, confirming distinct per-profile ulids did not collide). Closes the seeder half of `T-47`'s 6 red tests. Full evidence: `firestore-cutover-log.md`'s **P2-19** entry (written retroactively by P2-20 — see that entry's own process note for why). |
| T-46 | 2 | `todo` (new, P2-13) | **MINOR, informational — `T-41`'s export/import fix has no production caller.** `grep -rn 'DataExportImportService' lib/` outside its own file returns only doc-comment mentions; `exportData(`/`importData(` are never constructed in `lib/`. Correct hygiene, but it closes zero runtime risk today. Record-only — either wire the service to a real caller, or note in `T-41`'s row (done, above) that this half is dormant. |
| T-47 | 2 | **`done` (P2-19) — all 6 inherited red tests closed, none retired** | **FIXED — closed alongside `T-45`'s seeder fix.** The `T-43` test (one of the original set) went GREEN earlier — see `T-43`, above. The remaining 6 — all the same `ProfileModel.fromDriftRow: profile id … has no ulid — pre-P2-2 profile row with no ULID — wipe and reseed the device` `StateError` inherited from `feefe34b` (P2-3) — are now ALL green, 4 by `T-45`'s seeder fix alone (assertions unchanged) and 2 RESTATED because P2-3's own enforcement made their original observation point unreachable: (1) `AUD-profiles-02 — TutorWriteException from pushLearnerProfile propagates :: updateProfile propagates TutorWriteException instead of swallowing it` — fixed, seeder gave the inline row a real ulid (incidental setup, not the point under test). (2) `AUD-profiles-16 — log-less catch: cloud push failures now log :: updateProfile still succeeds offline-first AND logs the cloud push failure via AppLogger` — fixed, same mechanism as (1). (3) `ensureDefaultProfile fast path (account already has a profile) does NOT touch that profile's missing ulid` — **RESTATED, not force-greened:** now reads the raw Drift row via `localDb.profileDao.getProfileById` (the layer that still tolerates a null `ulid`) instead of the throwing domain read `adapter.getProfileById`, observing the same fact the test always meant to check. (4) `updateProfile does NOT backfill a missing ulid for a pre-P2-2 profile` — **RESTATED, the one test whose ORIGINAL assertion became FALSE:** it used to assert `updateProfile` succeeds with the ulid left null; that target no longer exists under P2-3's enforcement (`updateProfile` unconditionally rebuilds via `fromDriftRow`), so it is renamed `updateProfile hard-throws for a pre-P2-2 profile with a missing ulid, instead of silently backfilling or succeeding without one (P2-3 enforcement)` and now asserts the throw — the correct behaviour under R3's own greenfield ruling, not a new one; the local Drift `UPDATE` is recorded (not asserted as a virtue) as landing before the throw fires. (5) `profile_edit_delete_actions_test.dart`'s `AUD-profiles-02 — editProfileFlow surfaces tutor-routed push failures :: a failed tutor-routed pushLearnerProfile shows the tutorPermissionDenied snackbar instead of being silently swallowed` — fixed, seeder fix alone; this is the exact test P2-10's own report cited as the reason `ProfileRepositoryImpl` must keep `implements ProfileRepository` in full, and **that design justification now rests on a test that actually passes.** (6) `profile_picker_deep_l1_test.dart`'s `F: Delete flow F4` — fixed, the independent inline-seeder fix alone; the previously-reported secondary `Expected: <2> / Actual: <1>` was a symptom of the same `StateError`, resolved together. **Proof:** `flutter test test/features/profiles/` → `00:12 +425: All tests passed!` (was `+418 -6`); `test/features/profiles/data/repositories/profile_repository_impl_test.dart` alone → `00:00 +41: All tests passed!` (was `+37 -4`); each of the 6 re-confirmed individually by name, green, independently re-run at **P2-20**. Full evidence, including which two were RESTATED and why: `firestore-cutover-log.md`'s **P2-19** entry (written retroactively by P2-20). |
| T-48 | 2 | **`done` (P2-15)** | **FIXED — the read that could be wrong is deleted, not narrowed.** `ensureProfile` no longer calls `ref.get()` at all to decide `created_at`; it takes `createdAt` as a caller-supplied `required DateTime`, sourced from the Drift row's own immutable local creation timestamp (`ProfileDao.upsertFromSync`'s doc comment: "accountId/createdAt are left untouched" by any update), and always writes it via `SetOptions(merge: true)`. This can never be wrong regardless of Firestore's cache state — real, offline, or otherwise — because the value written no longer depends on anything Firestore reports back. `FirestoreProfileRepositoryAdapter._ensureFirestoreProfile` now passes `createdAt: model.createdAt`. **Proof:** a new test in `firestore_learner_profile_repository_test.dart` seeds the raw document with a DIFFERENT `created_at` than the caller supplies and confirms the write uses the caller's value regardless — the real, fake-detectable proof, since `fake_cloud_firestore` has no cache/offline semantics and cannot reproduce the cache-miss scenario itself (recorded as a DEFERRED VERIFICATION, `D18`, unchanged — still needs a device or an offline-cache integration test to see the actual cache-miss trigger, though the fix no longer depends on that trigger being absent to be correct). File: `firestore_learner_profile_repository_test.dart` → `+17` (was `+16`), all green. `profile_repository_impl_test.dart` (the call site's own test file) unaffected: `+37 -4`, same 4 pre-existing P2-3 `StateError` failures as every prior measurement. Full evidence: `firestore-cutover-log.md`'s **P2-15** entry. |
| T-49 | 2 | **`done` (P2-23) — closed for all three `_ensureFirestoreProfile` callers, not just one** | **History: P2-18's `done` was WRONG — it closed only ONE of `_ensureFirestoreProfile`'s three callers**, gave the method a `required bool activateProvider` parameter, and made `ensureRemoteProfile` (the fire-and-forget activation-heal dispatch from `SelectedProfileId.select()`) pass `false` — that part was real and stayed fixed. But `createProfile`/`ensureDefaultProfile` kept passing `activateProvider: true`, writing `activeProfileDocIdProvider` unconditionally AFTER `await firestoreRepo.ensureProfile(...)` — a real `DocumentReference.set()` whose `Future` does not complete while offline. P2-18's own justification for leaving these two — "direct, awaited calls with no later selection to race" — was FALSE: an `await` inside one call does not stop a DIFFERENT profile being selected elsewhere during the await window (`onboarding_profile_creation_step.dart:133`'s own comment already conceded this). P2-22 REPRODUCED the clobber BY EXECUTION with a throwaway probe (`Expected: 'ulid-probe-profile-b' / Actual: 'ulid-probe-profile-c'`), then deleted it (docs-only charter), and identified the fix without applying it. **P2-23 verified the reviewer's claim first (re-read the code, re-derived the mechanism), then applied the fix and made the probe permanent.** `FirestoreProfileRepositoryAdapter` now has THREE private methods instead of one method plus a boolean: `_resolveFirestoreProfileRepo` (resolves the Firestore repo once, non-fatal on a resolution failure), `_writeFirestoreProfile` (the write alone, non-fatal on a write failure), `_ensureFirestoreProfile` (write only — no activation, ever; `ensureRemoteProfile`'s only path) and `_activateThenEnsureFirestoreProfile` (activates `activeProfileDocIdProvider` to the profile's ulid the moment a cloud account is confirmed active — BEFORE calling the write — then writes; `createProfile`'s and `ensureDefaultProfile`'s self-heal branch's only path). The `activateProvider` boolean is DELETED — there is no longer a flag to pass wrong, because activation and the write are two different methods reached by two different callers. Activating before the write closes the race: by the time anything else could select a different profile, this profile's activation has already happened exactly once and is never repeated, so a later `select()` always wins. The "not ready (no active account)" contract is preserved — `_activateThenEnsureFirestoreProfile` still resolves the Firestore repo first and returns without activating when it is `null`, matching `profile_repository_impl_test.dart`'s existing "leaves activeProfileDocIdProvider unset" tests, which stayed green with no edit. **Proof — the probe made permanent, covering all three callers, not a throwaway:** new file `test/features/profiles/data/repositories/profile_repository_impl_t49_activation_ordering_test.dart` — reproduced RED on the unfixed tree first (`createProfile`: `Expected: 'ulid-p223-profile-b' / Actual: 'ulid-p223-profile-c'`; `ensureDefaultProfile`: same shape, `-b2`/`-d`; `ensureRemoteProfile`: already green, P2-18's fix untouched), GREEN after the fix (`+3`), then proved real by a byte-exact `cp` backup (never `git stash`) reverting the fix — RED again, identical failures — then restored, md5-verified byte-identical to the fixed file (`77cc1b295867180878b47044b160ecb3`). `flutter test test/features/profiles/` → `+428: All tests passed!` (425 baseline + 3 new, 0 failures). `dart analyze --fatal-infos`, both keying gates, and `make audit` (104/104) all green on the fixed tree. `make test` (full suite): see `firestore-cutover-log.md`'s **P2-23** entry for the verbatim number. **The false code comments this reopening depended on are now corrected IN CODE, in this same commit**, not only disclosed: `profile_repository_impl.dart`'s class doc comment, `_ensureFirestoreProfile`'s, `_activateThenEnsureFirestoreProfile`'s (new) and `ensureRemoteProfile`'s all state the TRUE mechanism — no surviving "no later selection to race" claim anywhere in `lib/`. Full evidence: `firestore-cutover-log.md`'s **P2-18** entry (the original, incomplete fix, left unedited — append-only), **P2-22** entry (the reopening, the probe, the reproduction, left unedited), and **P2-23** entry (this fix, the permanent test, the revert-proof). |
| T-50 | 2 | **`done` (P2-20)** | **FIXED IN CODE — the doc comment P2-16 corrected only in the three planning `.md` files, and P2-17 found still false in `lib/` but could not fix (docs-only charter), is now corrected in `lib/` itself.** `lib/data/firestore/repository_providers.dart:204-211`'s comment ("Every other provider in this file shares the identical … shape and therefore the same latent risk") now states what P2-16/P2-17 established: `bootstrap.dart:68-81` constructs the app's only `ProviderContainer` (re-verified: `grep -rn "ProviderContainer(" lib/` → exactly one construction site) with a container-level `retry: (_, __) => null`, already disabling Riverpod's default auto-retry for every provider before `main.dart:22`'s root `UncontrolledProviderScope` mounts it — so the other 12 providers carry no live production risk from this shape; the per-provider `retry:` declarations exist for bare-test-container parity, not to close a production gap. **Precision note added over the prior framing:** the second `UncontrolledProviderScope` in `lib/` (`account_actions.dart:355`) re-parents the SAME container via `ProviderScope.containerOf`, not a second one — the comment states this precisely rather than the looser "exactly one `UncontrolledProviderScope`" framing, which would itself have been slightly inaccurate. `dart analyze --fatal-infos` → `No issues found!`; `dart format` → `0 changed`; no behaviour change, no test affected. **This is the project's own named "stale doc comment" failure mode reproduced TWICE inside the remediation chartered to eliminate it (P2-16 missed it; P2-17 found and disclosed it but, being docs-only, could not close it) before finally being fixed in code at P2-20** — recorded plainly, not softened. Full evidence: `firestore-cutover-log.md`'s **P2-20** entry. |
| T-51 | 2 | **`done` — CARRIED-BY-RULING (P2-20)** | **RESOLVED by an explicit owner ruling, no code change.** The v38 schema migration materialises `ulid IS NULL` on every in-place app upgrade from v26..v37, with no backfill, and P2-3's `fromDriftRow` hard-throws on it — `lib/core/database/user/user_database.dart:795` (`if (from < 38) { … m.addColumn(learnerProfiles, learnerProfiles.ulid) … }`) adds the column as NULL for any existing `learner_profiles` row on an in-place upgrade; nothing in `lib/` backfills it. `ProfileModel.fromDriftRow` (P2-3, `feefe34b`) then throws `StateError: "... has no ulid — pre-P2-2 profile row with no ULID — wipe and reseed the device"`. This was already accurately recorded at P2-17, including the exact distinction that matters — the frozen plan's R3 (`firestore-phase2-plan.md:301`) rules a legacy row's crash acceptable under greenfield, but that ruling was written about pre-existing seeded **dev** data, and the live producer of this shape is instead an app **upgrade**, i.e. every existing install crossing v37→v38, a materially different population. P2-17 could disclose that distinction but not rule on it (docs-only charter). **The owner's 2026-08-07 ruling ("GREENFIELD. No live users, no data worth preserving … never write backfills … Correctness is NOT relaxed") answers it directly: there is no live population of either kind to protect, so the wipe-and-reseed remedy already accepted for a legacy row is confirmed to cover the app-upgrade population too, without extension.** No code change — greenfield stays in force (no backfill/dual-read/dual-write bridge); the crash-with-a-named-remedy is already the correct behaviour, now confirmed rather than merely defaulted-to. Deferred verification `D21` stays open, downgraded from blocking (pending a ruling) to a non-blocking regression/confirmation device check (nobody has run the actual v37→v38 device upgrade; still genuinely open). Full evidence: `firestore-cutover-log.md`'s **P2-20** entry. |
| T-52 | 2 | **`done` (P2-17)** | **MINOR — `make audit` named two different gates depending on the working directory, and no Phase 2 record stated which one was meant.** `learning_tracker/Makefile:1378`'s `audit` (104 checks) is what every gate block in this log and the phase plan means. The repo-root `/home/daniel/repos/learning-tracker/Makefile:112` also defines an `audit` target (12 enforcement greps) and it **fails today** — `10 non-baselined empty/comment-only catch block(s) found` → exit 2 — pre-existing (Makefile dated Jul 21, before this phase's first commit), unrelated to Phase 2. **FIXED, this commit (docs-only, no ambiguity resolvable in code):** `firestore-cutover-log.md`'s Recovery Protocol step 4 now states the directory explicitly and names the trap; `firestore-cutover-plan.md`'s verification-cadence paragraph does the same. Full evidence: `firestore-cutover-log.md`'s **P2-17** entry. |
| T-53 | 2 | **`done` (P2-21)** | **FIXED — 14 `test/e2e/journeys/**` CI failures, seven ulid-less `LearnerProfilesCompanion.insert(...)` seeders, the `T-45`/`T-47` class's latest recurrence.** `test/e2e/harness/e2e_harness.dart`'s `_seedIdentity` (the shared identity every `E2EHarness` test seeds) inserted a real Drift `learner_profiles` row with no `ulid:` — hard-thrown on by P2-3's `ProfileModel.fromDriftRow` the moment any FUTURE-based read (`ProfileRepositoryImpl.getProfilesByAccount`, `.updateProfile`, etc, reached via `profileListProvider`) touched it. Fixing the harness alone closed 8 of the 14; re-running `test/e2e/journeys/` as a directory net (never done by any prior round) surfaced 6 more, each from its own file's **second, independent** ulid-less seeder: `profiles_p0_test.dart` and `profiles_p1_test.dart`'s identical `_seedSecondProfile` helpers, and inline seeders in `profiles_tutoring_p2_test.dart` (Bob), `progress_p1_test.dart` (`otherProfileId`), `tutoring_p1_test.dart` (ChildForRescind) and `tutoring_p0_test.dart` (ChildForRevoke). All seven now mint `ulid: Value('ulid-$id')` after insert (insert-then-update — the id is Drift-autoincrement, unknowable inside the `.insert()` call itself). An eighth, non-failing site (`tutoring_p0_test.dart`'s `ChildToTutor`, backing E2E-1001) was fixed for consistency in the same file, disclosed as preventive, not a defect closure. **Proof:** all 14 originally-red tests individually confirmed green by `--plain-name`; `flutter test test/e2e/journeys/` → `00:35 +281 ~84: All tests passed!` (was `+275 ~84 -6` after the harness-only fix, `-14` before any fix); full-suite `flutter test --coverage --exclude-tags "serial-tools \|\| quarantine" --test-randomize-ordering-seed=random` → `08:57 +11511 ~131: All tests passed!` (was `+11497 ~131 -14`). `test/` only, 7 files, no `lib/` change. Full evidence: `firestore-cutover-log.md`'s **P2-21** entry. |
| T-54 | 2 | **`done` (P2-21)** | **FIXED — `functions/test/firestore_rules.test.mjs`'s `learning_order` describe block asserted a behaviour P2-6 deliberately changed.** P2-6 (`2e85b097`, T-33) changed `firestore.rules:456` from `allow delete: if false` to `allow delete: if isOwner(uid)` — correct, intended, unchanged since. The test's `learning_order` describe block (title still literally "delete denied") called the shared `expectOwnerWriteTutorRead(path, validOrder)` helper with no options, so its default `ownerCanDelete = false` asserted the owner's own `deleteDoc` **fails** — now false. The sibling `profile_programs`/`curriculum_scopes`/`study_day_configs` blocks already had owner-delete before Phase 2 and correctly pass `{ ownerCanDelete: true }` with a "(owner can delete)" test-name suffix — `learning_order`'s block now matches that pattern exactly (title, test name, and the `{ ownerCanDelete: true }` option). Three lines, one file — no `firestore.rules` change (the rule was already correct). **This also unblocked TQ-9** (`functions/tool/check_rule_coverage.mjs`, `Makefile:65`'s `&&` chain), which had not run even once since P2-6 because the stale test's failure short-circuited it every time `make test-rules` was invoked. **Proof:** `make test-rules` → `tests 116, pass 116, fail 0` (was `pass 115, fail 1`), then `TQ-9: rule coverage OK — all 37 conditional allow rule(s) in firestore.rules were evaluated at least once` (previously unreached). Full evidence: `firestore-cutover-log.md`'s **P2-21** entry. |
| T-55 | 2 | `todo` (new, P2-21) | **MINOR, informational — the ulid-less-seeder class (`T-45`/`T-47`/`T-53`) is far larger than the 9 known, fixed instances.** `grep -rln "LearnerProfilesCompanion.insert\|LearnerProfilesCompanion(" test/ \| xargs grep -L "ulid"` finds roughly 60 further files across `test/core/database/`, `test/story_acceptance/`, `test/integration/`, `test/sync/`, `test/features/**`, `test/app/` and `test/e2e/journeys/sync_p1_test.dart` (which builds its own separate in-memory `UserDatabase` outside `E2EHarness`) with the identical shape. None currently causes a test failure — confirmed by this round's own clean full-suite run (`+11511 ~131`, 0 failures) — implying each is either read only at the raw-Drift-row layer or otherwise never reaches P2-3's `fromDriftRow` enforcement. Not fixed this round (scope creep relative to the two named CI failures, on files not currently exercising the defect). Needs a decision: fix preventively, or wait for each to fail on its own the way `T-53`'s predecessors did — possibly worth a purpose-built lint/gate, mirroring how the `T-45` class eventually earned check 104's coverage for its production counterpart. Full evidence, including the grep output's file list: `firestore-cutover-log.md`'s **P2-21** entry. |
| T-56 | 2 | `todo` (new, P2-22) | **MINOR — a second, unguarded post-await write to `activeProfileDocIdProvider` in `AutoSelectedProfileId._resolveSelection`.** `lib/features/profiles/presentation/providers/profile_providers.dart:226-231`: `final existingProfile = await repo.getProfileById(current); if (existingProfile != null) { ref.read(activeProfileDocIdProvider.notifier).set(existingProfile.ulid); return current; }` — no check that `selectedProfileIdProvider` is still `current` after the await. The sibling branch 43 lines below, at `:271-274`, in the SAME method, already carries exactly that guard, with a comment explaining why: "Re-check after the await: the picker / sign-in flow may have selected a profile while we were fetching. Don't clobber an explicit choice." `ensureSelected()` is dispatched from `app_shell.dart:156` on an auth transition, concurrently with the route guard and the picker. Smaller race window than `T-49` (a local Drift read, not a Firestore write) but the identical shape — trivially closable by adding the same guard. Confirmed by direct code read; CURRENT STATE previously (falsely) described this exact branch as the ONE non-`select()` write and framed it as benign — corrected at P2-22. Full evidence: `firestore-cutover-log.md`'s **P2-22** entry. |
| T-57 | 2 | `todo` (new, P2-22) | **MINOR — creating an ADULT profile deterministically (not racily) mis-keys all 13 profile-scoped Firestore providers.** `add_profile_dialog.dart:258` awaits `repo.createProfile(...)`, which unconditionally sets `activeProfileDocIdProvider` to the new profile's ULID (`profile_repository_impl.dart:865`, `activateProvider: true`). The dialog then calls `select()` only at `:264`, `if (created.profileMode.isChild && context.mounted)`. For an ADULT profile nothing resets it, so `selectedProfileIdProvider` stays on the currently-active profile while `_watchActiveAccountAndProfile` (`repository_providers.dart:161-170`) re-keys every profile-scoped provider — bookmarks and learning_order included — to the new profile. Reachable from `manage_learners_screen.dart:68`, `profile_switcher_sheet.dart:257`, `profile_picker_screen.dart:232`. Introduced by `951d6187` ("feat(profiles): mint a Firestore profile on creation and activate its ULID"), confirmed an ancestor of `d74e3829` — pre-existing, predates this cutover, unrecorded until P2-22. Confirmed by direct code read. Full evidence: `firestore-cutover-log.md`'s **P2-22** entry. |
| T-58 | 5 | `todo` (new, P2-22) | **MINOR, informational — `test/tool/audit_and_arb_parity_test.dart :: 'prints file:line paths for violations'` is CONFIRMED RED in the `make test-serial-tools` lane.** REPRODUCED BY EXECUTION of the test's own filter (`line.contains('.dart:') && !line.startsWith('[') && line.isNotEmpty`, then `RegExp(r'\.dart:\d+:')`) against a freshly-run `make audit`'s stdout: `602` hitLines, `209` fail the regex. Most of the 209 are `lint-rules-test`'s own `dart test` progress output (which `make audit` shells out to), not only check 103's advisory WATCHLIST prose as previously (incompletely) attributed. **Not Phase-2-attributable** — `tool/check_profile_path_keying.dart` (the WATCHLIST source) was never touched by Phase 2, and the WATCHLIST format predates this phase's first commit. Previously undertracked: `T-38` (above) covers a DIFFERENT, `skip:`-disabled test in the same file; this one had no row at all until P2-22. Belongs with `T-38`'s Phase-5 gate-retarget work. Full evidence: `firestore-cutover-log.md`'s **P2-22** entry. |
| T-20 | 3 | todo | **Wire the 7 dead adapters and move ~96 feature files.** Built-but-never-constructed: `FirestoreCompletionRepositoryAdapter`, `FirestoreCurriculumTrackRepositoryAdapter`, `FirestoreGoalRepositoryAdapter`, `FirestoreProgressRepositoryAdapter`, `FirestoreStageDefinitionRepositoryAdapter`, `FirestoreStudyDayConfigRepositoryAdapter`, `FirestoreTrackLearningOrderRepositoryAdapter`. Order by data dependency — **writers before readers** — and add a writer/reader agreement test per collection. Prerequisite: T-39. |
| T-32 | 3 | decided | **Reorder amnesty is no longer stamped on any path.** Owner decision D2 (2026-08-04): restore both forgiveness paths. **The two are not equal cost.** Reorder stamp is cheap (`last_reorder_at` already permitted, `firestore.rules:412`); write it and move `daily_task_projection_service`'s read (`:443-446`) off Drift. **Content-reseed forgiveness needs a NEW mechanism** — the old detection used the Drift `learning_order.learningOrderVersion` column and the `learning_order` rules whitelist has no version field. Design explicitly; do not assume it comes free. |
| T-21 | 4 | todo | **Demolish the sync engine and Drift user database.** `lib/core/sync` (62 files, 12,819 lines), `lib/core/database/user` (2, 25,774), `daos` (49, 5,991), `tables` (26, 1,111), `views` (1, 34), plus 85 sync test files. ≈45,700 lines. |
| T-29 | 4 | todo | **ISO→Timestamp conversion dies with `core/sync`.** `firestore.rules` enforces `is timestamp` on `streak_events.created_at`, `completions`/`learning_ledger.completed_at`, `points_ledger.created_at`. `FirestoreGatewayImpl._timestampifyField` did that conversion and is deleted here. Verified 2026-08-03: the new repositories handle all three correctly (completions and points_ledger round-trip real `Timestamp`s; streak_events omits the key so the optional-field guard short-circuits). **Re-verify before deleting.** `fake_cloud_firestore` cannot catch a regression — it surfaces only as permission-denied on a real device. |
| T-36 | 4 | todo | **Remove Rule 5 allow-list entries as PAIRS.** `epic_25_story_25_9_lints_test.dart` exempts `firestore_learning_order_repository.dart` and `firestore_track_learning_order_repository.dart` alongside the Drift repositories they replace. Delete both halves together so the list shrinks rather than accumulating exemptions for files that no longer exist. |
| T-23 | 5 | todo | **Retarget enforcement gates.** Many encode the old sync engine's invariants and will be policing deleted code. |
| T-24 | 5 | todo | **Verify `resolve()` cold-start re-attach on a real device.** Mock-only today; runs every launch. **Corrected at P2-13** — the row's prior text (`ProfileGuard`'s single-profile auto-select calling `.select(id)` without `ulid:`, clearing `activeProfileDocIdProvider`) has been false since P2-2 and doubly false since P2-10: the parameter is `{required String ulid}` now (`router_provider.dart:65`, `profile_guard.dart:167`), and a legacy null-`ulid` row fails OPEN rather than silently clearing anything (`profile_guard.dart`'s `_resolve`). What actually still needs a real device: confirming `T-40`'s activation-heal listener (see that row) fires on a genuine cold start once its wiring is fixed — this task and `T-40`/`D15` cover overlapping ground and should be executed together, not separately, once `T-40` lands. |
| T-25 | 5 | todo | **Add a gate rejecting non-text source files.** One NUL byte makes grep treat a file as binary, silently disabling all 103 audit checks on it. Has happened once. |

## Done

| ID | Phase | Commit | Task |
|----|-------|--------|------|
| T-01 | — | — | Baseline gate: `make audit` + `make ci` green on untouched `dev` |
| T-16 | — | — | A1 — named-app auth: each account owns its own authenticated `FirebaseApp` |
| T-19 | — | — | B2 — build repositories for the whole data model |
| T-26 | — | — | `track_learning_order` repository |
| T-27 | — | `58b2e396` | Onboarding switches to the child profile before add-track |
| T-28 | — | `5b4d7924` | Bookmarks vertical slice — end-to-end onto Firestore |
| T-P0 | 0 | `9a5cb97c` | Phase 0 — three blocking decisions resolved (D1/D2/D3) |
| T-P1 | 1 | `a2a21d0a` | Phase 1 — audit check 103, keying gate + agreement helper |
| T-33 | 2 | `2e85b097` | `learning_order` owner delete + real `resetToDefault` (rules + code, one commit, per D3) |
| T-34 | 2 | `b398bea5` | Divergent bookmark writer — **resolved by deletion, not reconciliation.** The task's stated premise (reconcile the doc-id formula with T-31) was incomplete: `TutoredWriteRouter.pushBookmark` was reachable only from `BookmarkRepositoryImpl`, which nothing in `lib/` constructs — the live provider builds `FirestoreBookmarkRepositoryAdapter`, which refuses tutored bookmark writes outright. Its "mirror firestore_gateway_impl" doc-id comment described a formula that file no longer implements for bookmarks (emits `curriculum_id` bare), so the divergent writer produced a trailing-underscore id matching nobody, on a path nothing could reach. Deleted; kept as a pass-through override (Dart `implements` conformance required a body). |
| T-35 | 2 | `30790fef` | Tutored guard hoisted into `_watchActiveAccountAndProfile` — all 13 profile-scoped providers now refuse uniformly during a tutored session, closing a live latent corruption (12 of 13 previously served the tutor's own account+profile). **Known, deliberate regression carried into Phase 3:** the talmid's scheduler does NOT go empty as originally recorded — see T-42 / the log's P2-7 entry, SERIOUS DEFECT 1 for the corrected observable. |

---

## Conventions

- **One row per unit of work**, with enough file:line evidence to act without
  re-deriving anything. A task nobody can execute cold is not recorded.
- **Update in the same commit as the work.** A stale task list is worse than
  none — this project has already lost a live feature to a stale doc comment.
- **`decided` ≠ `done`.** The owner has ruled; the code has not moved.
- Verify a file:line before trusting it. Citations here age like any other.
