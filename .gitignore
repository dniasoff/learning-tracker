.runtime/
.claude/*
!.claude/skills/
.logs/
.beads/
.linear-cache/

# Gas Town (added by gt)
__pycache__/
state.json

# Gas Town (added by gt)
.claude/

# Firebase
firebase-debug.log

# Dart/Flutter build artifacts (e.g. packages/custom_lints/.dart_tool/)
.dart_tool/

# Firebase emulator debug logs
**/firestore-debug.log

# Repo-root build scratch space (AUD-repo-04). learning_tracker/ has its own
# .gitignore covering learning_tracker/build/ (Flutter + `make seed` output);
# this covers the repo root in case a tool script is ever run from there.
/build/
/*.log

# Serena MCP local index
.serena/
**/.serena/

# Generated Drift files inside the archived (non-building) snapshot
docs/_archive/**/*.g.dart
.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml
_bmad/render/

# orvex-installer: _bmad is generated; only configuration is tracked
/_bmad/**
!/_bmad/
!/_bmad/*/
!/_bmad/config.toml
!/_bmad/config.user.toml
!/_bmad/*/config.yaml
!/_bmad/custom/
!/_bmad/custom/*.toml
