<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-variant only (android/app/src/debug/): permits cleartext (non-TLS)
  HTTP to the Android emulator's host-loopback alias so on-device
  integration tests (e.g. Story 2.1's Firestore/Auth-emulator smoke test,
  integration_test/firestore_multi_app_isolation_test.dart) can reach
  `firebase emulators:start` running on the host at 10.0.2.2. Without this,
  Android API 28+ blocks cleartext traffic by default and every emulator
  call fails with "Cleartext HTTP traffic to 10.0.2.2 not permitted".

  Scoped to src/debug/ only — release/profile builds are untouched, so
  production's cleartext-blocked posture is unaffected.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
