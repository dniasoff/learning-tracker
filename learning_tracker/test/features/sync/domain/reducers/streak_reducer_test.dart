import 'package:flutter_test/flutter_test.dart';

void main() {
  // TODO(DNI-337): fill in once df0aa11f merges.
  //
  // The full reducer test suite was authored against the DNI-337 branch
  // (`core/streak/` location). On the current `dev` tip the reducer
  // still lives at `lib/features/sync/domain/reducers/streak_reducer.dart`;
  // the batch-4 cherry-pick moves it. To avoid coupling this file to a
  // moving target, the real tests are activated when DNI-337 lands.
  test('StreakReducer coverage placeholder', () {}, skip: 'DNI-337 pending');
}
