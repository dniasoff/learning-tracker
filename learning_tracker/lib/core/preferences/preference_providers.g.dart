// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'preference_providers.dart';

// **************************************************************************
// RiverpodGenerator
// **************************************************************************

// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint, type=warning
/// Long-lived singleton holding the [HebrewTermsPreference] object. Riverpod
/// keeps it alive so the underlying broadcast stream survives route changes.
// keepAlive: profile-scoped store; lives for the app session.

@ProviderFor(hebrewTermsPreference)
final hebrewTermsPreferenceProvider = HebrewTermsPreferenceProvider._();

/// Long-lived singleton holding the [HebrewTermsPreference] object. Riverpod
/// keeps it alive so the underlying broadcast stream survives route changes.
// keepAlive: profile-scoped store; lives for the app session.

final class HebrewTermsPreferenceProvider
    extends
        $FunctionalProvider<
          HebrewTermsPreference,
          HebrewTermsPreference,
          HebrewTermsPreference
        >
    with $Provider<HebrewTermsPreference> {
  /// Long-lived singleton holding the [HebrewTermsPreference] object. Riverpod
  /// keeps it alive so the underlying broadcast stream survives route changes.
  // keepAlive: profile-scoped store; lives for the app session.
  HebrewTermsPreferenceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'hebrewTermsPreferenceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$hebrewTermsPreferenceHash();

  @$internal
  @override
  $ProviderElement<HebrewTermsPreference> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  HebrewTermsPreference create(Ref ref) {
    return hebrewTermsPreference(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(HebrewTermsPreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<HebrewTermsPreference>(value),
    );
  }
}

String _$hebrewTermsPreferenceHash() =>
    r'd3825499beb19397702c4f1ec5974c2ca4aa1cc9';

@ProviderFor(hebrewDatePreference)
final hebrewDatePreferenceProvider = HebrewDatePreferenceProvider._();

final class HebrewDatePreferenceProvider
    extends
        $FunctionalProvider<
          HebrewDatePreference,
          HebrewDatePreference,
          HebrewDatePreference
        >
    with $Provider<HebrewDatePreference> {
  HebrewDatePreferenceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'hebrewDatePreferenceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$hebrewDatePreferenceHash();

  @$internal
  @override
  $ProviderElement<HebrewDatePreference> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  HebrewDatePreference create(Ref ref) {
    return hebrewDatePreference(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(HebrewDatePreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<HebrewDatePreference>(value),
    );
  }
}

String _$hebrewDatePreferenceHash() =>
    r'f2cee5454bc21a24d2ef3e90ac4a694ef44b947d';

@ProviderFor(nikudPreference)
final nikudPreferenceProvider = NikudPreferenceProvider._();

final class NikudPreferenceProvider
    extends
        $FunctionalProvider<NikudPreference, NikudPreference, NikudPreference>
    with $Provider<NikudPreference> {
  NikudPreferenceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'nikudPreferenceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$nikudPreferenceHash();

  @$internal
  @override
  $ProviderElement<NikudPreference> $createElement($ProviderPointer pointer) =>
      $ProviderElement(pointer);

  @override
  NikudPreference create(Ref ref) {
    return nikudPreference(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(NikudPreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<NikudPreference>(value),
    );
  }
}

String _$nikudPreferenceHash() => r'ec1c52fbf76a60d57c72a01133b73dfbe2f0a920';

@ProviderFor(transliterationVariantPreference)
final transliterationVariantPreferenceProvider =
    TransliterationVariantPreferenceProvider._();

final class TransliterationVariantPreferenceProvider
    extends
        $FunctionalProvider<
          TransliterationVariantPreference,
          TransliterationVariantPreference,
          TransliterationVariantPreference
        >
    with $Provider<TransliterationVariantPreference> {
  TransliterationVariantPreferenceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'transliterationVariantPreferenceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$transliterationVariantPreferenceHash();

  @$internal
  @override
  $ProviderElement<TransliterationVariantPreference> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  TransliterationVariantPreference create(Ref ref) {
    return transliterationVariantPreference(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(TransliterationVariantPreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<TransliterationVariantPreference>(
        value,
      ),
    );
  }
}

String _$transliterationVariantPreferenceHash() =>
    r'f884a196b2dd2dd2c5d7fd10c4bfbb58a5f651d7';

@ProviderFor(textDisplayPreference)
final textDisplayPreferenceProvider = TextDisplayPreferenceProvider._();

final class TextDisplayPreferenceProvider
    extends
        $FunctionalProvider<
          TextDisplayPreference,
          TextDisplayPreference,
          TextDisplayPreference
        >
    with $Provider<TextDisplayPreference> {
  TextDisplayPreferenceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'textDisplayPreferenceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$textDisplayPreferenceHash();

  @$internal
  @override
  $ProviderElement<TextDisplayPreference> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  TextDisplayPreference create(Ref ref) {
    return textDisplayPreference(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(TextDisplayPreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<TextDisplayPreference>(value),
    );
  }
}

String _$textDisplayPreferenceHash() =>
    r'edc7eb2168517732087c61fb3b867f0769032924';

/// Long-lived store for the per-(profile, curriculum) siyum-granularity
/// preference. Keyed by [CurriculumId] because the value is per-curriculum;
/// the underlying broadcast stream survives route changes so the journey and
/// settings screens stay in sync.
// keepAlive: profile-scoped store; lives for the app session.

@ProviderFor(siyumGranularityPreference)
final siyumGranularityPreferenceProvider = SiyumGranularityPreferenceFamily._();

/// Long-lived store for the per-(profile, curriculum) siyum-granularity
/// preference. Keyed by [CurriculumId] because the value is per-curriculum;
/// the underlying broadcast stream survives route changes so the journey and
/// settings screens stay in sync.
// keepAlive: profile-scoped store; lives for the app session.

final class SiyumGranularityPreferenceProvider
    extends
        $FunctionalProvider<
          SiyumGranularityPreference,
          SiyumGranularityPreference,
          SiyumGranularityPreference
        >
    with $Provider<SiyumGranularityPreference> {
  /// Long-lived store for the per-(profile, curriculum) siyum-granularity
  /// preference. Keyed by [CurriculumId] because the value is per-curriculum;
  /// the underlying broadcast stream survives route changes so the journey and
  /// settings screens stay in sync.
  // keepAlive: profile-scoped store; lives for the app session.
  SiyumGranularityPreferenceProvider._({
    required SiyumGranularityPreferenceFamily super.from,
    required CurriculumId super.argument,
  }) : super(
         retry: null,
         name: r'siyumGranularityPreferenceProvider',
         isAutoDispose: false,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$siyumGranularityPreferenceHash();

  @override
  String toString() {
    return r'siyumGranularityPreferenceProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $ProviderElement<SiyumGranularityPreference> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  SiyumGranularityPreference create(Ref ref) {
    final argument = this.argument as CurriculumId;
    return siyumGranularityPreference(ref, argument);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(SiyumGranularityPreference value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<SiyumGranularityPreference>(value),
    );
  }

  @override
  bool operator ==(Object other) {
    return other is SiyumGranularityPreferenceProvider &&
        other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$siyumGranularityPreferenceHash() =>
    r'eb98c14c8c19c78d2648f60563c2fa0477a5c7eb';

/// Long-lived store for the per-(profile, curriculum) siyum-granularity
/// preference. Keyed by [CurriculumId] because the value is per-curriculum;
/// the underlying broadcast stream survives route changes so the journey and
/// settings screens stay in sync.
// keepAlive: profile-scoped store; lives for the app session.

final class SiyumGranularityPreferenceFamily extends $Family
    with $FunctionalFamilyOverride<SiyumGranularityPreference, CurriculumId> {
  SiyumGranularityPreferenceFamily._()
    : super(
        retry: null,
        name: r'siyumGranularityPreferenceProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: false,
      );

  /// Long-lived store for the per-(profile, curriculum) siyum-granularity
  /// preference. Keyed by [CurriculumId] because the value is per-curriculum;
  /// the underlying broadcast stream survives route changes so the journey and
  /// settings screens stay in sync.
  // keepAlive: profile-scoped store; lives for the app session.

  SiyumGranularityPreferenceProvider call(CurriculumId curriculum) =>
      SiyumGranularityPreferenceProvider._(argument: curriculum, from: this);

  @override
  String toString() => r'siyumGranularityPreferenceProvider';
}

/// Whether to render Jewish learning terms in Hebrew script for the active
/// profile. This is the **only** `core/` entry point for the toggle —
/// `core/labels/` reads here, never via `features/settings/`.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.

@ProviderFor(UseHebrewTerms)
final useHebrewTermsProvider = UseHebrewTermsProvider._();

/// Whether to render Jewish learning terms in Hebrew script for the active
/// profile. This is the **only** `core/` entry point for the toggle —
/// `core/labels/` reads here, never via `features/settings/`.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.
final class UseHebrewTermsProvider
    extends $NotifierProvider<UseHebrewTerms, bool> {
  /// Whether to render Jewish learning terms in Hebrew script for the active
  /// profile. This is the **only** `core/` entry point for the toggle —
  /// `core/labels/` reads here, never via `features/settings/`.
  // keepAlive: mirrors a persisted preference; must survive widget unmounts.
  UseHebrewTermsProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'useHebrewTermsProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$useHebrewTermsHash();

  @$internal
  @override
  UseHebrewTerms create() => UseHebrewTerms();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(bool value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<bool>(value),
    );
  }
}

String _$useHebrewTermsHash() => r'75d25112eebb612ed60b064abe1e0649fc036f78';

/// Whether to render Jewish learning terms in Hebrew script for the active
/// profile. This is the **only** `core/` entry point for the toggle —
/// `core/labels/` reads here, never via `features/settings/`.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.

abstract class _$UseHebrewTerms extends $Notifier<bool> {
  bool build();
  @$mustCallSuper
  @override
  void runBuild() {
    final ref = this.ref as $Ref<bool, bool>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<bool, bool>,
              bool,
              Object?,
              Object?
            >;
    element.handleCreate(ref, build);
  }
}

@ProviderFor(UseHebrewDate)
final useHebrewDateProvider = UseHebrewDateProvider._();

final class UseHebrewDateProvider
    extends $NotifierProvider<UseHebrewDate, bool> {
  UseHebrewDateProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'useHebrewDateProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$useHebrewDateHash();

  @$internal
  @override
  UseHebrewDate create() => UseHebrewDate();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(bool value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<bool>(value),
    );
  }
}

String _$useHebrewDateHash() => r'157344ced29846975f3398d41b553c1c5567dcf5';

abstract class _$UseHebrewDate extends $Notifier<bool> {
  bool build();
  @$mustCallSuper
  @override
  void runBuild() {
    final ref = this.ref as $Ref<bool, bool>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<bool, bool>,
              bool,
              Object?,
              Object?
            >;
    element.handleCreate(ref, build);
  }
}

@ProviderFor(ShowNikudPref)
final showNikudPrefProvider = ShowNikudPrefProvider._();

final class ShowNikudPrefProvider
    extends $NotifierProvider<ShowNikudPref, bool> {
  ShowNikudPrefProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'showNikudPrefProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$showNikudPrefHash();

  @$internal
  @override
  ShowNikudPref create() => ShowNikudPref();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(bool value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<bool>(value),
    );
  }
}

String _$showNikudPrefHash() => r'e2ed1aaad5730da9f022a576f48829c3c62dddb9';

abstract class _$ShowNikudPref extends $Notifier<bool> {
  bool build();
  @$mustCallSuper
  @override
  void runBuild() {
    final ref = this.ref as $Ref<bool, bool>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<bool, bool>,
              bool,
              Object?,
              Object?
            >;
    element.handleCreate(ref, build);
  }
}

/// The UI locale the app is currently rendering in — derived from the DEVICE
/// language (there is no in-app language setting). Consumed by notification
/// providers to localize background text where no BuildContext is available;
/// invalidated on a runtime device-locale change by
/// `LearningTrackerApp.didChangeLocales`.
// keepAlive: read by background notification code with no widget listener.

@ProviderFor(currentAppLocale)
final currentAppLocaleProvider = CurrentAppLocaleProvider._();

/// The UI locale the app is currently rendering in — derived from the DEVICE
/// language (there is no in-app language setting). Consumed by notification
/// providers to localize background text where no BuildContext is available;
/// invalidated on a runtime device-locale change by
/// `LearningTrackerApp.didChangeLocales`.
// keepAlive: read by background notification code with no widget listener.

final class CurrentAppLocaleProvider
    extends $FunctionalProvider<Locale, Locale, Locale>
    with $Provider<Locale> {
  /// The UI locale the app is currently rendering in — derived from the DEVICE
  /// language (there is no in-app language setting). Consumed by notification
  /// providers to localize background text where no BuildContext is available;
  /// invalidated on a runtime device-locale change by
  /// `LearningTrackerApp.didChangeLocales`.
  // keepAlive: read by background notification code with no widget listener.
  CurrentAppLocaleProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'currentAppLocaleProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$currentAppLocaleHash();

  @$internal
  @override
  $ProviderElement<Locale> $createElement($ProviderPointer pointer) =>
      $ProviderElement(pointer);

  @override
  Locale create(Ref ref) {
    return currentAppLocale(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(Locale value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<Locale>(value),
    );
  }
}

String _$currentAppLocaleHash() => r'517ccb5e227d69aeef54681263d4e3afd113f276';

/// The EFFECTIVE Hebrew-script decision for domain/curriculum labels: Hebrew
/// script is used when the device UI language is Hebrew (a Hebrew device shows
/// Hebrew terms automatically, and the per-profile toggle is hidden there) OR
/// when the per-profile Hebrew-Terms [useHebrewTermsProvider] toggle is on.
///
/// This is the single locale-aware source every LABEL-RENDERING path must read
/// — the breadcrumb / curriculum-tree renderers previously read the raw
/// [useHebrewTermsProvider] toggle, so on a Hebrew device with the toggle
/// persisted OFF (and hidden) they leaked Latin transliteration. Mirrors the
/// `resolveUseHebrewTerms` logic used by `DomainTermLabels`.

@ProviderFor(effectiveUseHebrewTerms)
final effectiveUseHebrewTermsProvider = EffectiveUseHebrewTermsProvider._();

/// The EFFECTIVE Hebrew-script decision for domain/curriculum labels: Hebrew
/// script is used when the device UI language is Hebrew (a Hebrew device shows
/// Hebrew terms automatically, and the per-profile toggle is hidden there) OR
/// when the per-profile Hebrew-Terms [useHebrewTermsProvider] toggle is on.
///
/// This is the single locale-aware source every LABEL-RENDERING path must read
/// — the breadcrumb / curriculum-tree renderers previously read the raw
/// [useHebrewTermsProvider] toggle, so on a Hebrew device with the toggle
/// persisted OFF (and hidden) they leaked Latin transliteration. Mirrors the
/// `resolveUseHebrewTerms` logic used by `DomainTermLabels`.

final class EffectiveUseHebrewTermsProvider
    extends $FunctionalProvider<bool, bool, bool>
    with $Provider<bool> {
  /// The EFFECTIVE Hebrew-script decision for domain/curriculum labels: Hebrew
  /// script is used when the device UI language is Hebrew (a Hebrew device shows
  /// Hebrew terms automatically, and the per-profile toggle is hidden there) OR
  /// when the per-profile Hebrew-Terms [useHebrewTermsProvider] toggle is on.
  ///
  /// This is the single locale-aware source every LABEL-RENDERING path must read
  /// — the breadcrumb / curriculum-tree renderers previously read the raw
  /// [useHebrewTermsProvider] toggle, so on a Hebrew device with the toggle
  /// persisted OFF (and hidden) they leaked Latin transliteration. Mirrors the
  /// `resolveUseHebrewTerms` logic used by `DomainTermLabels`.
  EffectiveUseHebrewTermsProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'effectiveUseHebrewTermsProvider',
        isAutoDispose: true,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$effectiveUseHebrewTermsHash();

  @$internal
  @override
  $ProviderElement<bool> $createElement($ProviderPointer pointer) =>
      $ProviderElement(pointer);

  @override
  bool create(Ref ref) {
    return effectiveUseHebrewTerms(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(bool value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<bool>(value),
    );
  }
}

String _$effectiveUseHebrewTermsHash() =>
    r'c4b2632d09b9f1e31d351fc0f5606c99fd4fe30e';

@ProviderFor(CurrentTransliterationVariant)
final currentTransliterationVariantProvider =
    CurrentTransliterationVariantProvider._();

final class CurrentTransliterationVariantProvider
    extends
        $NotifierProvider<
          CurrentTransliterationVariant,
          TransliterationVariant
        > {
  CurrentTransliterationVariantProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'currentTransliterationVariantProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$currentTransliterationVariantHash();

  @$internal
  @override
  CurrentTransliterationVariant create() => CurrentTransliterationVariant();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(TransliterationVariant value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<TransliterationVariant>(value),
    );
  }
}

String _$currentTransliterationVariantHash() =>
    r'1584ed999662925d53bb34faaf5cfe18b71895cb';

abstract class _$CurrentTransliterationVariant
    extends $Notifier<TransliterationVariant> {
  TransliterationVariant build();
  @$mustCallSuper
  @override
  void runBuild() {
    final ref =
        this.ref as $Ref<TransliterationVariant, TransliterationVariant>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<TransliterationVariant, TransliterationVariant>,
              TransliterationVariant,
              Object?,
              Object?
            >;
    element.handleCreate(ref, build);
  }
}

@ProviderFor(CurrentFontSize)
final currentFontSizeProvider = CurrentFontSizeProvider._();

final class CurrentFontSizeProvider
    extends $NotifierProvider<CurrentFontSize, FontSize> {
  CurrentFontSizeProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'currentFontSizeProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$currentFontSizeHash();

  @$internal
  @override
  CurrentFontSize create() => CurrentFontSize();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(FontSize value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<FontSize>(value),
    );
  }
}

String _$currentFontSizeHash() => r'e6bd3e513d9bb0e4424bd76093929d80ee9aab6d';

abstract class _$CurrentFontSize extends $Notifier<FontSize> {
  FontSize build();
  @$mustCallSuper
  @override
  void runBuild() {
    final ref = this.ref as $Ref<FontSize, FontSize>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<FontSize, FontSize>,
              FontSize,
              Object?,
              Object?
            >;
    element.handleCreate(ref, build);
  }
}

/// The finest [MilestoneLevel] the active profile wants celebrated for
/// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
/// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
/// the current shipped behaviour. The journey layer reads this and suppresses
/// any milestone finer than the chosen level; it can only ever remove
/// emissions, never add one.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.

@ProviderFor(SiyumGranularity)
final siyumGranularityProvider = SiyumGranularityFamily._();

/// The finest [MilestoneLevel] the active profile wants celebrated for
/// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
/// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
/// the current shipped behaviour. The journey layer reads this and suppresses
/// any milestone finer than the chosen level; it can only ever remove
/// emissions, never add one.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.
final class SiyumGranularityProvider
    extends $NotifierProvider<SiyumGranularity, MilestoneLevel> {
  /// The finest [MilestoneLevel] the active profile wants celebrated for
  /// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
  /// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
  /// the current shipped behaviour. The journey layer reads this and suppresses
  /// any milestone finer than the chosen level; it can only ever remove
  /// emissions, never add one.
  // keepAlive: mirrors a persisted preference; must survive widget unmounts.
  SiyumGranularityProvider._({
    required SiyumGranularityFamily super.from,
    required CurriculumId super.argument,
  }) : super(
         retry: null,
         name: r'siyumGranularityProvider',
         isAutoDispose: false,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$siyumGranularityHash();

  @override
  String toString() {
    return r'siyumGranularityProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  SiyumGranularity create() => SiyumGranularity();

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(MilestoneLevel value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<MilestoneLevel>(value),
    );
  }

  @override
  bool operator ==(Object other) {
    return other is SiyumGranularityProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$siyumGranularityHash() => r'8d125fff51dda37dc32703bf96bdb996bc6e64d5';

/// The finest [MilestoneLevel] the active profile wants celebrated for
/// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
/// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
/// the current shipped behaviour. The journey layer reads this and suppresses
/// any milestone finer than the chosen level; it can only ever remove
/// emissions, never add one.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.

final class SiyumGranularityFamily extends $Family
    with
        $ClassFamilyOverride<
          SiyumGranularity,
          MilestoneLevel,
          MilestoneLevel,
          MilestoneLevel,
          CurriculumId
        > {
  SiyumGranularityFamily._()
    : super(
        retry: null,
        name: r'siyumGranularityProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: false,
      );

  /// The finest [MilestoneLevel] the active profile wants celebrated for
  /// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
  /// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
  /// the current shipped behaviour. The journey layer reads this and suppresses
  /// any milestone finer than the chosen level; it can only ever remove
  /// emissions, never add one.
  // keepAlive: mirrors a persisted preference; must survive widget unmounts.

  SiyumGranularityProvider call(CurriculumId curriculum) =>
      SiyumGranularityProvider._(argument: curriculum, from: this);

  @override
  String toString() => r'siyumGranularityProvider';
}

/// The finest [MilestoneLevel] the active profile wants celebrated for
/// [curriculum] (a Riverpod family keyed by curriculum). Defaults to
/// [MilestoneLevel.unit] — the finest tier — so an unset preference reproduces
/// the current shipped behaviour. The journey layer reads this and suppresses
/// any milestone finer than the chosen level; it can only ever remove
/// emissions, never add one.
// keepAlive: mirrors a persisted preference; must survive widget unmounts.

abstract class _$SiyumGranularity extends $Notifier<MilestoneLevel> {
  late final _$args = ref.$arg as CurriculumId;
  CurriculumId get curriculum => _$args;

  MilestoneLevel build(CurriculumId curriculum);
  @$mustCallSuper
  @override
  void runBuild() {
    final ref = this.ref as $Ref<MilestoneLevel, MilestoneLevel>;
    final element =
        ref.element
            as $ClassProviderElement<
              AnyNotifier<MilestoneLevel, MilestoneLevel>,
              MilestoneLevel,
              Object?,
              Object?
            >;
    element.handleCreate(ref, () => build(_$args));
  }
}
