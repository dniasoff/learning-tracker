# AUD-docs-03 stale-story-file-target baseline — generated by
# tool/check_stale_story_file_targets.dart --update-baseline.
# Pre-existing backlog only (stories NOT named by AUD-docs-03: 19-8
# and 19-9 were archived by that fix, so they never appear here). A
# NEW violation not listed here fails `make audit`. Burn this list
# down over time — do NOT add fresh entries to paper over a
# new violation.
19-6-startup-sequence-hardening.md	lib/core/network/connectivity_service.dart
