<workflow>
  <critical>The workflow execution engine is governed by: {project-root}/_bmad/core/tasks/workflow.xml</critical>
  <critical>You MUST have already loaded and processed: {installed_path}/workflow.yaml</critical>
  <critical>Communicate all responses in {communication_language} and generate all documents in {document_output_language}</critical>

  <critical>🔥 CRITICAL MISSION: You are creating the ULTIMATE story context engine that prevents LLM developer mistakes, omissions or
    disasters! 🔥</critical>
  <critical>Your purpose is to create comprehensive Dev Notes in the Linear issue description that gives the DEV agent
    EVERYTHING needed for flawless implementation</critical>
  <critical>COMMON LLM MISTAKES TO PREVENT: reinventing wheels, wrong libraries, wrong file locations, breaking regressions, ignoring UX,
    vague implementations, lying about completion, not learning from past work</critical>
  <critical>🚨 EXHAUSTIVE ANALYSIS REQUIRED: You must thoroughly analyze ALL artifacts to extract critical context - do NOT be lazy or skim!
    This is the most important function in the entire development process!</critical>
  <critical>🔬 UTILIZE SUBPROCESSES AND SUBAGENTS: Use research subagents, subprocesses or parallel processing if available to thoroughly
    analyze different artifacts simultaneously and thoroughly</critical>
  <critical>❓ SAVE QUESTIONS: If you think of questions or clarifications during analysis, save them for the end after the complete story is
    written</critical>
  <critical>🎯 ZERO USER INTERVENTION: Process should be fully automated except for initial epic/story selection or missing documents</critical>
  <critical>📝 LINEAR IS THE SINGLE SOURCE OF TRUTH: Dev Notes are written to the Linear issue description, not to local files</critical>

  <step n="1" goal="Determine target story from Linear">
    <check if="{{story_key}} is provided by user or user provided the epic and story number such as 2-4 or 1.6 or epic 1 story 5">
      <action>Parse user-provided story info: extract epic_num, story_num, story_key</action>
      <action>Set {{epic_num}}, {{story_num}}, {{story_key}} from user input</action>
      <action>GOTO step 1b</action>
    </check>

    <action>Load {{linear_mapping_file}} to get team_id and project_id</action>
    <check if="linear_mapping_file does not exist OR team_id is empty">
      <output>Linear mapping not initialized. Run sprint-planning first to configure Linear integration.</output>
      <action>HALT</action>
    </check>

    <action>Query Linear for backlog stories:
      mcp__linear__list_issues with team={{team_id}}, project={{project_id}}, label="BMAD-Managed", state="Backlog"
    </action>
    <action>Filter results to only include stories (exclude parent epic issues by checking for "[Epic-" prefix in title)</action>
    <action>Sort results by title to get ordered story keys</action>

    <check if="no backlog stories found in Linear">
      <output>No backlog stories found in Linear with BMAD-Managed label.

**Options:**
1. Run `create-epics-and-stories` to create the product backlog
2. All stories may already have Dev Notes added (check for "Todo" status stories)

To see current status, run `sprint-planning`.
      </output>
      <action>HALT</action>
    </check>

    <action>Parse first result title to extract story key: "[1-2-name] Title" → "1-2-name"</action>
    <action>Extract {{epic_num}}, {{story_num}}, {{story_key}} from story key</action>
    <action>Store {{linear_issue_id}} for the selected story</action>
  </step>

  <step n="1b" goal="Load Linear issue details">
    <action>If {{linear_issue_id}} not set, query Linear to find issue:
      mcp__linear__list_issues with team={{team_id}}, project={{project_id}}, label="BMAD-Managed", query="[{{story_key}}]"
    </action>
    <action>Store {{linear_issue_id}} from query result</action>

    <action>Get full issue details: mcp__linear__get_issue with id={{linear_issue_id}}</action>
    <action>Extract existing description content (contains Story and Acceptance Criteria from create-epics-and-stories)</action>
    <action>Store {{original_description}} for later use</action>

    <output>📋 Selected Story: [{{story_key}}]
Linear Issue ID: {{linear_issue_id}}
Current Status: Backlog
    </output>
  </step>

  <step n="2" goal="Load and analyze core artifacts">
    <critical>🔬 EXHAUSTIVE ARTIFACT ANALYSIS - This is where you prevent future developer fuckups!</critical>

    <!-- Load all available content through discovery protocol -->
    <invoke-protocol name="discover_inputs" />
    <note>Available content: {prd_content}, {architecture_content}, {ux_content}, {project_context}</note>

    <!-- Get epic context from Linear -->
    <action>Get parent epic issue: mcp__linear__get_issue with id={{epic_parent_id}}</action>
    <action>Extract epic context from parent issue description</action>

    <!-- Extract story requirements from Linear issue -->
    <action>From {{original_description}}, extract:
      - User story statement (As a, I want, so that)
      - Acceptance criteria
      - FRs covered
    </action>

    <!-- Previous story analysis for context continuity -->
    <check if="story_num > 1">
      <action>Query Linear for previous story in this epic:
        mcp__linear__list_issues with team={{team_id}}, project={{project_id}}, query="[{{epic_num}}-{{previous_story_num}}-"
      </action>
      <action>Get previous story issue details</action>
      <action>Extract Dev Notes section from previous story description</action>

      **PREVIOUS STORY INTELLIGENCE:**
      - Dev notes and learnings from previous story
      - Review comments and corrections needed
      - Patterns established in previous implementation
      - Problems encountered and solutions found
    </check>

    <!-- Git intelligence for previous work patterns -->
    <check if="previous story exists AND git repository detected">
      <action>Get last 5 commit titles to understand recent work patterns</action>
      <action>Analyze 1-5 most recent commits for relevance to current story</action>
      <action>Extract actionable insights for current story implementation</action>
    </check>
  </step>

  <step n="3" goal="Architecture analysis for developer guardrails">
    <critical>🏗️ ARCHITECTURE INTELLIGENCE - Extract everything the developer MUST follow!</critical>

    **ARCHITECTURE DOCUMENT ANALYSIS:**
    <action>Systematically analyze architecture content for story-relevant requirements:</action>

    **CRITICAL ARCHITECTURE EXTRACTION:**
    <action>For each architecture section, determine if relevant to this story:</action>
    - **Technical Stack:** Languages, frameworks, libraries with versions
    - **Code Structure:** Folder organization, naming conventions, file patterns
    - **API Patterns:** Service structure, endpoint patterns, data contracts
    - **Database Schemas:** Tables, relationships, constraints relevant to story
    - **Security Requirements:** Authentication patterns, authorization rules
    - **Performance Requirements:** Caching strategies, optimization patterns
    - **Testing Standards:** Testing frameworks, coverage expectations, test patterns
    - **Deployment Patterns:** Environment configurations, build processes
    - **Integration Patterns:** External service integrations, data flows

    <action>Extract any story-specific requirements that the developer MUST follow</action>
    <action>Identify any architectural decisions that override previous patterns</action>
  </step>

  <step n="4" goal="Web research for latest technical specifics">
    <critical>🌐 ENSURE LATEST TECH KNOWLEDGE - Prevent outdated implementations!</critical>

    **WEB INTELLIGENCE:**
    <action>Identify specific technical areas that require latest version knowledge</action>

    <action>From architecture analysis, identify specific libraries, APIs, or frameworks</action>
    <action>For each critical technology, research latest stable version and key changes:
      - Latest API documentation and breaking changes
      - Security vulnerabilities or updates
      - Performance improvements or deprecations
      - Best practices for current version
    </action>

    **EXTERNAL CONTEXT INCLUSION:**
    <action>Include in Dev Notes any critical latest information:
      - Specific library versions and why chosen
      - API endpoints with parameters and authentication
      - Recent security patches or considerations
      - Performance optimization techniques
    </action>
  </step>

  <step n="5" goal="Write comprehensive Dev Notes to Linear issue description">
    <critical>📝 UPDATE LINEAR ISSUE - Write the ultimate developer guide to the issue description!</critical>

    <action>Build comprehensive Dev Notes section:</action>

    ```markdown
    ## Dev Notes

    ### Architecture Guidance
    [Architecture requirements, patterns, constraints relevant to this story]

    ### Technical Requirements
    - **Tech Stack:** [specific versions and libraries]
    - **File Structure:** [where new files should go, naming conventions]
    - **API Patterns:** [service structure, endpoint patterns to follow]
    - **Database:** [schemas, migrations needed]

    ### Testing Requirements
    - **Test Framework:** [framework and patterns to use]
    - **Coverage Expectations:** [what must be tested]
    - **Test Patterns:** [unit, integration, e2e requirements]

    ### Previous Story Learnings
    [What worked, what didn't, patterns to follow from previous story]

    ### Implementation Tasks
    - [ ] Task 1: [description] (AC: #1)
      - [ ] Subtask 1.1
    - [ ] Task 2: [description] (AC: #2)
      - [ ] Subtask 2.1
    [Continue for all tasks needed to complete the story]

    ### References
    - [Source: docs/architecture.md#Section]
    - [Source: PRD.md#Requirement]
    ```

    <action>Combine original description with Dev Notes:
      {{original_description}}

      ---

      {{dev_notes}}
    </action>

    <action>Update Linear issue with comprehensive description:
      mcp__linear__update_issue with id={{linear_issue_id}}, description={{combined_description}}
    </action>

    <action>Update Linear issue state to "Todo" (ready-for-dev):
      mcp__linear__update_issue with id={{linear_issue_id}}, state="Todo"
    </action>

    <action>Add comment noting Dev Notes were added:
      mcp__linear__create_comment with issueId={{linear_issue_id}}, body="Dev Notes added by create-story workflow. Story is now ready for development."
    </action>
  </step>

  <step n="6" goal="Update epic status if needed and finalize">
    <!-- Mark epic as in-progress if this is first story -->
    <check if="this is first story in epic (story_num == 1)">
      <action>Get epic parent issue: mcp__linear__get_issue with id={{epic_parent_id}}</action>
      <check if="epic state is 'Backlog'">
        <action>Update epic to "In Progress":
          mcp__linear__update_issue with id={{epic_parent_id}}, state="In Progress"
        </action>
        <output>📊 Epic {{epic_num}} status updated to In Progress</output>
      </check>
    </check>

    <action>Update {{linear_mapping_file}} with story status</action>

    <action>Report completion</action>
    <output>**🎯 ULTIMATE BMad Method STORY CONTEXT CREATED, {user_name}!**

**Story Details:**
- Story Key: {{story_key}}
- Linear Issue: {{linear_issue_id}}
- Status: Todo (ready-for-dev)

**Dev Notes written to Linear issue description include:**
- Architecture guidance and constraints
- Technical requirements and specifications
- Testing requirements and patterns
- Previous story learnings
- Implementation tasks breakdown

**Next Steps:**
1. View the story in Linear to review Dev Notes
2. Run `dev-story` to implement the story
3. Run `code-review` when complete

**The developer now has everything needed for flawless implementation!**
    </output>
  </step>

</workflow>
