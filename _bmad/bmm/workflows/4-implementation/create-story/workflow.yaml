name: create-story
description: "Add comprehensive Dev Notes to a Linear story issue, making it ready for development"
author: "BMad"

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/config.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
document_output_language: "{config_source}:document_output_language"
date: system-generated
planning_artifacts: "{config_source}:planning_artifacts"
implementation_artifacts: "{config_source}:implementation_artifacts"

# Workflow components
installed_path: "{project-root}/_bmad/bmm/workflows/4-implementation/create-story"
instructions: "{installed_path}/instructions.xml"
validation: "{installed_path}/checklist.md"

# Variables and inputs
variables:
  story_key: "" # Will be auto-discovered from Linear or provided by user

  # Linear is the single source of truth
  linear_mapping_file: "{implementation_artifacts}/linear-mapping.yaml" # Maps BMAD items to Linear issue IDs

# Project context for analysis
project_context: "**/project-context.md"

# Smart input file references - used for Dev Notes analysis
# These planning artifacts are analyzed to create comprehensive Dev Notes
input_file_patterns:
  prd:
    description: "PRD for requirements context"
    whole: "{planning_artifacts}/*prd*.md"
    sharded: "{planning_artifacts}/*prd*/*.md"
    load_strategy: "SELECTIVE_LOAD"
  architecture:
    description: "Architecture for technical constraints"
    whole: "{planning_artifacts}/*architecture*.md"
    sharded: "{planning_artifacts}/*architecture*/*.md"
    load_strategy: "SELECTIVE_LOAD"
  ux:
    description: "UX design for interface requirements"
    whole: "{planning_artifacts}/*ux*.md"
    sharded: "{planning_artifacts}/*ux*/*.md"
    load_strategy: "SELECTIVE_LOAD"
